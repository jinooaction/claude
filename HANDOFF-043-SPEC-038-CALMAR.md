# HANDOFF 043 — 스펙 038: 칼마 비율 (자본 방어 측정, 2026-06-04)

main 머지 `9ae4238`(PR #172). Kernel 터치 0건, 돈 0 이동. 운영자 지시: "계속해(옵션 2 =
Calmar 정밀화). 그리고 실거래도 앞으로 항상 실거래 기반으로 고도화하고 싶다 — 돈을 못 벌면
이 고도화 아무 의미 없다."

## 한 줄 요약

forward-verdict 가 샤프(변동성 대비 수익)로만 판정했는데, 추세 필터(스펙 036)의 핵심 가치는
*드로다운 방어*다. 그래서 **칼마(연환산수익률/최대낙폭)**를 일급 지표로 추가해 "돈을 잃지
않는 능력"을 직접 측정한다. 페이퍼·라이브 트랙 공통.

## 무엇을

- `calmar_ratio(total_return_pct, max_drawdown_pct, n_obs)` — CAGR%/최대낙폭%. 낙폭 0·전손·
  기간 0 이면 None(정의 불가, 정직히).
- `EdgeVerdict` + `strategy_calmar`·`benchmark_calmar`·`beats_benchmark_calmar`(schema 1.1).
  JSON·text·판정 사유에 반영. **게이트는 불변**(EDGE 는 샤프+PSR+DSR), 칼마는 *보고* 전용 —
  추세 필터가 샤프-엣지는 못 넘어도 칼마 우위가 있으면 사유에 명시(운영자가 드로다운 방어
  를 직접 보게). 게이트 약화 없음.
- 신규 테스트 7건. 전체 1485 통과·4 스킵, 린트 깨끗.

## ⚠ 다음 큰 작업 — 실거래 격차 (운영자 핵심 지적)

운영자가 거듭 강조: **"앞으로 항상 실거래 기반으로 고도화. 돈 못 벌면 의미 없다."**

현 상태의 격차:
- **고도화(스펙 032~038)**: 포트폴리오 재조정·다요인·추세 필터·forward 판정·A/B·칼마 — 전부
  **페이퍼 전용**.
- **실거래(라이브 캐너리)**: `deploy/canary-live-rules.toml` = 단순 3룰 qty=1(SPY 눌림목 1주,
  MSFT 골든크로스 1주, AAPL), $12k 자본, `AUTO_INVEST_MODE=live`(2026-05-30 무장). 즉 정교한
  고도화가 **실제 돈을 안 만진다.**

다음 작업 = **고도화(포트폴리오/추세 전략)를 라이브 캐너리에 올리기**(소액 실거래):
- 보수적 라이브 포트폴리오 설정(추세 ON 방어, 소수 우량주, 타이트 캡, 실계좌 화이트리스트).
- `rebalance-once --mode live --confirm-live` 가드 경로 + go-live 채널(`automation/
  go-live-canary.request` 패턴) 또는 워커 루프 배선.
- 킬스위치: 스펙 014 손실 서킷 브레이커.
- **돈 움직이는 결정**이므로 자본 금액·드로다운 한도는 운영자 확정 필요(헌법 X.4: 라이브
  전환은 운영자 결정, 자율 무장은 소액 캐너리·전체 캡·가드 채널 한정).
- ⚠ 긴장: 새 전략은 아직 forward 미검증(verdict=INSUFFICIENT_DATA). 그러나 헌법 VI 2단계
  캐너리 = *소액 실거래 검증* 단계이므로, 소액 라이브 캐너리는 운영자 지시 하에 정당.

## 검증 도구는 이제 라이브에도 적용된다

스펙 035 forward-verdict + 스펙 038 칼마는 `--mode live` 로 실거래 트랙도 같은 잣대로 판정
한다(라이브 NAV 스냅샷이 쌓이면). 즉 고도화를 실거래에 올리면 그 실거래 P&L 을 디플레이티드
샤프 + 칼마로 정직히 판정할 수 있다 — "실거래 기반 고도화"의 측정 토대는 준비됨.
