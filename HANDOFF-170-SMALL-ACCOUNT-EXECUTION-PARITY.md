# HANDOFF-170 - 소액 계좌 실행 동등성과 증거 판정 교정

## 상태

PR #689와 후속 교정 PR #690이 `main`에 병합됐다. 현재 시스템은 약 145달러 연구
자본으로 실제 정수 주 주문을 구성할 수 있고, 신호 ETF와 실행 ETF의 최근 252개 공통
거래일 동등성을 자동 검증한다. 실행 계층은 준비됐지만 전략 증거가 부족해 현재 상태는
`WAIT_EDGE`, 단 0, 자본 0, 주문 0건이다.

## 운영자 의심에 대한 결론

운영자의 의심은 일부 맞았다. 전략이 모두 나빠서만 멈춘 것이 아니었다.

1. 과거 연구 진입 기준은 같은 탐색 위험을 겹쳐 처벌해 검출력이 낮았다. PR #687에서
   실제 17개 연구 가족 기준으로 교정했다.
2. 약 145달러로 고가 ETF를 정수 주 매수할 수 없어, 좋은 전략도 첫 자본을 실제 주문으로
   표현하지 못하는 실행 설계 오류가 있었다. PR #689에서 저가 실행 ETF로 분리했다.
3. 실패한 동등성 문서도 해시가 진짜라는 이유만으로 통과시키는 판정 오류가 있었다.
   PR #690에서 `passed=true`를 필수로 만들었다.

세 오류를 모두 고친 뒤에도 현재 배포 전략은 최근 구간에서 기준자산을 이기지 못했다.
따라서 지금 남은 차단점은 "영원히 통과할 수 없는 시스템"이 아니라 "현재 엣지 미확정"이다.

## 무엇을 고쳤나

- 신호는 `SPY/IEF/GLD`로 유지하고 실제 주문은 `SCHX/SPTI/IAUM`으로 바꿨다.
- 252개 공통 거래일, 수익률 상관 0.95 이상, 연 추적오차 6% 이하, 연 수익률 차이
  3%포인트 이하, 중앙 거래대금 100만 달러 이상, 자료 7일 이내를 고정했다.
- 동등성 증거는 36시간 안이어야 하고 내용 해시와 `passed=true`가 모두 맞아야 한다.
- 생산 동등성 점검은 장기 전략 DB를 바꾸지 않고 매번 깨끗한 임시 KIS DB에서 계산한다.
- 강화 캐너리는 종목별 거래일 합집합이 아니라 모든 종목의 최신 공통 거래일을 사용하고,
  내부 누락 거래일을 별도로 검사한다.
- 첫 자본 진입과 첫 실제 체결 직전 재검증 모두 주문 구현성과 동등성을 필수로 요구한다.

## 생산 사실

- 기능 merge: `ecc84b93abda36efc5c104cf5894be664d70f15a` (#689).
- 교정 merge: `0e8db900947fcfb510a22caee507ae270d321fb3` (#690).
- 최종 배포: `33025942987`, 성공.
- KIS 읽기 전용 점검: `33025962368`, 6/6 통과, 최근 주문·미체결 0건.
- 최종 자동 판정: `33026038166`, `WAIT_EDGE`, 단 0 -> 0, 자본·주문 0건.
- 강화 캐너리: 공통 거래일 45개, 감사 무결성 구멍 0건, `PASS`.
- 주문 구현성: 145달러로 IAUM 1주와 SCHX 2주, `fundable=true`.

| 신호 -> 실행 | 수익률 상관 | 연 추적오차 | 연 수익률 차이 | 결과 |
| --- | ---: | ---: | ---: | --- |
| SPY -> SCHX | 0.996963 | 1.0010% | 0.6255%포인트 | PASS |
| IEF -> SPTI | 0.976832 | 1.5789% | 0.2692%포인트 | PASS |
| GLD -> IAUM | 0.999651 | 0.8215% | 0.5123%포인트 | PASS |

## 마땅한 전략이 정말 없는가

역사적으로 가장 강한 후보는 `globalfixed-ensemble-3-6-9-12`다. 235개월 홀드아웃에서
비용 후 연복리 8.68%, 샤프 1.83, 최대낙폭 5.57%였고 기준자산은 각각 8.29%, 1.26,
17.27%였다. "검토한 모든 전략이 쓸모없다"는 말은 사실이 아니다.

다만 현재 5년 앵커드 평가는 표본 외 샤프 1.94여도 세 구간 모두 기준자산 대비 샤프
우위에 실패했고 평균 샤프도 단순 보유보다 낮다. 깨끗한 전진 관측도 1개뿐이다. 그러므로
역사적 최우수 후보와 현재 실거래 합격 전략을 같은 것으로 부르면 안 된다.

## 다음 우선순위

같은 후보를 기다리기만 하지 않는다. 다음 전략 작업은 `globalfixed`의 최근 실패 원인을
직접 겨냥하는 독립 저회전·레짐 적응 전략군을 결과 확인 전에 사전등록하고, 교정된 가족
관문과 현재 실행 동등성으로 즉시 검증한다. 목표는 다음 세 가지다.

1. 최근 주식·채권 동반 약세에서도 현금·금·단기채 전환으로 손실을 줄인다.
2. 월 1회 이하 저회전으로 소액 정수 주 오차와 비용을 견딘다.
3. 개발 선택, 격리 구간, 홀드아웃, 현재 앵커드 구간을 분리해 사후 선택을 막는다.

## 검증

- 전체 테스트: 3118 passed, 6 skipped.
- 린트: `uv run ruff check src tests` 통과.
- 엄격 하네스 14/14, HANDOFF 사실 검사, YAML·셸 구문, PR 품질 관문 통과.
- 안전 경계: 연구 자본 10%, 손실 예산, 정규장, 킬스위치, 주문·노출 한도, 외부 보유
  제외를 유지했다. 실패 문서 거부는 안전 경계를 완화하지 않고 더 엄격하게 만든다.
