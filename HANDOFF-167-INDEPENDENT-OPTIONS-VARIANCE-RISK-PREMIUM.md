# HANDOFF-167 - 독립 옵션 변동성 위험 프리미엄·752회 판정

## 상태

PR #682가 merge 커밋 `2141a7c`로 `main`에 들어갔다. 생산 전략 공장, 서버 배포,
현재 main KIS 읽기 전용 점검이 모두 성공했다. 최종 판정은
`GATE_OR_REFERENCE_SUSPECT`이며 돈 경로는 `PREVIEW_ONLY`, `ACCUMULATING_EDGE`,
단 0, 자본 0, 실주문 불가다.

## 무엇을 확인했는가

- 공식 Cboe PUT/VIX, Kenneth French 일별 시장·무위험 수익, FRED DGS3MO를 사용했다.
- Cboe 파일의 고립된 2007년 이전 7개 행은 다년 공백 수익률을 만들지 않도록 제외했다.
- 결과 전에 16개 후보, 개발 84개월, 격리 1개월, 홀드아웃 최소 120개월, 비용
  25/50/100bp와 회전비용 10/25/50bp, 꼬리손실 관문을 고정했다.
- 실제 정렬은 2007-03~2026-06의 232개월이며 홀드아웃은 147개월이다.

## 기준 의심에 대한 결론

- 통계 장치는 망가지지 않았다. 500회 16후보 선택 모의실험에서 평균 0 전략 거짓
  합격률은 `2.4%`, 심은 엣지 검출률은 `81.6%`, 올바른 선택률은 `83.8%`다.
- 알려진 100% PUT은 50bp 비용 후에도 현금보다 연 `+5.177143%`, PSR `0.964744`,
  최대낙폭 `20.779543%` 대 주식 `24.899519%`, 최악 5% 월평균 `-6.728762%` 대
  주식 `-8.950168%`다. 경제적 위험 프리미엄은 존재한다.
- 하지만 최근 2014-2026 주식 샤프가 더 높아 PUT의 샤프 개선이 `-0.124261`이었다.
  “프리미엄 존재”와 “주식보다 나은 대체 포트폴리오”를 같은 관문에 묶은 것이 오류다.
- 기준을 결과에 맞춰 낮추지 않았다. 경제적 프리미엄 진단은 승격 불가 설명 항목이고,
  원래 채택 실패를 유지해 최종 판정을 `GATE_OR_REFERENCE_SUSPECT`로 냈다.

## 선택 후보와 사후 감사

- 사전 선택 후보는 `options-vrp-tail_guarded-7a1729980754`다.
- 홀드아웃 PSR `0.803758`, 연 현금 초과 `+0.662141%`, 주식 대비 샤프
  `+0.039261`, 최대낙폭 `6.427959%`, 최악 5% 월평균 `-2.166609%`다.
- 관찰 기준은 통과하지만 실거래급 기준의 PSR 0.95, 연 현금 초과 2%, 샤프 개선
  0.05를 통과하지 못했다.
- 같은 50% 단순 PUT보다 연 `-1.958738%`, 샤프 `-0.043496`라 자동 타이밍
  추가가치는 확인되지 않았다.
- 50% 단순 PUT과 12개월 릿지 50%가 사후 실거래급 수치 관문을 통과했다. 홀드아웃을
  본 뒤 알게 된 결과라 `promotion_allowed=false`이며 현재 승격할 수 없다.

## 생산 증거

- merge: `2141a7c972cf3a125a3b7bd0801d73b5d76ce4ed`
- strategy factory: `32897412078` success, 16/16 current, 736 prior,
  752/752 unique
- deploy: `32897412147` success
- KIS read-only smoke: `32898262246` success, main commit, 5/5
- KIS: cash $934.27, NAV $1454.23, external ORANY 28 shares,
  recent orders 0, open unfilled 0
- money path: main `2141a7c`, `PREVIEW_ONLY`, `ACCUMULATING_EDGE`, rung 0/5,
  capital $0, forward 0/20, real-order submission false

## 검증

- `uv run pytest`: 3084 passed, 5 skipped
- `uv run ruff check src tests`: 통과
- YAML, 실제 752개 JSON 계약, 엄격 하네스 14/14, HANDOFF 사실 검사,
  PR 품질 관문: 통과

## 안전 경계

실제 주문, 자본, 마진, 무장, 허용목록, 포지션 한도, 손실 예산, 헌법, 커널은
바꾸지 않았다. Cboe PUT은 가상 기준지수이며 PUTW/SPX 옵션 실행 코드, 배정·세금·담보·
마진 규칙, 역사 정합, 허용목록, 강화 캐너리, 소비자 지문 정합이 없다.

## 다음 우선순위

1. 현재 홀드아웃에서 찾은 두 사후 후보를 그대로 승격하지 않는다.
2. 독립 Cboe 옵션 기준지수와 중첩형 워크포워드를 사용하는 목적·선택 교정 전략군을
   새 등급 4 SDD로 결과 전에 사전등록한다.
3. 프리미엄 존재, 포트폴리오 채택, 자동 타이밍 추가가치를 세 개의 별도 질문으로 유지한다.
4. 과거 홀드아웃은 설계 자료로만 쓰고 새 승격 증거로 재사용하지 않는다. 사후 후보의
   실제 채택 판단은 독립 기준지수 복제 또는 자본 없는 새 전진 관측이 필요하다.
