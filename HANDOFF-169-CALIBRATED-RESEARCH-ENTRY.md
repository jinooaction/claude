# HANDOFF-169 - 연구 가족 기준과 검정력 교정

## 상태

PR #687이 `main`에 병합됐다. 생산 전략 공장, 자동 자본 판정, KIS 읽기 전용 점검까지
새 코드 기준으로 완료했으며 현재 상태는 `PREVIEW_ONLY`, 단 0, 자본 0, 주문 0건이다.
서버 코드 교체는 미국 장중 배포 금지에 따라 장 마감 뒤 타이머로 연기됐다.

## 운영자 의심에 대한 결론

운영자의 의심은 맞았다. 이전 `family-complete-v3`는 같은 탐색 과적합을 DSR 0.95,
PBO 0.10, 원시 752개 Bonferroni로 겹쳐 막았다. 거짓 합격은 거의 없었지만 연환산 샤프
0.6을 심은 전략도 약 36~42%만 찾아, “좋은 전략도 지나치게 탈락시키는 기준”이었다.

그렇다고 현재 후보가 억울하게 탈락한 것은 아니다. 중복 문턱을 제거한 뒤에도 현재 옵션
가족의 PBO는 `0.371429`로 새 상한 `0.25`보다 높고 선택 후보도 없다. 기준 오류와 후보
실패가 동시에 있었으며, 이번 작업으로 둘을 분리했다.

## 바뀐 기준

1. 752개 원시 후보를 실제 17개 연구 가족으로 재구성한다.
2. 연구 자본 진입은 홀드아웃 PSR `0.95`와 현재 가족 PBO `0.25`를 함께 요구한다.
3. DSR `0.95`와 원시 Bonferroni는 계속 재계산하지만 중복 차단하지 않는 진단값이다.
4. 가족당 거짓 진입 상한은 `0.01`, 프로그램 전체 예산은 `0.20`, 최대 가족은 20개다.
5. 소비자가 가족 식별자·개수·지문·현재 개발 승자·PSR·PBO를 원시 행에서 다시 계산한다.
6. 당시 시점 자료, 과거 자료 미재사용, 기준·실행 동등성, 자금 구현성은 계속 필수다.
7. `gate_version=3.1`만 자본을 열 수 있고 v3.0, v2, 구형 증거는 진단 전용이다.

## 검정력과 거짓 합격

고정 시드 60,000, 500회, 후보 상관 0.8, 개발 204개, 홀드아웃 235개 관측으로 측정했다.

| 가족 크기 | 무효 진입률 | 샤프 0.6 검출률 | 요구사항 |
|---:|---:|---:|---|
| 16 | 1.0% | 84.0% | 1% 이하, 80% 이상 |
| 64 | 0.4% | 80.4% | 1% 이하, 80% 이상 |

현재 17개 가족의 프로그램 거짓 진입 상한은 `0.17`로 예산 `0.20` 안이다. 21번째 가족은
예산을 넘어 자동 차단된다.

## 생산 사실

- 기능 PR: #687, merge `42f3c54865e989a01912ece56d2eb77f3f1f8ac7`.
- 전략 공장: `32979548961`, gate `3.1`, 752/752 고유 행, 17/17 가족,
  현재 PBO `0.371429`, 선택 후보 없음, `eligible=false`.
- 자동 자본 판정: `32980657704`, `WAIT_EDGE`, 단 0→0, 자본 이동·주문 0건.
- 자금 구현성: NAV `$1456.75`, 연구 자본 `$145`, SPYM 미리보기 한 주 `$90.24`.
  주문별 50% 캡보다 커 `SKIPPED_PER_TRADE_CAP`, `fundability_passed=false`.
- KIS 읽기 점검: `32980668128`, 5/5 통과, 현금 `$934.27`, NAV `$1456.75`,
  외부 보유 ORANY 28주, 최근 주문·체결 행 0, 미체결 0.
- 배포: `32979548873`은 systemd 유닛 동기화에 성공했지만 미국 장중 코드 교체를 거부했다.
  다음 허용 시각 `2026-08-26T20:00:00Z`, 자동 타이머 `21:00 UTC`다.

## 마땅한 전략이 정말 없는가

“세상에 전략이 없다”는 결론은 아니다. 이 저장소에서 독립 검증한 범위의 현재 상태다.

- `globalfixed-ensemble-3-6-9-12`는 235개월 홀드아웃에서 비용 후 연복리 8.68%, 샤프
  1.83, 최대낙폭 5.57%로 가장 강한 역사 후보다. 다만 현재 전진 관측은 1개뿐이고 강화
  점검은 감사 무결성 실패 2건이라 자본 진입 증거가 아니다.
- 옵션 PUT는 한 지수에서 현금 대비 프리미엄이 보였지만 독립 WPUT에서 재현되지 않았고,
  현재 가족 PBO도 0.371429다.
- 다른 외환·국채·신용·원자재·수급·에너지 가족은 일부 경제적으로 유망한 사후 후보가
  있었지만 사전 선택 승자가 독립 문턱을 모두 통과하지 못했다.

따라서 “마땅한 후보가 전혀 없었다”가 아니라 “역사적으로 유망한 후보는 있으나 지금
자동 주문을 정당화하는 독립·실행 가능 증거를 갖춘 후보는 없다”가 정확하다.

## 다음 우선순위

통계 문턱을 더 낮추면 안 된다. 다음은 서로 독립인 두 실행 문제다.

1. 약 145달러 연구 자본으로도 한도를 지키며 만들 수 있는 저가 상품, 단일 다리,
   또는 중개인이 실제 지원하는 소수점 주문 경로를 사전등록하고 검증한다.
2. `globalfixed` 강화 점검의 감사 무결성 실패 2건을 원인별로 재현해, 실제 결함이면 고치고
   자료 범위 문제면 사전등록된 대체 충격 구간으로 다시 검증한다.

정수 주식 한 주가 주문 캡보다 큰 문제는 통계 기준을 바꿔 해결할 수 없다. 이 두 문제가
해결되기 전에는 어떤 새 전략이 합격해도 첫 10% 자본을 정확히 표현하지 못한다.

## 검증

- 집중 테스트: 60 passed.
- 전체 테스트: 3106 passed, 5 skipped.
- 건너뛴 5개: `KIS_LIVE_TEST=1` 전용이며 생산 읽기 점검에서 5/5 별도 통과.
- 린트: `uv run ruff check src tests` 통과.
- 워크플로 YAML: Ruby Psych 파싱 통과.
- 엄격 하네스: 14/14 통과.
- HANDOFF 사실 검사와 PR 품질 관문 통과.
