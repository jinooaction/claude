# HANDOFF-157 - 독립 거시 전략 공장과 512회 검정

## 상태

PR #657이 `main`에 병합됐고 배포, 공개 자료 수집, KIS 실연결 점검, 생산 전략
공장이 모두 성공했다. 생산 512회 판정은 `NO_FACTORY_EDGE`이므로 권위 상태는
`armed:false`, 단 0/5, 자본 0이며 실주문은 불가능하다.

## 무엇을 만들었나

- 수익률곡선 경기 순환, 물가 방향, 실시간 고용 충격, VIX 충격 회복을 각각
  16개씩 총 64개 후보로 결정론적으로 생성한다.
- FRED DGS2·DGS10·CPIAUCNS·SAHMREALTIME과 Cboe VIX를 시점 기준으로 읽고,
  월간 자료에는 45일 발표 지연을 적용해 미래 정보 누출을 막는다.
- 사전 탐색 192회와 기존 생산 256회를 공식 64회에 합쳐 DSR과 PBO의 총 검정
  횟수를 512회로 고정한다. 전략 지문도 512개가 모두 고유해야 한다.
- 연구와 주문 준비는 같은 `macro_target_weights` 함수를 사용한다. 자료 신선도,
  교차 검증, 후보 ID, 전략 지문, 공장 코드 커밋 계보가 어긋나면 브로커 호출 전
  실패 폐쇄한다.
- BLS 직접 익명 API는 할당량 소진 시 실패할 수 있어 최선 노력 보고용으로 남겼다.
  투자 판단 CPI 대조는 FRED와 DBnomics의 BLS 미러로 고정하고, FRED 최신성은
  별도 70일 관문으로 확인한다.

## 생산 결과

- main: `001254b`(#657), 기능 커밋 `3709cab`.
- 공개 자료 run `32608652165`: 13/13 발행, `overall_ok=true`, 교차 검증 5/5 통과.
- 전략 공장 run `32608652168`: 묶음 `macro-strategy-factory-0e24eeb78685`.
- 검정 수: 생산 256, 사전 탐색 192, 공식 64, 합계 512, 고유 지문 512.
- 판정: `NO_FACTORY_EDGE`, 선택 후보 없음, 연구 캐너리 자격 없음.
- 잠정 최고: `macro-curve_cycle-a9f40878ef88`.
- 통과: DSR `0.999970`, 구간 승률 `0.60`, 샤프 우위 `0.212173`, 칼마 우위,
  낙폭 방어, 50bp 비용 후 양의 수익.
- 실패: PBO `0.833333 > 0.10`, PSR `0.812002 < 0.95`.

## 계좌와 주문

- `armed:false`, 단 0/5, 승인 자본 0, 계좌 NAV 1457.59달러.
- KIS smoke run `32608652207`: 5/5 통과, 현금 934.27달러, ORANY 28주.
- 최근 주문 0건, 열린 미체결 0건, 신규 접수·체결 0건, 전략 손익 0달러.
- 최신 money-path는 `PREVIEW_ONLY`, `can_submit_real_orders=false`다.
- 합격 전략이 없으므로 강화된 주문 없는 검사와 자동 무장은 실행하지 않았다.

## 배포와 검증

- 배포 run `32608652200`: 성공.
- `uv run pytest -q`: 2949 passed, 5 skipped.
- `uv run ruff check src tests`: 통과.
- `uv run python scripts/agent_harness_probe.py --strict`: OK 14/14.
- `uv run python scripts/check_handoff_facts.py`: OK.
- 변경 Python 포맷, workflow YAML, 셸 구문, `git diff --check`, PR 품질 관문: 통과.

## 안전 경계

공장 합격 기준을 낮추지 않았다. K1 포지션 한도, K2 허용 종목, 손실 예산 20%,
정규장, 현금 1% 여유, production 기계 승인, 서명·nonce, 추가 전용 감사 로그를
유지한다. 이 기능 출시와 생산 검증에서 실제 주문, 자본 배분, live 전략 교체는
발생하지 않았다.

## 다음 세션 판단

거시 국면 후보도 통계적으로 실거래할 수준이 아니다. 같은 문턱 조합을 반복하거나
PBO·PSR 기준을 낮추지 않는다. 다음 전략군은 `independent_asset_universe_or_carry`다.
새 자산군의 독립 수익 원천 또는 캐리 수익을 먼저 사전등록하고, 이번 512회까지
포함한 다중검정과 같은 주문 전 관문으로 검증해야 한다. 합격 전략의 실제 체결과
양의 전략 손익 전에는 돈을 벌었다고 보고하지 않는다.
