# HANDOFF-155 - 최신 엣지 재검증과 자동 무장 해제

## 상태

PR #646~#651이 `main`에 병합됐고 배포, KIS smoke, 주문 없는 live canary,
자본 사다리 자동 강등, 전략 손익 측정, money-path 재평가가 모두 성공했다.

현재 전략은 전진 관측 47개에서 PSR `0.601730 < 0.95`로 `NO_EDGE`다.
따라서 권위 상태는 `armed:false`, 단 0, 자본 0이며 실주문은 불가능하다.
전략 체결은 0건이고 실현·미실현·총손익은 모두 0달러다.

## 무엇을 고쳤나

- 첫 전략 체결 전에는 매 실제 주문 직전에 최신 엣지 자격을 다시 확인한다.
- 역사 홀드아웃, 전진 관측 40개 이상, PSR 0.80 이상, 칼마 우위, 강화 캐너리
  PASS, 36시간 이내 증거를 모두 요구한다.
- 증거 실패·누락·오래됨·모순은 서명과 브로커 주문 전에 차단한다.
- 첫 체결 전 최신 탐색 자격이 실패하면 자본 사다리가 단 1을 0으로 자동 강등한다.
- 체결 뒤에는 최신 연구 실패만으로 위험 축소 매도를 막지 않고 기존 라이브 손실·정합성
  관문을 우선한다.
- 수익 증거에 표준 실행 시각을 넣고 pipeline 감시기에 개별 sidecar ref 재수집을 추가했다.
- 전진 엣지가 미달이어도 같은 자료를 반복하지 않는 주문 없는 병렬 challenger를 선택한다.
- 주문 시도 뒤 체결 동기화, 전략 손익, 정합성, halt 결과를 한 실행 증거로 닫는다.
- 무장 해제 뒤 계좌 NAV는 읽기 전용 측정 분모로만 사용하고 승인 자본 0은 유지한다.
- GitHub Actions 기본 권한은 읽기 전용으로 유지하면서 자동 상태 PR 생성 허용 설정을 켰다.

## 생산 증거

- 핵심 기능: PR #646, main `be08514`, 기능 `ed5c57e`.
- 수익 증거 시각 계약: PR #647, main `e708650`.
- 자본 사다리 전략 성과 입력: PR #648, main `001ca81`.
- 자동 강등 상태: PR #649, main `bf48336`.
- 무장 해제 뒤 손익 관찰: PR #650, main `54db9f6`.
- 계좌 NAV 소수점 정규화: PR #651, main `6856294`.
- 자동 강등: run `32570303365`, 단 1 -> 0, 자본 0.
- 자동 강등 후 재평가: run `32570553036`, `WAIT_EDGE`, 단 0 유지.
- 병렬 후보: `candidate-parallel-edge-challenger-9325da7f6b01`, `EXECUTION_READY`, no-live.
- pipeline liveness: run `32569968280`, `OK`.
- 주문 없는 live canary: run `32570485409`, 실주문 job 생략, 주문 0건.
- live profit: run `32570966264`, `NO_FILLS_YET`, 체결 0건, 총손익 0달러.
- money-path: run `32571001761`, `PREVIEW_ONLY`, 실주문 불가.
- 배포: run `32570962053`, 성공.
- KIS smoke: run `32571047289`, main `6856294`, 5/5 통과, 열린 미체결 0건.
- 계좌 관찰: NAV 1457.59달러, 현금 934.27달러, 외부 보유 ORANY 28주.

## 검증

- `uv run pytest -q`: 2917 passed, 5 skipped.
- `uv run ruff check src tests`: 통과.
- `uv run python scripts/agent_harness_probe.py --strict`: OK 14/14.
- `uv run python scripts/check_handoff_facts.py`: OK.
- PR 품질 관문과 GitHub 검사: 통과.

## 안전 경계

K1 포지션 한도, K2 허용 종목, 손실 예산 20%, 정규장, 현금 1% 여유,
production 기계 승인, 서명·nonce, 추가 전용 감사 로그는 유지된다. ORANY 28주는
시스템 시작 전 외부 보유라 전략 손익에서 제외한다. 신규 challenger는 주문·취소·자본
배분·live 전략 변경을 하지 않는다. 검증 기준을 낮추거나 수익을 보장하지 않는다.

## 다음 세션 판단

현재 전략을 억지로 주문하지 않는다. 자율 작업이 선택한 병렬 challenger를 주문 없이
검증하고, 새 증거가 모든 관문을 통과할 때만 자본 사다리가 상태 PR을 만들어 다시 무장한다.
그 전까지는 `PREVIEW_ONLY`가 정상이다. 체결과 양의 전략 손익 전에는 돈을 벌었다고
보고하지 않는다.
