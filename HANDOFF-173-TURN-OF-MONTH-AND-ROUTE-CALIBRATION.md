# HANDOFF-173 - 판정 경로 교정과 독립 월말·월초 전략

## 상태

PR #699가 `main`의 `fc5af95`로 병합됐고 배포 실행 `33318101840`이 성공했다.
같은 커밋의 KIS 읽기 전용 스모크 `33318101829`도 6/6 통과했다. 워커는 드라이런이며,
공식 전략 공장 `33318101856`도 성공해 784개·19가족 판정을 sidecar에 게시했다.
실거래 전환·자본 배분·실주문·라이브 전략 변경은 없었다.

## 운영자 질문에 대한 결론

“수많은 전략이 모두 탈락하는 것이 프로그램 오류이거나 프로그램이 둔한 것 아닌가”라는
문제 제기는 일부 맞았다. 실제 오류와 설계 왜곡이 있었다.

1. 빈 앵커드 판정을 표준 판정과 항상 결합하면서 표준 `significance_method`를 잃어,
   정상 `EDGE_CONFIRMED`도 `LEGACY_EDGE_EVIDENCE`로 잘못 막을 수 있었다.
2. 표준 전진 교정은 `UNDERPOWERED`와 오합격 통제 실패를 구분했지만 자본 소비자가 그
   결과를 읽지 않았다.
3. 앵커드·탐색 경로는 각자 전체 경로 교정 없이 자본 진입에 쓰일 수 있었다.
4. 중앙 장부의 752개는 752개의 독립 전략이 아니라 17개 가족의 상관된 변형이었다.
   별도 출시된 레짐 가족 16개도 중앙 장부에서 빠져 있었다.
5. 돈 경로 설명은 첫 자본 10%라고 썼지만 표준 경로 코드는 단2 20%로 직접 들어갔다.

따라서 “현재 실자본 합격 전략이 없다”는 결과는 유지되지만, “프로그램에는 문제가 없다”는
결론은 틀렸다. 오류는 고쳤고 기준은 낮추지 않았다.

## 판정·돈 경로 수정

- 표준 전진 교정은 `false_positive_control_passed`와 `detection_power_passed`를 따로 낸다.
- 같은 코드 커밋, 같은 짝수익 유의성 방법, 두 교정 불리언, `CALIBRATED`를 모두 만족해야
  표준 경로가 상향 자본 증거가 된다.
- 앵커드와 탐색 판정은 원본 연구 증거를 보존하지만 별도 전체 경로 교정 전에는 진단 전용이다.
- 모든 상위 단 승격과 NAV 증가 상향 재산정은 현재 표준 경로 자격을 다시 확인한다.
  강등·정지·매도·NAV 감소 하향 재산정은 교정 실패로 막지 않는다.
- 공장 첫 진입 10%, 표준 첫 진입 20%를 코드와 돈 경로 설명에서 분리했다.

## 독립 월말·월초 전략 결과

기존 추세·거시·금리·신용·외환·원자재·옵션과 다른 달력 효과를 검증했다. 월말 1~4거래일과
월초 1~4거래일의 16개 조합을 결과 전에 고정하고, 1926-07~2005-12 개발, 2006 차단,
2007-01~2026-06 홀드아웃으로 한 번만 재생했다.

- 개발 승자: `calendar-turn-last2-first4-225649baec83`.
- 가족 PBO: `0.126984`로 과적합 관문 통과.
- 홀드아웃 현금 초과 PSR: `0.619345`로 0.95 기준 실패.
- 홀드아웃 연 현금 초과수익: `+0.213632%`로 0.50% 기준 실패.
- 양의 시대 구간: 2/4로 3/4 기준 실패.
- 단일 연도 기여 집중: `29.91%`로 25% 기준 실패.
- 편도 25bp 비용 스트레스: 연 `-3.335994%`로 실패.
- 최대 낙폭: `32.91%`로 시장 `50.17%`보다 작고, 이동 위약시험은 정상 실패했다.
- 최종 판정: `NO_FACTORY_EDGE`, 종이 관찰 후보도 아님.

즉 달력 신호 흔적은 있지만 비용 후 크기·신뢰도·시간 분산이 실자본 기준에 못 미친다.
기준을 완화하거나 홀드아웃에서 다른 창을 다시 고르지 않았다.

## 감사 장부와 현재 돈 상태

- 중앙 기존 752개 + 출시 레짐 16개 + 달력 16개 = 784개 고유 후보.
- 독립 전략 가족 19개, 프로그램 오합격 상한 `0.19 <= 0.20`.
- 최신 돈 경로: `PREVIEW_ONLY`, `ACCUMULATING_EDGE`, 단0, 자본 0.
- `globalfixed-ensemble-3-6-9-12`: 전진 4/20, 16개 관측 남음.
- KIS: 키 유효, 6/6 통과, 현금 `$934.27`, NAV `$1427.63`, 외부 보유 ORANY 28주,
  최근 주문·미체결 0건.
- 첫 표준 자본은 교정까지 통과할 경우에만 단2 20%다. 현재는 어떤 자본도 열리지 않는다.

## 다음 우선순위

다음 독립 연구 가족은 회계 기반 횡단면 다요인이다. 가격 시계열 변형을 더 늘리지 말고,
수익성·투자·가치처럼 기업 회계에서 나오는 신호를 시점 보존 자료와 실제 체결 가능한
종목 집합으로 사전등록해야 한다. 그 다음 후보가 공시 후 드리프트다.

남은 안전 검토점도 있다. 상위 단의 기존 라이브 성과 관문은 관측 수·기간·낙폭 중심이며
양의 수익 자체를 별도 요구하지 않는다. 이번 변경은 헌법 기준을 임의 확대하지 않고 현재
경로 재승인만 추가했다. 이 항목을 바꾸려면 별도 안전 경계 명세가 필요하다.

## 검증

- 관련 회귀시험: 175 passed.
- 전체: 3155 passed, KIS 자격증명 전용 6 skipped.
- `uv run ruff check src tests`: 통과.
- 엄격 하네스: 14/14.
- HANDOFF 사실 검사: 통과.
- JSON Schema와 생산 요약/전체 원시 결과 대조: 통과.
- PR 품질 관문: 통과.
- 배포: `33318101840` 성공, 배포 상관 ID `6606b7fb09a2daff6cefa3a7a213bccd`.
- KIS 스모크: `33318101829` 성공, 대상 `fc5af95`.
- 공식 전략 공장: `33318101856` 성공, 대상 `fc5af95`, 784개 고유 후보·19개 가족,
  `NO_FACTORY_EDGE`, 다음 가족 `accounting-cross-sectional-factors`.

## 안전 경계

헌법·커널·화이트리스트·주문 한도·비밀값·감사 로그 삭제는 건드리지 않았다. 교정되지 않은
상향 노출 경로를 닫았고 위험 축소 경로는 유지했다. `Backtest -> Canary -> Full` 순서와
장중 배포 금지는 그대로다. 모든 새 전략 결과는 주문 0건, 자본 변경 없음, 라이브 전략 변경
없음을 기계 판독 필드로 남긴다.
