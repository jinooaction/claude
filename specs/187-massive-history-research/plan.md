# 계획

기존 httpx·거래 달력·5분봉 정규화·연구기·독립 증거 검사를 재사용한다. 새 모듈은
market_data/massive_history.py, 명령은 scripts/massive_history_review.py다.
고정14일 청크는 최대20160개의1분 기초 구간이므로50000 한도 이내다. 다음 페이지가
있으면 부분 결과를 완결로 처리하지 않는다. 원본 캐시로 실패 후 같은 범위를 재개한다.

새 공급자 전용 manifest를 만들어 기존 분석 로더에 전달한다. 기존 KIS/Alpaca 수집기,
승인 파일, 실행 엔진, 헌법/커널은 수정하지 않는다. 새 기능 사용 중지로 복구하며
이미 보관한 원본과 이전 결과는 삭제하지 않는다. 구매·키 연결은 운영자 외부 단계다.
