# 개발 검증 — 2026-09-08~10

## 원본 범위 현금·관측 구간 거래량 모델 — 2026-09-11

FR044/045: 신규 모델26개 포함 단타707 passed/4 skipped/3497 deselected(42.52초),
린트·하네스14/14·인계 검사 통과. 실제 계좌 GET의 원본 분류 뒤 현금 계산/공개 금액
제외, 실제 체결 GET과 SQLite 전송 전 기록에서 구간 생성, 두 봉 모두의 거래량·
같은 종목 복수 주문 합산·경계·빈/누락/중복/미래 봉·잘못된 수량/시각·입력 지문을
검증했다. 초기 줄 길이 린트 오류를 수정하고 최종 전체 단타 검사를 다시 실행했다.
현재 전체 계좌 인증이나 다음 봉 체결 모델 동등성은 제공하지 않으며 실제 주문 없음.
이번 변경 후 전체 저장소 회귀는 미실행이다. 이전 전체4168개 결과는 아래 실행 코드
eb22ad4 기준이며 신규 모델을 포함하지 않는다.

## 최신 실행 코드 전체 회귀와 시작 실패 복구 — 2026-09-11

eb22ad4의 전체 시험4168 passed/12 skipped(797.47초), 종료0.
제외는 실제 KIS11개와 가동 전 상태 전용1개다. 이 실행 중 추가한 실제 시작 경로
시험2개는 별도 실행한 시작 검사10개(13.05초)에 포함해 통과했다. 따라서 전체4168개에
신규2개가 포함됐다고 보고하지 않는다. 실행 소스 변경은 없으며 린트·하네스14/14·
인계 검사는 통과했다.
신규 시험은 launch→build_kis_program→prepare_qualification→ExecutionCostSource→
실제 인증/GET 파서→SQLite까지 연결한다. 연구/전진 합격만 시험용으로 격리한다.
증거 부족과 요청 중 취소 각각 두 번 실행해 DB 닫힘·원본 주문/체결0·GET 및 인증만
허용을 검증한다. 과거 부분 체결/중지/재시작 시험도 전체 회귀에 포함됐다.
정상 KIS 순현금 제공자와 시간/거래량 모델 증명은 구현되지 않았다. 따라서 이는 현재
구현의 전체 회귀 완료이며 T009/T010의 정상 생산 시작 성공 또는 전체 목표 완료가 아니다.

## 실제 체결 비용 공급자 연결 — 2026-09-11

FR043: 관련59개(1.23초), 단타679 passed/4 skipped/3497 deselected(28.45초),
린트·하네스14/14·인계 사실 검사 통과. 모의 HTTP의 인증/실제 주문 및 거래 파서→SQLite
대조→prepare_qualification을 연결했다. 연구/전진 합격 부분은 단위 격리이며 실제 합격
증거가 아니다. 정상 보고서 부분 증명과 계좌/전략/기간/수수료/동시 장부 변경 거절,
형식은 맞지만 계산과 다른 승인 지문 거절, 공용 보유 평가 포함/외부 기준 비중복을 검증했다.
초기 모의 응답의 필수 연속조회 헤더 누락을 수정한 뒤 통과했다. 엄격한 주문 파서의
시각 미제공을 발견해 날짜 대응을 추정하지 않고 명시적 미충족 조건으로 보존했다.
이 변경 후 전체 저장소 시험과 실제 KIS 조회는 실행하지 않았다. T009/T010은 미완료다.

## 단일 시세 연결 보정 및 원본 계약 재확인 — 2026-09-11

관련73개(1.01초)·린트 통과. 전략5개와 SCHX/IAUM을 한 연결의7개 구독으로 처리하고
전략 뷰는5개, 계좌 뷰는7개인지 확인했다. 전략 포함40개 허용/41개 거절을 연결 전에
검증했다. 기존 실제 운용기 잠금/종료 시험도 포함한다. 같은 키의 두 웹소켓 사용 가능성을
입증한 것이 아니며 그런 가정을 없앴다. 아래4cfcb5f의 두 연결 구현은 이번 보정으로 대체한다.
공식 현금/거래 속성을 재조회한 특정 미확정 필드는 contracts/unresolved-source-fields.md에
기록했다. T009/T010 자격 계산과 최종 CLI 통합은 완료하지 못했고 전체 회귀/머지는 하지 않았다.

## 전략 시세와 전체 보유 평가 연결 분리 — FR042/T038 (2026-09-11)

관련119개(1.06초) 후 시세 해석기/거래소 탐색 코드를 실행 지문에 포함했다. 최종 단타
670 passed/4 skipped/3497 deselected(26.36초), 린트·하네스14/14·인계 통과.
모의 HTTP로 SCHX/IAUM 거래소를 탐색하고 실제26필드 해석기로 발생시각/가격을 얻었다.
REST 가격99999는 평가에 사용하지 않고 실시간 가격40만 반환했다.31초 경과/종료 시
가격이 사라지고 기존 전략 subscription_key는 외부 종목을 계속 거절한다. 미지원ORANY는
가격 없음으로 거절하며0으로 대체하지 않는다. 실제 운용기 잠금 중 중복 구독0과 두 시세
작업의 중지/종료를 검증했다. 제외4개는 실제 KIS 인증 필요 시험이다. 전체 회귀·서버
실행·전체 계좌 현금·최종 자격 검증은 아직 완료하지 않았다.

## 시작 시점부터 계좌 원본 기록 — FR041/T037

실제 조회 스크립트와 API 파서→모의 HTTP→비공개 SQLite를 연결해 신규13개 포함
관련195개(1.35초)·린트 통과. 날짜 없이9개 GET 원본과 기존 체결2건의 전후 장부 순번을
저장하고 다음 호출의 새 원본을 추가했다. 기존 장부 바이트 보존, 부분 실패 보존/재개,
계좌 변경·연결 해시 변조 거절, 공개 파일/링크/다른 DB 거절, 저장 권한0600,
인증 비노출, 조회 중 동시 장부 기록 식별을 확인했다. 초기 시험의 migrate 함수명과
스크립트 import 줄 길이 오류는 수정 후 위 결과로 재검증했다. 전체 회귀는 핵심 연결 후
수행한다. 시작 순현금/전체 범위 인증이나 서버 설치·실거래 가동의 증거는 아니다.

## 전략 청산 의도 보존 — FR040/T036

수정 전 실제 전략의 매도 뒤 미체결/부분 체결 취소→SQLite 닫기/재열기→비판단 봉에서
잔여 주문이 나오지 않는2개 실패를 재현했다. 수정 후 신규19개 포함 단타650개(24.99초),
린트 통과. 원래 지정가·잔여 수량·같은 구간 중복 방지·새 보유 회차 분리·관리 단독 재개,
노후 시세/권한/매도가능수량 거절과 회복, 잘못된 기록 보존·명시적 정리 우선을 검증했다.
처음 통과한 관련83개(5.33초) 후 기록 오류가 명시적 정리까지 막지 않도록 보강했고,
최종650개 결과는 이를 포함한다. 초기 린트 줄 길이2건은 줄 나눔 후 재검증해 해결했다.
전체 회귀는 핵심 입력·자격·최종 통합 연결 후 수행한다. KIS 원본 제공자나 실제 비용/
체결 자격이 완료된 것으로 간주하지 않는다.

## 남은 주문 처리와 수집기 종료 — FR038/FR039

수정 전 실제 신호→실제 운용기→모의 KIS 시험에서5종목 중1종목만 처리해2개 시험이
실패했다. WAIT_BROKER를 수집 신호 완료로 표시하지 않도록 고쳐 모두 처리하며,
부분 체결 취소된 첫 주문은 중복 발급하지 않는다. 중지·노후 시세·수집 실패 반례도 확인했다.
반복 수집 실패 시험에서3초 종료 제한이 지나도 정상 반환하는 문제를 별도로 재현했다.
최초 관련 검사24개는 SIGINT 진단 개입 후142.77초에 종료해 최종 증거로 쓰지 않는다.
시간 제한/취소 전달 수정 뒤24개가3.01초, 부모 취소 회귀 추가 뒤25개가3.02초에
외부 개입 없이 통과했고 린트 통과. 단타631개(24.82초) 통과. 전체 회귀는 아직 미완료다.

## 순현금 사건 재계산 — FR037/T033

단타624개(22.98초) 통과 뒤 신규 계산기를 실행 지문에 추가했고 관련72개(0.92초),
린트·하네스14/14·인계 검사 통과. 원본 사건 순서/고유 번호/통화/시각/각 잔액/끝 잔액을
검사한다. 거래 정산·이체·환전·배당·이자·비용·채무와 작은 외부 소수 정밀도에서도
정확한 계산 및 반복 호출 비변경을 확인했다. 관측 변환기는 보고 순현금/NAV를 무시하고
계산한 순현금에 전체 보유 평가를 더한다. 원본 범위 검증이 없으면 여전히 거절된다.
코드f86514e 전체4114 passed/12 skipped(761.15초), 종료0으로 완료됐다.
KIS 원본 정규화 제공자·최종 자격·T009/T010
전체 통합은 구현되지 않았으며 정상 시험은 내부 모의 입력으로 수행했다.

## 자체 순자산 계산 연결 — FR036/T032

단타603개(23.67초)·린트 통과. 검증된 순현금과 전체 보유 원본 가격으로 NAV를 직접
만들고 보고 NAV 값/플래그는 사용하지 않는다. 실제 엔진·모의 증권사·SQLite 재열기로
부분 체결→중지→늦은 체결→재시작→청산과 비용을 반영한 현금/NAV를 확인했다.
추가13개 반례/성공 시험은 누락 순현금·미검증 현금/범위·단타 외 기존 보유·채무·
비정상/누락 가격·외부 Decimal 문맥도 포함한다. 기존 보고 NAV 계약은 유지한다.
코드e068ca5 전체4092 passed/12 skipped(778.61초), 종료0으로 완료됐다. 실제 KIS
현금 원본 제공자나 전략 체결 자격을 완료한 것으로 해석하지 않는다.

## 이전 전체 검증 — 시험 계좌 비용 보정 포함

코드7176a14의 전체 시험은4079 passed/12 skipped(773.42초), 종료0으로 끝났다.
아래6a30340의 모든 변경과 FR019 시험 계좌 수수료 보정을 포함한다. 실제 KIS11개와
가동 전 상태 전용1개는 제외했다. T009/T010의 실제 입력 제공자와 최종 통합 완료를
입증한 결과는 아니다. 실제 주문·취소·자금 배정·실거래 서비스 활성화는 수행하지 않았다.

## 이전 전체 검증 — 거래 원본과 장부 대조 포함

코드6a30340의 전체 시험은4079 passed/12 skipped(771.87초)로 끝났다.
T029 체결 시각 출처, T030 정산 산술, T031 장부 대조를 포함한다. 제외12개는
실제 KIS11개와 가동 전 상태 전용1개다. 전체 계좌 입력·체결 검증 제공자 및
최종 통합 T009/T010은 여전히 미완료다.

후속7176a14는 가짜 계좌의 현금에서 매수·매도 가정 수수료가 빠지던 오류를 수정했다.
부분 체결·늦은 체결·중지·재시작·청산의 현금/순자산과 반복 관측에서 중복 차감이
없는지 실제 운용기 시험으로 확인했다. 관련73개(2.79초), 단타590개(22.36초),
린트·하네스14/14·인계 사실·PR 본문 검사 통과. 이 후속의 전체 시험은 위7176a14의
773.42초 결과로 완료됐다. 실제 KIS 수수료를 검증했다는 의미는 아니다.

## 이전 전체 검증 — 사용자 시작 연결 포함

코드9a99abbb43c6dc699334da3d5e3c19e442e0e05b:
`uv run pytest -q`는 **4033 passed, 12 skipped**, 892.09초, 종료0.
제외는 실제 KIS11개와 이미 가동된 사다리에 적용되지 않는 가동 전 시험1개다.
인증·시세 수명·전략 재검사·사용자 시작 연결(T024~T027)을 모두 포함한다.
아래 단계별 기록의 전체 검사 대기는 이 결과로 대체한다. 전체 계좌 계산·체결 동등성
검증 및 최종 통합(T009/T010)은 여전히 미완료이며 실제 거래 준비 완료를 뜻하지 않는다.

## 최신 사용자 시작 연결

T027: 관련58개(1.13초), 단타549 passed/4 skipped/3492 deselected(26.53초),
린트·하네스14/14·인계 통과. 도움말 실제 실행과 명령 경로 전달, 자격 실패 전 네트워크0,
빈 초기 토큰 대신 갱신 토큰으로 첫 체결 조회, 연결 닫기, 없는 DB/기준표 거절,
운용 설정 지문 불일치, 오류 후 주문0 단정 방지를 검증했다. 모의 계좌/자격을 연결한
실제 엔진에서 중지 후 같은 장부를 재개해 기존 표식과 정리 기록이 남는지 확인했다.
현재 전체 계좌/체결 검증까지 성공한 생산 시작 시험은 아니다. 실제 시작은 수행하지 않았다.
이 변경은 위9a99abb 전체4033/12 검사에 포함됐다.

## 최신 전략 재검사 연결

T026: 신규24개 포함 관련52개(1.11초), 단타540 passed/4 skipped/3492 deselected
(23.98초), 린트·하네스14/14·인계 통과. 시험용 의존성 결과로 역사→서명된 전진 재검사
순서와 같은 사전등록 바이트, 부족 관찰·체결 미검증·변조/만료 승인 거절을 확인했다.
서버 파일의 root 소유·권한·크기·심볼릭 링크·중복 키 검사도 검증했다. 정상 승인 분기는
시험용 운영 승인과 의존성 결과이며 실제 연구 합격·60일·체결/캐너리/배포 검증이 아니다.
생산 발급/설치/활성화는 하지 않았고 현재 전진 검사의 execution_parity_verified=false는
QUALIFICATION_PARITY_NOT_VERIFIED로 거절한다. 최종 자격·전체 계좌·CLI 시작은 미완료다.
전체3988개 결과는 이전4c989eb이며 이 변경은 전체 재검증에 포함되지 않았다.

## 최신 시세 수명 연결 검증

T025: 신규3개 포함 관련25개 통과(0.98초), 단타516 passed/4 skipped/3492 deselected
(23.63초). 실제 엔진·임시 장부와 모의 시세 작업으로 잠금 이후1회 구독, 중복 시작의
구독0, 시세 종료 시 캐시 잔존과 무관한 진입 차단, 실행 취소 후 연결 닫기·정리 재개를
검증했다. 기존 시세 파서/전송 시험도 단타 회귀에 포함했다. 린트·하네스14/14·인계 통과.
실제 KIS4개는 제외, 전체 검사는 아래4c989eb가 마지막이며 이 변경을 포함하지 않는다.
실제 시세 서비스나 주문·자본은 활성화하지 않았다. 사용자 시작 명령·자격·계좌 계산은 미완료다.

## 최신 계좌 인증 연결 검증

T024: 새 KIS 인증 관측기8개와 실행 구성 연결1개 포함 관련51개 통과(1.02초).
단타 전체 회귀513 passed/4 skipped/3492 deselected(28.81초). 제외4개는 실제 KIS 검사다.
실제 인증/캐시/잔고 파서와 엔진을 모의 HTTP로 연결했다. 토큰 발급1회 뒤2회
관측에서 계좌 GET8회를 수행하며 동일 계좌·갱신 토큰을 사용한다. 실제 엔진은
ACCOUNT_SCOPE_UNVERIFIED로 중단한다. 연결 변경·인증 오류는 계좌 조회 전에 거절한다.
린트, 하네스14/14, 인계 사실 검사 통과. 전체 시험은 아래4c989eb 결과이며
이후 인증 연결 변경을 포함하지 않는다. 실제 서비스 접속·주문 전송은 수행하지 않았다.

## 최신 전체 검증 — 2026-09-10

검증한 코드 `4c989eb` (전략 서명과 공통 가격 계산 포함):

- `uv run pytest -q`: **3988 passed, 12 skipped**, 786.05초, 종료0.
- 제외: 명시적 `KIS_LIVE_TEST=1`이 필요한 실제 KIS 연동11개와 이미 가동된 사다리에
  적용되지 않는 가동 전 상태 시험1개. 실제 계좌/주문 검증을 통과한 것으로 해석하지 않는다.
- `uv run ruff check src tests scripts/intraday_operator.py`: 통과.
- `uv run python scripts/agent_harness_probe.py --strict`:14/14,
  `uv run python scripts/check_handoff_facts.py`:OK. 원격 main9fbdef8 재확인.
- 원격 PR793은 같은 코드 지문, OPEN/draft/MERGEABLE이다. T009/T010이 미완료이므로
  전체 시험 통과만으로 준비 완료 전환·머지·가동하지 않는다.

완료 판단: 계좌 변환기·연구 선택·내부 시작 조립·원본 전진 장부 재계산의 회귀는 통과했다.
생산 KIS 현금/전체 자산 검증값, 적격 단타 권한 제공자, 사용자 실행 시작 명령은
아직 구현되지 않았다. 기존182 FR-008과 헌법 X.4의 하루1회 권한을 반복 단타 주문에
재사용할 수 없다. 관찰일 수 계산은 전략 고정 시각 인증·체결 동등성·권한 발급이 아니다.
이 공백은 시험용 공급자가 성공했다는 이유로 완료 처리하지 않는다.

## 초기 검증

초기 구현 코드: `3756aee`. 아래 초기 결과와 기존 계좌 후속 검증을 구분한다.

- 새 기능57개: 4.03초, 실패0.
- 기존 주문 실행·모의 운용·실시간 워커를 포함한 관련155개: 10.90초, 실패0.
- `uv run pytest -q`: **3767 passed, 10 skipped**, 762.39초, 종료0.
- 제외10개: 실제 KIS 연동9개(환경의 명시적 실계좌 검사 설정 없음),
  이미 가동된 자본 사다리에 적용되지 않는 가동 전 조건1개.
- `uv run ruff check src tests scripts/intraday_operator.py`: 통과.
- `uv run python scripts/agent_harness_probe.py --strict`: 14/14.
- `uv run python scripts/check_handoff_facts.py`: OK, origin/main292805f 확인.
- `scripts/check_pr_quality_gate.py`로 초안 PR 본문 검사: 통과.

## 실제로 실행한 통신과 수명주기

- 로컬 웹소켓 서버와 실제 소켓을 연결해 `/tryitout` 경로, 구독, 발생시각 있는
  체결 수신, 연결 종료 시 캐시 폐기를 검증했다. 가격 자료는 테스트용이다.
- 실제 KIS 서버 `/tryitout` 웹소켓 연결 후 정상 종료: PASS.
  인증·구독·실제 가격 수신은0이다. 이를 실제 시세 검증 완료라고 부르지 않는다.
- 새 CLI에서 기존 PaperRuntime을 실행하고 20회 재시작했다.
  18개 전략 장부, 모의 체결 발생, 중복 봉 재입력의 장부 불변을 확인했다.
- 중복 프로세스 거절, 현재 run_id의 중지 요청만 적용, 대기 중 중지,
  강제 중단 기록, 과거 장부와 추가 운영 기록 보존을 확인했다.

## 남은 검증과 사용자 목표

새 입력기의 인증 후 실제 KIS 시세·해당 가격 구매력 응답은 확인하지 못했다.
이 Mac 환경과 격리 작업 폴더에는 KIS 인증 정보가 없으며 서버 비밀값을 복사하지 않았다.
전체 계좌 현금/NAV·단타 전략 적격 권한·실주문 운용 연결 T009/T010은 미완료다.
이번 변경은 초안 PR793에 보존했으며 main 병합·운영 서버 배포·실주문 활성화는 없다.

## 기존 계좌 재사용 후속

운영자 결정: 기존 한국투자 계좌 유지. 명시적 외부 보유 기준표를 단타 엔진에 연결했다.
기준표의 실제 보유 수량을 임의 수정하거나 가짜 체결을 추가하지 않았다.

- 신규25개 포함 관련196개 검사: 10.75초, 실패0.
- 같은 종목 기존4주와 단타 부분 체결3주를 분리하고 취소 후 실제 DB를 닫고 다시 열었다.
  마감 청산은 단타3주만 제출하며 기존4주가 남고, 재호출은 중복 주문을 만들지 않는다.
- 미신고 보유 변동, 잘못된 기준표, 입력 매핑 변경, 기준표만으로 권한 취득,
  기존 비거래 종목 가격 누락·노후화를 거절한다.
- 기존 보유는 전체 노출과 계좌 손실 방어에 포함하고 단타 손익에는 포함하지 않는다.
  600달러 준비 한도도 기존 보유가 있다는 이유로 늘어나지 않는다.
- 린트, 하네스14/14, HANDOFF 사실 검사 통과. 실제 증권사 주문은0건.
- 격리 저장소의 로컬 `core.hooksPath`가 없는 `githooks`를 가리켜
  추적 중인 실행 가능 `.githooks`로 수정했다. 원본 작업 폴더 설정은 보존했다.
  기존 충돌 검사를 실제 커밋·푸시에 적용하는 복구이며 검사 코드는 바꾸지 않았다.
- 2026-09-09 KST 현재 수정본 전체 회귀: **3792 passed, 10 skipped**, 796.09초,
  종료0. 제외는 로컬의 실제 KIS9개와 이미 가동된 사다리의 가동 전 조건1개다.
  아래 별도 서버 검사에서 기존 main의 실제 KIS9개를 모두 검증했다.

## 기존 계좌 실제 읽기 재검증

기존 서버 인증 환경에서 main292805f의 읽기 전용 KIS 검사를 실행했다.
실행34240823757은9 passed(16.77초), 성공이다. 새 개발 코드의 실계좌 검증과 구분한다.
미체결0, 계좌 읽기 페이지 완료, 준비 한도600/일중 정지12 유지, 자본 변경0·실주문0.
직접 잔고의 체결기준 보유 행은 조회 전후2개, 결제기준 보유 행은1개다.
각 통화·요약은1행이고 단일 USD 수치 비교는EQUAL, 조회 전후 통화 값은 안정적이다.
그러나 전체 계좌·현금·NAV 검증 판정은 여전히false다.
계좌 수집기는 미해석 자산1개, 분류되지 않은 증거금 행34개를 보고했다.
과거 완성 세션2026-09-04의5종목×78봉 읽기는 통과했다. 이는 현재 시세 수신 증거가 아니다.
이 결과를 실제 전체 잔고 검증이나 단타 실행 권한으로 승격하지 않는다.

## 주문 관리와 운용 수명주기 후속

기존 실제 주문 엔진과 OrderRouter를 새 운용 모듈에 연결했다. 검증의 증권사 응답은
`httpx.MockTransport`로 구성한 KIS 시험 서버이며 실제 주문·취소는 실행하지 않았다.

- 신규 통합19개와 실행 코드 식별자 회귀1개를 추가했다. 최종 관련51개는2.51초, 실패0.
- 전략 봉으로 매수 판단부터 주문 접수, 외부 중지, 늦은 부분 체결, 단타 수량 청산,
  증권사 체결 확인 후 중지 완료까지 같은 운용 루프에서 검증했다.
- 수집 실패·시간 초과에도 주문 만료 취소와 장 마감 관리는 계속된다.
  계좌 관측·권한 실패 시 기존 차단을 유지한다.
- 중지 직전 주문 경합, 실제 DB 닫기·재열기, 같은 날 두 번의 중지·재개,
  기존4주를 보존한 단타3주 정리, 별도 CLI 프로세스의 상태·중지 명령을 검증했다.
- 같은 DB의 심볼릭 링크는 같은 잠금을 사용하며 하드 링크 DB는 거절한다.
  중지 파일은 원자적으로 교체하고 디렉터리까지 동기화한다.
- 새 운용 제어 소스도 실행 코드 식별자에 포함한다. 과거 적격 증거로 변경된
  제어 코드를 실행하지 않으며 미정리 상태에서 전략 식별자 변경도 거절한다.

생산 KIS 관측 공급자와 적격 권한을 생성하는 시작 연결은 아직 없다.
따라서 T012/T013 구현과 T009/T010 실계좌 시작 완료를 구분한다.

최종 변경본 전체 검사: **3812 passed, 10 skipped**, 806.13초, 종료0.
제외는 실제 KIS 연동9개와 이미 가동된 자본 사다리의 가동 전 조건1개다.
`uv run ruff check src tests scripts/intraday_operator.py` 통과,
하네스14/14, HANDOFF 사실 검사와 PR 본문 품질 검사도 통과했다.
