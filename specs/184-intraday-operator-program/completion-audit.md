# 실사용 완성도 점검

## 현재 판정 — 2026-09-13

ef44227(실행 코드40ab005, FR069 포함) 전체4702 passed/12 skipped(781.94초), 종료0.
`/tmp/claude-184-cancel-only-full.log`. 실제 KIS11개와 가동 전 상태 전용1개는 제외했다.
전체 프로그램은 미완료이며 PR793은 초안이다. 아래 FR060 당시 내용은 과거 기록이다.

| 요구 | 현재 확인한 구현 | 완료 여부와 남은 조건 |
| --- | --- | --- |
| 원본 잔고 계산 | FR061~065 원화/달러·미결제·국가 전체 보유·관측 구간·자산19분류 대조 | 계산 구현. 전체 계좌와 거래 후 비용 포함 순현금 인증은 미완료 |
| 실제 원본 수집 | main896d31d 실행34700917080: KIS11개,11응답·순번13 COMPLETE·주문0 | 사용 가능. 최근3개 등록일0행을 전체 거래 부재나 수수료0으로 취급하지 않음 |
| 체결 시각 없는 검증 | 관측 구간·가능한 봉·원래 주문량·가격/거래량·날짜별 비용과 최종 자격 검사 | 구현. 실제 자료 없이 실제 시각이나 실행 적격을 발급하지 않음 |
| 체결 거래대금 | FR067/068 동일 수량 금액 대조, 누적/추가 거래대금 보존, 재조회·비용·손익 소비 | 구현 및 재시작/정산 검증. 외부 현금 사건과 수수료 포함 순현금은 별개 |
| 전체 보유 평가 | OTC 보고 평가와 실행 시세를 분리하고 검증된 순현금에 전체 보유 평가 합산 | 소비자 구현. 정상 전체 계좌/현금 생산자 T009는 미완료 |
| 중지·복구 | 기존 생명주기와 FR069 순현금 미검증 중 우리 주문만 취소하는 분리 경로 | 모의 증권사/SQLite 검증. 정상 전체 계좌 제공자의 최종 통합 T010은 미완료 |

`KISExecutionObserver._read`는 실제 같은 계좌 원본을 읽고 계산하지만 전체 계좌와
현금 집계 검증을 참으로 반환하지 않는다. 정상 새 주문용 `build_observation`은
거절하며 모의 정상 입력 검사 통과가 이 생산자의 완료를 증명하지 않는다.

현재 확보한 공식 명세와 서버 원본으로 확정하지 못한 것은 미결제 매수/매도·예수금의
거래 후 수수료 포함/갱신 관계, 순자산 요약과 차감할 주식 평가의 같은 범위·통화·
기준 시점이다. CURRENT는 공식 설명상 미국 거래와 최종 수수료 반영이 지연될 수 있다.
방금 받은 전후 숫자의 일치만으로 거래 반영 완료를 증명할 수 없다.

검토한 대안과 한계:

1. 보고 구성 직접 합산은 조건부 계산까지 구현했다. 미확정 포함 관계를 인증하지 않는다.
2. 시작 잔고와 자체 장부는 거래대금·비용 대조까지 연결했다. 체결 장부만으로 입출금,
   환전, 배당, 기타 비용 전체와 미확정 비용의 완전성을 증명할 수는 없다.
3. 순자산 요약에서 주식 평가를 차감하는 대안은 `reported-nav-contract.md`의 검토
   단계다. output1의 결제 기준을 output2에 적용하거나 다른 부분의 값을 합치지 않았다.
4. 보수적인 비용 충당에는 적용 범위와 비용 한계의 근거가 필요하다. 임의 상수를
   확정 순현금으로 표시하지 않는다.

거래대금 보존, 취소 관리 분리, 이번 최종 검사에 걸쳐 같은 원본 의미의 막힘이 남았다.
다음 조치는 관계를 확인할 추가 원본/기준 확보와 그에 맞춘 생산자 구현이다. 같은
조회나 검사 반복, 검증값 변경은 해결이 아니다. 문의는 보조 경로이며 화면 접근·
평생 거래내역이 일괄 필수 조건은 아니다. 승인된 문의의 아직 미접수 상태는
`../182-intraday-kis-execution/capital-review/kis-cash-contract-inquiry.md`에 기록되어 있다.

실제 주문·취소·자본 배분·운영 승인 발급·활성화는 수행하지 않았다. FR069 취소용
대조를 새 주문·청산 매도·STOPPED 판정이나 전체 계좌/현금 인증으로 확대하지 않는다.

## 과거 기록 — FR060 당시

2026-09-12 FR060 국내 현재잔고 연결 및 전체 회귀 기준.
전체 프로그램은 아직 미완료다. 아래 구현 여부와 실제 운영 자료 확보 여부를 구분한다.

원본 확보는 해결됐다.186/PR803으로 수집기를 출시했고 실제 서버 비공개 장부에
추가 기록한다. PR807/main2f7c11f의34685116089은 KIS11개 통과·9응답·순번6 COMPLETE다.
공통 USD 예수금은 현재 전후/결제 USD 값과 일치했다. 현재 환산액/총자산식 후보는
차이가 있으며 결제 보고는 두 식의 자료가 부족하다. PR809의 실제34686417861에서
OTCB 보유의 수량/매도가능/보고 가격/평가가 모두 양수임을 확인했다. 보고값을 보존하며
가격 발생 시각은 만들지 않는다. 원본 위치/화면 접근을 다시 요구하지 않는다.
FR058~FR060을 포함한 실행 코드8658792 전체4474 passed/12 skipped(829.41초),
종료0. 국내 조회도 정상 입력기에 연결했고 관련71개/단타912개 통과·4개 조건부 제외.
실제 국내 조회34690683642는 보유/대출0이지만 예수금/정산액/순자산 양수다. 국내·해외
현금 대조 PR813은 별도 진행 중이다. `reported-nav-contract.md`는 원본 순자산을
활용하는 대안의 검증 항목이며 채택·구현 완료된 계산 계약이 아니다.

| 요구 | 구현과 확인 근거 | 남은 조건 |
| --- | --- | --- |
| 원본에 맞춘 현금 계산 | `intraday_cash_baseline.py`가 전후 USD 보고/예수금 대조를 실제 입력기에 연결, `intraday_cash_ledger.py`의 시작 잔액·후속 사건 재계산 | 시작 보고 범위와 전체 계좌 순현금은 별개. 거래 후 미결제·채무 포함 정규화 미완료 |
| 전체 보유 평가 연결 | 실제 KIS 읽기의 OTC 보고 형식 변환·구독 분리, `intraday_observation.py`의 순현금+시세/보고 평가 합산, 실제 엔진 전역 한도 검증 | 실제 전체 계좌 범위/현금 제공자 연결 미완료 |
| 체결 시각 없는 실행 검증 | 전송 전 기록과 실제 조회 수신 완료로 관측 구간을 만들고 가능한 모든 봉을 검사 | 정확한 체결 시각을 복원했다는 의미는 아님. 실제 자료도 동일 조건을 통과해야 함 |
| 수량·가격·수수료·거래일 대조 | 원본 주문 수량, 신호의 뉴욕 거래일, 다음 봉 모델, 날짜별 묶음 수수료 대조와 최종 자격 소비 구현 | 연구·전진 관찰 원본 재계산과 별도 운영 승인 조건 유지 |
| 시작·중지·재시작·복구 | `test_intraday_launch.py`, `test_intraday_execution_runtime.py`, `test_intraday_observation.py` 등에서 모의 증권사·실제 장부 재열기 검증 | 검증된 전체 계좌 제공자를 사용하는 정상 시작부터 종료까지 최종 통합은 미완료 |

## 실제로 막힌 연결

`KISExecutionObserver._read`는 같은 주문 권한의 인증을 갱신한 뒤
`observe_account`, USD 시작 잔고 대조기와 국내 현재잔고 수집기를 호출한다. 이 실제 입력기는 현재 전체 계좌 범위와 현금 집계 검증을
참으로 반환하지 않는다. 따라서 `build_observation`에서 거절되며, 내부 정상 입력 시험이
통과하더라도 실제 제공자가 완성된 것으로 보지 않는다. T009/T010을 미완료로 유지한다.

다음 구현은 이미 확보한 원본의 통화, 기준 시점, 결제·미결제, 채무와 중복 포함 여부를
정규화하는 것이다. 원본 수량/평가의 보고 형식과 구독 연결은 구현했으며 같은
시작 경로의 정상·누락·오래된 자료·재시작 시험을 수행해야 한다. 화면 접근이나 평생 거래
내역은 보편적인 필수 조건이 아니다. 검증된 시작 상태와 그 이후의 완전한 변경 내역도
대안이지만, 기존 서버의 일부 체결 이력만으로 현금 변경 전체가 증명되지는 않는다.

## 안전 경계와 되돌림

고정된 미구현 실행 모델 표시는 실제 계산 결과를 모두 확인하는 검사로 대체했다.
알 수 없는 새 조건, 원본 불일치, 미종료 주문, 누락된 승인에 대한 거절은 유지한다.
구매력을 순현금으로 바꾸거나 미평가 자산을 0으로 처리하지 않는다.
이 작업에서 실제 주문·취소·자본 배분·승인 파일 발급은 수행하지 않았다.
PR793은 초안이며 main 배포는 하지 않았다. 되돌림은 해당 기능 커밋을 별도 되돌림
커밋으로 처리하고 변경된 코드 지문으로 기존 자격을 재검사하는 방식이다.

전체 회귀 검사 수치와 제외 항목은 `validation.md`의 해당 코드 기준 기록을 따른다.
검사 통과 자체가 위 두 미완료 항목의 대체 증거는 아니다.
