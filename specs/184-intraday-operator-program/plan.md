# 구현 계획: 단타 실행 프로그램

FR063, 등급3: 고정 CURRENT→MARGIN→CURRENT 원본의 output1을 숫자/식별자/시각/
페이지 기준으로 정규화한다. ccld_qty_smtl1, ord_psbl_qty1, loan_rmnd와 통화/시장으로
전후 보유 안정성을 확인하고 frcr_evlu_amt2는 원본 보고 평가로만 남긴다. 가격/평가
차이는 허용하며 조회 시각을 가격 시각으로 만들지 않는다. 일반 미국 positions와
unverified_assets의 양수 수량 합집합을 전체 국가 보유와 대조한다. OTC 소수 수량은
Decimal로 비교하고 일반 잔고가 버린0수량은 양수 범위에서 제외하되 원본에는 남긴다.
같은 KISExecutionObserver의 읽기 결과와 코드 지문에 연결한다. 새 API 호출/승인은 없다.
일치/누락/추가 보유/다른 통화·시장/수량 변화/가격 변화/소수/0/중복/누락/시간을 검사한다.
되돌림은 새 범위 대조 연결의 되돌림이며 원본과 기존 실행 검증값은 보존한다.

FR062, 등급3: 기존 CURRENT→MARGIN→CURRENT의 같은 메모리 원본을 별도 조건부
미결제 현금 해석기에 전달한다. 전후 사용가능금액/증거금/요약이 안정적이고
`사용가능 USD + 매수증거금 + 기타증거금 = 보고 예수금`, `증거금 합 = 보고 증거금`인
범위만 지원한다. 국가별 반복 예수금을 합산하거나 첫 행을 임의 선택하지 않는다.
동일 통화 보고와 대조된 예수금/증거금 및 모든 행에 같은 미결제 구성으로 원본 금액
산술을 수행한다. 외화미수와 다른 통화/미분류 금액은0이어야 한다. 이 조건에 맞지
않는 다른 사용가능금 표시 방식을 추정해 통과시키지 않는다. 기존0조정 결과는 유지하고
새 결과를 별도 scope로 정상 현금 평가에 연결한다. 실제 주문이나 전체 계좌 검증값 변경은 없다.
근거: https://file.truefriend.com/Storage/research/hts_guest_0130.pdf 35쪽은 증거금과
주문가능금액을 구분하고40쪽은 총자산의 미결제 매도 가산/매수 차감을 설명한다.
이는 공식 HTS 설명의 산술을 명시한 정책이며 현재 API 수수료 포함 방식의 인증은 아니다.
누락/불일치/안정성/부호/전량·부분 매매/미체결 증거금/외부 정밀도와 실제 모의 GET→
원화·달러 평가를 검사한다. 되돌림은 새 해석/연결 제거이며 원본/기존 승인/한도는 보존한다.

FR061, 등급3: 기존 USD 전후 조회에서 tot_dncl_amt와 frst_bltn_exrt를 같은 숫자인
경우에만 선택적으로 보존한다. 누락/변화는 기존 USD 계산을 훼손하지 않고 새로운
복수 통화 현금 평가만 비교 불가로 만든다. 새 정규화기는 원화 예수금을 국내 정산액/
순자산/총평가와 전후 총예수금으로 대조하고, 원화·달러 원본을 각각 보존한 뒤
`USD + KRW / 최초고시 USD-KRW 환율`을 Decimal80·소수12자리 내림으로 계산한다.
이는 보고 환율 평가 정책이며 실시간 환전 시세나 전체 계좌 인증이 아니다.
같은 KISExecutionObserver._read에서 국내 조회 뒤 연결하고30초 관측 순서를 검사한다.
누락/변화/대출/미결제/매매/음수/정밀도/환율0과 실제 모의 GET 연결·지문 변경을 검증한다.
새 API 호출·운영 승인·주문 전송은 없다. 되돌림은 새 평가 연결/메타데이터 보존을
되돌림 커밋으로 복구하며 기존 원본과 거래 장부는 유지한다.

FR055, 등급3: ExecutionQualification이 같은 비용 지문/계좌/전략의 계산 결과를
종합한다. 알려진 구형 미구현 표시만 계산 결과로 대체하며 새로운 미지원 조건은 거절한다.
실제 모의 GET/장부/계산/최종 자격과 모의 운영 승인까지 연결한 정상 사례, 각 조건 누락/
불일치/새 미지원 사유/승인 철회 반례를 검증한다. 연구/시장 출처의 독립 검증은 유지한다.
복구는 코드 되돌림이며 승인 발급/설치·실제 주문·자본 변경을 수행하지 않는다.

FR054, 등급3: 실행 비용 자료에 fee_groups_json을 추가한다. 각 날짜/종목/방향별
수량·대금을 먼저 대조하고 묶음의 주문 식별자와 수수료를 보존한다. 모델은 모든 체결
주문이 정확히 한 묶음에 속하는지 확인하고 묶음 단위 기준 대금 합으로 수수료를 비교한다.
날짜 간 상쇄·중복/누락·실제 다중 주문 GET 소비·가격 기준 차이를 검증한다. 전체
모델/계좌 인증은 별도이며 기존 기록과 권한을 유지한 코드 복구가 되돌림이다.

FR053, 등급3: 엄격한 누적 체결 파서의 reported_order_quantity를 장부 수량과 대조한다.
ExecutionCostAssessment.orders_json에는 미체결을 포함한 모든 조회 주문을 보존하고,
동일 원본 봉/참여율로 정수주 모델 수량을 재계산한다. 부분/전량/미체결·수량 누락/변경·
낮은 외부 소수 정밀도와 실제 GET/자격 소비를 검증한다. 기존 승인·원본·장부는 유지하고
되돌림은 코드 복구다. 실제 주문·취소나 과거 기록 재작성은 수행하지 않는다.

FR052, 등급3: BrokerExecution에 선택적 reported_order_date를 추가하고 엄격한
누적 체결 파서에서만 원문 달력 날짜를 보존한다. 날짜 충돌/형식/윤일을 검증하고 기존
ordered_at_utc=None을 유지한다. 비용 소비의 항상 비어 있던 UTC 시각 조건을 이 원문
날짜 비교로 대체한다. 기존 진단/체결 합산/취소/동기화 회귀와 모의 GET→비용 검증을
실행한다. 날짜 일치와 조회 범위 완전성을 구분하며 복구는 원본을 유지한 코드 복구다.

FR051, 등급3: 비용 대조를 통과한 단일 주문/종목/방향 보고 수수료와 평균 체결가를
관측 구간에 연결한다. 다음 봉 시가×체결수량×연구 수수료율, 시가의 매수/매도 불리한
가격 한도를 계산하고 자격 준비의 같은 원본/기본 모델에서 소비한다. 입력 누락/경계/
반대 방향/낮은 외부 소수 정밀도와 실제 GET 소비를 검증한다. 기존 승인 차단은 유지하며
되돌림은 코드 복구다. 기존 원본·주문·감사 기록 수정이나 실주문은 없다.

FR050, 등급3: intraday_execution_claims와 STRATEGY_EXIT_REQUESTED에 signal_bar_end를
추가하고 이전 행은 그대로 읽는다. 비용 스냅샷의 의도 기록을 주문 식별자/방향/수량/가격과
대조해 관측 구간에 연결한다. 신호 봉 종료와 실제 구간의 포함 관계를 자격 소비 결과에
추가한다. 청산 재시작·경계·누락·의도 불일치 반례를 검사하고 기존 주문 승인 차단을
보존한다. 되돌림은 코드 복구이며 원본 기록/권한/자본 변경은 없다.

FR049, 등급3: 별도 고정 root 소유 시장 수집 키를 쓰는 collect_attested_kis가 직접
HTTP 연결과 시계를 소유한다. --attest-market-data를 기존 모의 운용 명령에 전달하며
PaperRuntime의 선택적 collection_proof를 재생하고 완결 세션의 출처 검증 결과를
자격 모델 소비자에 연결한다. 키/봉/수집 시각/코드 변경·서명 없음·실제 모의 HTTP→장부
재열기→재생의 반례를 검사한다. 기본 진단 수집과 과거 기록은 보존한다. 되돌림은 선택
옵션 사용 중지와 코드 복구이며 기존 서명/감사 삭제·권한 발급·키 설치·실제 주문은 없다.

FR048, 등급3: 전체 스냅샷의 조회 전후 비교를 보존하고, 비용 지문을 만드는 단계에서만
검증 기간으로 선택한 주문의 correlation_id 집합으로 체결/상태/감사를 한정한다.
기간 밖 추가 기록으로 지문이 변하는 실패를 먼저 재현하고, 기간 안 변경·혼입과
동시 변경 거절을 회귀 검사한다. 기존 승인 발급·기간 선택·모델 미입증 차단은 유지하며
되돌림은 코드 복구다. 실제 기록의 삭제/변경이나 주문 전송은 없다.

FR047, 등급3/K4 감사 모델: sync_fills의 GET 완료 시각을 apply_fill_plan으로 전달하고
FillPayload에 선택적 broker_response_received_at_utc를 추가한다. 값 없는 기존 기록은
그대로 읽는다. 비용 증명 스냅샷에 FILL 감사를 포함하고 전체 체결 대응을 확인한 경우만
가장 늦은 수신 상한을 사용한다. 부분 체결/중복/이전 기록/시계 오류/재시작을 검사한다.
복구는 코드 되돌림이며 기존 장부·감사·시간 필드·권한·한도를 유지한다.

FR046, 등급3: assess_forward의 선택적 내부 봉 출력은 완결 세션만 포함하며 기본 공개
보고서에는 없다. assess_registered_forward가 동일 서명/원본 재생 경로로 이를 요청한다.
prepare_qualification은 원본 봉과 실제 비용 증명의 구간으로 assess_interval_volume을
호출하고 interval_model_json에 결과를 고정한다. 호출 여부/거래량 경계/미완결 제외/
기존 자격 차단 유지 시험을 추가한다. 새 외부 요청·주문·승인 발급 없이 연결하며,
되돌림은 해당 커밋 복구다. 기존 기록·권한·금액 제한은 변경하지 않는다.

FR044/045, 등급3: intraday_observation_models의 순수 계산기를 실제 observe_account와
ExecutionCostSource에 연결한다. 기존 USD 원본 분류 이후에만 현금 계산을 호출하며 공개
결과에는 금액을 제외한다. 실행 장부 스냅샷에 order_state_history를 포함하고 양수 체결
조회 완료 직후 시간을 잡아 불변 비용 증명에 구간을 연결한다. 구간 평가는 완결5분 봉과
참여율을 입력받아 모든 가능한 봉의 최악 수량을 합산한다. 인접 경계·누락 봉·중복 봉·
동시 주문·저정밀 문맥·미래 시각 반례를 검사한다. 기존 운영 승격/전체 계좌 인증은
변경하지 않으며 복구는 해당 코드 되돌림이다. 기존 장부·감사·원본은 수정하지 않는다.
새로 수신한 시각 때문에 기존 비용 승인 지문을 매번 바꾸지 않는다. 구간·봉·참여율은
비용 지문에 연결된 별도 interval_digest로 묶으며 현재 운영 승인에는 사용하지 않는다.

FR043, 등급3: ExecutionCostSource가 라우터의 동일 계좌 인증 클라이언트로 세 거래소
주문 조회→거래 명세→주문 재조회를 수행한다. 읽기 전 DB 복사본과 읽기 후 장부를 비교하며
원본 장부는 쓰지 않는다. prepare_qualification/build_kis_program을 비동기로 연결한다.
기존 실행 동등성 참값 의존을 계산된 부분 증명과 명시적 미충족 조건으로 대체한다.
승인 파일의 broker_execution_parity_digest는 계산 지문과 정확히 일치해야 한다.
주문 조회의 엄격한 해석기는 주문 시각을 의도적으로 비우므로 날짜 연결을 추측하지 않는다.
시간/거래량/주문-거래일 연결 미입증은 실행 승인 차단을 유지한다. 실패 시 이전 코드로
되돌리며 기록·주문 제한·자본은 변경하지 않는다. 모의 HTTP의 실제 파서와 SQLite를
자격 소비까지 통과시키고 계좌/전략/기간/수수료/동시 변경/승인 지문 반례를 검사한다.

FR042, 등급3: build_kis_program에서 기존 보유 중 전략5종목 밖의 자산을 별도
_ValuationFeed로 구독한다. KIS 가격 GET은 거래소 탐색에만 사용하고 원본26필드
StrictQuoteFeed 해석을 재사용한다. 추가 구독 매핑은 전략5종목 덮어쓰기/미지원 거래소를
거절하며 전략 포함 최대40종목이다. 하나의 물리 연결에서 받은 가격을 전략용5종목 뷰와
전체 평가 뷰로 나눈다. 기존 자원 잠금 수명에 묶이고 종료하면 진입을 막는다. 기존 보유
소유 분리·주문 허용목록·발생시각 검증을 보존한다. 복구는 코드 되돌림이며 원본 장부는
수정하지 않는다. 모의 HTTP→실제 해석기, 노후/미지원OTC, 단일 연결/구독 총량을 검사한다.
전체 계좌 원본/현금 인증·자격 검증은 별도 미완료다.

FR041, 등급3: 기존 balance-check GET 연결을 AccountHistory의 제한된 읽기 중계기로
감싼다. 전체/부분 수신 원본을 계좌 해시별 SQLite에 추가하고 시작/끝 실행 장부 순번을
읽기 전용으로 함께 저장한다. 새 파일0600, 기존 비공개 일반 파일만 허용하며 응답 출력과
조회 조건만 보관한다. 인증/헤더/자유문구는 제외한다. 해시 연결·순번·계좌를 재검사하고
UPDATE/DELETE를 차단한다. 실패한 수집은 FAILED, 성공한 조회도 현금 검증은 별도다.
원본에 없는 순현금 사건을 생성하지 않는다. 실제 API 파서→모의 HTTP→SQLite 재열기와
중간 오류·계좌 변경·원본 변조·비밀 비노출을 검증한다. 되돌림은 --history-db 사용 중지로
가능하며 기존 주문 장부는 수정하지 않는다. T009/T010 완료를 이 기록 기능으로 대체하지 않는다.

## 프로그램 완성 후속

FR040, 등급3: on_bars에서 실제 전략이 낸 전량 매도 신호를 첫 매수 f.seq로 식별한
보유 회차에 STRATEGY_EXIT_REQUESTED 사건으로 저장한다. _step은 현재 회차의 기록만
읽어 해당 종목 목표0·원래 지정가를 적용하며 manage에서도 관리한다. 신규 테이블 대신
기존 추가 전용 사건 장부를 쓰고, 일반 청산 재요청 식별자는 회차+현재5분 구간에 묶는다.
기존 청구/권한/신선도/매도가능수량/중지 경계와 마감·손실 우선 정리를 보존한다.
실제 전략·모의 KIS·SQLite 닫기/재열기로 미체결/부분 체결, 같은 구간 중복, 새 보유
회차 분리, 원본 가격 유지, 입력 실패 차단을 검증한다. 되돌림은 정리 완료 확인 후
수정 코드 복구이며 원본 사건은 삭제하지 않는다. 소스 변경은 기존 실행 지문에 반영된다.

FR039, 등급3의 종료 보정: 수집 제한 시간을 asyncio.timeout 문맥으로 적용해 내부
별도 작업 완료와 취소가 겹치는 경로를 줄인다. 자식 수집기 취소 확인만 무시하며
부모 운용기의 취소는 다시 전달한다. 종료 대기 중 취소에도 INTERRUPTED·지속 정리
요청과 잠금 반환을 보존한다. 반복 수집 실패와 종료 대기 중 외부 취소를 실제 운용기/
모의 전송으로 검증한다. 기존 시간 제한과 관리 우선은 유지하며 복구는 수정 코드 되돌림이다.

FR038, 등급3의 기존 운용 연결 보정: 운용기가 신호를 실행하기 전에 수집 세대를
소비 완료로 기록해 WAIT_BROKER 이후 남은 종목을 잃는 문제를 수정한다. 실행 결과가
WAIT_BROKER인 경우 완료 기록을 보류하고 다음 관리 주기에서 같은 신호를 다시 검사한다.
처리 도중 새 수집 결과가 와도 새 세대를 완료로 오인하지 않도록 호출 전 세대를 캡처한다.
기존 관리 우선·수집 오류 캐시 삭제·신선도·한도·중복 주문·중지 경계는 유지한다.
모의 전송과 실제 전략/운용기로 전체·부분 체결 후 나머지 종목 처리, 오래된 신호/중지/
수집 실패 차단을 확인한다. 되돌림은 이 수정 커밋 복구이며 원본 장부는 수정하지 않는다.

FR037, 등급3: intraday_cash_ledger.compute_net_cash가 내부 공급자의 시작/끝 스냅샷과
사건 원본을 정밀도80으로 재계산한다. 원본 순서 번호와 매 사건 후 잔액을 요구하며
누락을 정렬/자동 번호 부여로 숨기지 않는다. intraday_observation에서 이 결과로 전체
보유 NAV를 계산한다. 원본 범위 인증이나 KIS 변환기를 구현한 것으로 취급하지 않는다.
기존 입력 경로/권한을 유지하며 되돌림은 신규 기준 사용 중지 및 코드 복구다.
잔액/통화/번호/시각 반례와 관측 변환기 연결을 검증한다.

FR036, 위험 등급3: 계좌 변환기에 순현금과 전체 상장주식 평가액의 자체 합산 경로를
연결한다. 현금/계좌 범위가 검증된 내부 공급자에 한하며 net_cash는 채무·미결제 등을
반영한 순현금이고 execution_cash는 주문에 쓸 현금이다. 공급자가 이 평가 기준을 명시하면
보고 NAV/승인 플래그를 요구하지 않고 Decimal 정밀도80에서 직접 계산한다. 기존 검증된
보고 NAV 경로는 유지한다. 실패 시 기존 입력 거절·원본 가격 신선도·한도 검사를 보존한다.
복구는 신규 계산 경로의 사용 중지/코드 되돌림이며 원본 장부·실행 권한은 변경하지 않는다.
단위 반례와 모의 계좌→변환기→실제 주문 엔진/정리 통합을 검사한다. 실제 KIS 순현금
원본 검증 제공자와 T009/T010 전체 완료를 이 산술 구현으로 대체하지 않는다.

FR019의 기존 자체 시험 보정: 가짜 증권사 계좌의 현금도 전진 모의 운용과 같은
0.25% 가정 비용을 매수·매도 양쪽에 반영한다. 누적 체결 원본에서 다시 계산하므로
반복 관측·부분 체결·재시작에서 비용을 중복 차감하지 않는다. 실제 운용기의 중지→
늦은 체결→재시작→청산 시험에서 현금과 순자산까지 확인한다. 실제 계좌 수수료를
입증하거나 체결 자격을 발급하는 변경은 아니다. 위험 등급1의 시험 환경 버그 수정이며
실제 계좌 공급자와 T009/T010의 최종 통합은 계속 미완료다. 복구는 이 시험 보정의
되돌림이며 실제 장부·권한·주문 경로는 변경하지 않는다.

사용자 정정에 따라 실제 투자 실행은 목표에서 제외하고 프로그램의 연결·복구·실패 차단을
구현한다. T009/T010의 미구현은 계속 추적하되 실제 체결 증거 없이 소프트웨어 시험을
수행할 수 있다. 첫 단계 FR019는 기존 주문 엔진·운용기를 시험용 전송과 임시 DB로
직접 실행하는 자체 시험 명령이다. 운영 환경 변수·외부 네트워크·사용자 DB를 사용하지
않고 성공·실패를 고정 출력으로 반환한다. 기존 명령은 유지하며 복구는 새 명령 사용 중지다.
시험 전송은 코드에 고정하며 실제 접속으로 변경하는 옵션은 제공하지 않는다.

FR020은 execution/intraday_observation.py에서 기존 계좌 읽기 함수와 시세 스냅샷을
기존 Observation으로 변환한다. 공급자 계약 검사·30초 전체 소요·원본 발생시각·보유/주문
정규화를 수행하고 기존 validate_observation을 재사용한다. 기존 observe_account는
미검증 현금/NAV를 보고하므로 이 공급자로는 명확한 입력 오류가 반환된다. 정상 경로는
모의 공급자→변환기→실제 IntradayExecutor→MockTransport로 검증하며 현재 KIS 생산
검증이 완료된 것으로 기록하지 않는다. 기존 프로그램 내 공급자는 신뢰 경계이며
외부 JSON의 true 값이나 사용자 승인만으로 검증 공급자를 만들지 않는다.

## 기술 환경

FR035, 등급3. execution/intraday_cost_reconciliation.py는 실제 BrokerExecution 목록과
거래 원본을 받는다. 원본의 산술을 다시 계산하고 mode=ro/BEGIN으로 로컬 주문/체결을
한 시점에서 조회한다. 주문번호→종목/방향→누적 수량/금액과 명세 그룹의 수량/거래금액을
순서대로 비교한다. 비용을 주문별로 나누거나 보고서 MATCH 표시를 승인으로 읽지 않는다.
기존 잔고 점검의 --execution-db는 두 날짜 옵션과 기존 파일을 요구하고 동일 계좌 인증으로
실제 주문체결 조회를 추가한다. 원본 두 조회의 날짜 기준 차이와 DB 계좌 인증은 이 결과가
입증하지 않으므로 실행 자격 값은 계속 false다. 결과는 고정 사유와 건수만 공개한다.
모의 HTTP/실제 SQLite를 연결해 누적 부분 체결·중복/외부 주문·수량/금액 차이·미생성 DB·
장부 바이트 보존을 검증한다. 기존 기능/안전장치 제거 없음. 복구는 선택 옵션 사용 중지이며
원본 DB/감사/권한은 수정하지 않는다.

FR034, 등급3. 기존 거래 명세 조회기가 모든 페이지를 읽은 후 audit_settlements를
호출한다. 정규화 행을 다시 검증하고 넉넉한64자리 Decimal 문맥에서 통화별 합산한다.
두 수수료 항목의 합과 정산 차액이 일치하는지만 판단하며 항목 의미가 맞다는 별도
증명이 아니다. 불일치/무거래의 검증 합계는 None이며 이후 소비자가 일부 합계를 현금으로
잘못 쓰지 못하게 한다. 원본/페이지 요약/중복 행은 유지한다. 공개 변환은 건수/판정만
허용하며 현금/체결 검증 상태는 바꾸지 않는다. 매수·매도·여러 통화·중복·빈 보고서·
반올림 경계·불일치와 실제 모의 HTTP→기존 점검 명령 연결을 검사한다.
복구는 신규 검사 사용 중지/코드 복구이며 기존 장부·권한·조회 경로를 제거하지 않는다.

FR033, 등급3·K4 감사 기록 영역. fill_sync가 BrokerExecution.ordered_at_utc를
PlannedFill.executed_at_utc에 복사하는 오류를 제거한다. 체결 자체의 시각이 없으면
기존 기록용 시각은 조회 시각이며 FILL 감사에 timestamp_basis=OBSERVED,
observed_at_utc를 추가한다. 기존 수량/금액·중복 방지·상태 전이는 유지한다.
명시적 체결 시각은 PROVIDED_EXECUTION으로 구분하고 시간대가 없거나 미래이면
트랜잭션 전체를 거절한다. 기존 감사 모델의 기본 출처는 UNSPECIFIED다.
fill_sync와 감사 모델 소스를 실행 지문에 포함한다. 역사 조회의 주문시각을 재현하는
기존 오검증 시험을 관측 출처 확인으로 바꾸고 실제 임시 DB/감사 보존·오류 롤백을 시험한다.
되돌림은 신규 실행을 중지하고 버전을 되돌리는 것이며 새로 기록한 출처/기존 장부는
삭제하지 않는다. 실제 발생 시각을 확보하는 후속 대조까지 완료한 것으로 표시하지 않는다.

FR032, 등급3. broker/intraday_transactions.py가 기존 제한/재시도 클라이언트로
CTOS4001R을 읽고 FK100/NK100 연속 조회를 끝까지 수행한다. 읽기 전체30초 제한과
모든 페이지 오류의 전부 실패를 적용한다. 기존 잔고 점검 명령에 선택적 등록일 구간을
추가하되 기본 호출은 유지한다. 새 자료는 내부 대조기의 원본 입력이며 실제 장부와의
거래 대응·수수료 검증 및 현금 계산 연결은 별도 미완료로 남긴다. 기존 기능 제거 없음.
복구는 선택 옵션 사용 중지와 커밋 되돌림이며 실제 계좌/장부에 쓰지 않는다.
모의 HTTP로 정확한 조회 인수·전체 페이지·중복/소수 보존·후속 페이지 오류·비밀 비노출을
검증한다. 현재 구현을 실제 계좌 조회 성공 또는 체결 비용 검증으로 보고하지 않는다.

FR025는 기존6bp 지정가 계약의 수치 오류 보정이다. 모의100달러 매수가100.05와 실행
100.06의 차이를 재현했다. 공통 Decimal 가격 함수로 모의/신호/강제 정리의 반올림을
일치시키며 수수료·위험 한도·실제 전송 권한은 변경하지 않는다. 공통 코드 지문을 모의
identity와 실행 지문에 넣어 기존 가격 모델 기록을 새 모델로 이어쓰지 못하게 한다.
가격 경계·실제 모의 신호/체결·실행 신호 비교 및 기존 정리 회귀를 검증한다.
복구는 운용 중지 후 버전 복구다. 기존 모의 기록은 보존하며 새 모델은 새 장부를 요구한다.

FR024는 별도 서버 키를 사용하는 HMAC-SHA256 전략 등록을 추가한다. 원본 연구는
비공개 고정 사본의 사전등록으로 재계산하며 실패 시 등록하지 않는다. 고정 경로 키는
O_NOFOLLOW와 열린 파일의 소유자/종류/권한/정확한32바이트 검사로 읽는다.
키 생성·설치나 실제 등록은 이번 개발에서 실행하지 않는다. 잘못된 키·서명·후보·소스·
미래 시각·사전등록 변경을 시험한다. 이 경로가 서명한 서버 시각을 인증하되 서버 시계
오류나 키 관리자 악의까지 증명하지 않는다. 회수는 키 교체이며 이전 서명은 거절된다.
헌법 IX.D는 운영자 지시 아래 필요한 절차 규칙 개정을 허용하므로 X.4를 개발 금지로
해석하지 않는다. 기존 하루1회 권한을 재사용하지 않고 별도 단타 권한 구현은 계속한다.

FR023은 모의 장부의 직접 검사와 독립 재생을 구현한다. SQLite 읽기 전용 연결에서
backup으로 일관된 임시 스냅샷을 만들고 원본은 수정하지 않는다. 현재 PaperRuntime의
identity와 원본 identity를 비교한 뒤 모든 사건을 같은 원본 봉/수신시각으로 재생한다.
사건 해시가 다르면 재작성된 상태로 간주해 실패한다. 프로그램 내부의 검증된 전략 고정
시각이 세션 시작보다 앞선 완결 세션만 집계하고 늦은 자료·중단·마감 잔여를 제외한다.
전진 최소60세션은 원래 사전등록값을 사용한다. 이 단계는 고정 시각 인증·수집 등록·
비용/체결 동등성·실행 자격 발급기를 구현한 것이 아니며 그 연결은 별도 남긴다.

FR022는 execution/intraday_program.py에 명시적인 시작 조립 경로를 추가한다.
기존 연구 선택·관측기·라우터를 연결하고 자격 검사 함수를 필수로 받는다. 테스트에서는
모의 전송의 라우터와 명시적 시험 자격을 사용하며 생산 승인 발급을 대체하지 않는다.
구성 직전·실행 중 실제 후보/소스 지문과 라우터 계좌를 확인하고 기존 쓰기 검사는 유지한다.
확정 준비 한도600 USD 이내에서만 구성하며 계좌 허용목록에 현재 계좌가 있어야 한다.
기존 운용기의 중복 잠금·지속 중지·관리 우선·실패 복구를 그대로 호출한다.
새 프로그램 소스도 실행 지문에 포함한다. 정상 시작·대기·실패·재시작을 실제 엔진과
모의 전송으로 검증하며, 아직 없는 생산 자격 제공자를 이 조립 완료로 표시하지 않는다.

실행 지문은 후보가 주장하는 전략 해시만이 아니라 실제 후보 매개변수 전체를 포함한다.
따라서 같은 strategy_fingerprint를 남긴 채 내부 parameters를 바꿔도 실행 지문이
바뀌며 기존 엔진의 비교가 차단한다. 허용 후보·한도·기존 자격 요구는 변경하지 않는다.

FR021은 날짜별 원본 자료를 review_archives로 재계산하여 실행 구성의 연구 후보를
선택한다. 사전 작성된 연구 보고서를 실행 권한으로 읽는 경로는 제공하지 않는다.
필요 파일은 비공개 임시 폴더에서 생성·읽고, 선택 후보는 원래 사전등록 레지스트리와
지문이 같아야 한다. 후보·자료 지문·현재 실행 지문은 결과에 묶되 실제 자격은 false다.
실행 지문에는 관측 변환기와 연구 선택 연결 소스도 포함한다. 이 후속 변경의 전체 시험은
최종 시작 구성이 완성된 뒤 수행하며, 현재 구성에서 오래된 연구/실행 지문은 사용하지 않는다.

Python 3.11+, httpx, SQLite, asyncio, websockets 15~17의 asyncio 전송.
기존 `scripts/intraday_runtime.py`와 `PaperRuntime` 장부를 재사용한다.
새 시세 입력은 기존 기본 비활성 `realtime.py`와 분리해 기존 워커 동작을 바꾸지 않는다.

## 헌법 점검

등급 4(기존 계좌 재사용 후속 포함). 허용 종목, 현금·계좌 검증, 주문 권한, 단계별 자본, 공용 거래 DB와
계좌 잠금, 감사 로그, 장중 배포 금지는 그대로 유지한다. 공식 시세 접속과
외화 구매력 GET만 새로 실행 가능하다. 모의 장부는 실거래 공용 장부와 구분한다.
전체 계좌 NAV를 임의로 만들거나 사용자 기준선을 브로커 검증값으로 승격하지 않는다.

## 조사 및 결정

계획 기술의 독립 조사 두 건(operator_runtime_research, kis_inputs_research)을 수행했다.
결과와 공식 출처는 research.md에 기록한다. 주입형 시세 코드에는 실제 전송·발생시각이
없었고, 고정 AAPL/1 구매력 조회는 주문별 상한으로 사용할 수 없었다.
전체 NAV 입력과 실제 단타 권한은 미완료이며 시세 연결 성공으로 완료 처리하지 않는다.

## 구조

- src/auto_invest/broker/intraday_inputs.py: 엄격한 체결 파서, 실제 전송, 외화 구매력.
- scripts/intraday_operator.py: run/status/stop/quotes/buying-power의 단일 진입점.
- tests/unit/test_intraday_inputs.py: 프로토콜·수량·시각·비밀 오류 경계.
- tests/integration/test_intraday_operator.py: 실제 로컬 전송, 수명주기, 명령 연결.

## 순서와 검증

1. 명세·입력 계약·작업 목록을 작성한다.
2. 순수 파서와 외화 구매력 조회를 구현하고 반례 테스트한다.
3. 실제 웹소켓 전송·해지·연결 종료와 캐시 무효화를 검증한다.
4. 기존 모의 실행기를 단일 명령의 지속 운용·상태·중지로 연결한다.
5. 문서 명령을 실제 실행하고 관련 주문 엔진 회귀, 전체 테스트, 린트, 하네스를 확인한다.
6. 사용자 전체 목표의 미완료 실주문 연결은 숨기지 않고 초안 PR에 남긴다.

## 복구

신규 모의 프로세스를 협조적으로 중지하고 기존 실행 명령으로 돌아갈 수 있다.
기존 장부·봉 자료·감사 기록을 삭제하거나 재작성하지 않는다. 실거래 배포·가동은 하지 않는다.

## 기존 계좌 재사용 후속 구현

운영자는 기존 한국투자 계좌 사용을 확정했다. 새 계좌 개설을 요구하지 않는다.
기존 일일 대조의 외부 보유 기준표 계약을 단타 엔진에도 적용한다. 위험 등급4이며
명세 FR-009~011, 자료 모델, 실행 계약과 작업 목록을 함께 확장한다.

입력은 기존 `load_external_holdings`의 정규화된 종목·정수 수량 매핑이다.
엔진은 검증한 복사본을 읽기 전용으로 보관한다. `_refresh`는 공용 체결 장부와
기준표 합계가 증권사 수량과 같은지 대조한다. `_owned`는 자기 전략 실제 체결만
추적하는 계약을 유지한다. 일별 기준과 주문 결과에 기준표 해시를 추가한다.
기존 보유 가격도 신선도·전체 노출 검사 대상이다. 기준표 파일 자체는 수정하지 않는다.

실제 KIS 관측과 전략 권한 연결을 이 변경으로 대체하지 않는다. 소수 수량과 미확인 자산은
현재 정수 수량 계약을 통과시키지 않는다. 복구는 엔진 중지 후 변경 커밋 되돌림이며
기본 빈 기준표는 이전 엄격 대조와 동일하다. 신규 테스트에서 기존 보유가 있는
매수·부분 체결·취소·청산·재시작과 미신고 변동 및 노출 한도를 실제 주문 엔진으로 검증한다.

## 실주문 운용 수명주기

등급4, FR-012~016. 기존 엔진에 봉 없이 실행하는 관리 주기를 추가한다.
신규 매수는 전략 판단 경로에서만 가능하며 관리 주기는 보유·미체결 확인, 만료 취소,
기존 손실 방어와 장 마감 정리를 담당한다. 중지 요청과 완료는 추가 전용 DB 기록으로
보존하고, 최종 주문 직전 진입 검사에 중지 상태를 더한다.

새 `execution/intraday_runtime.py`는 DB 경로에 묶인 수명주기 잠금, 실행 식별자 상태,
독립 봉 수집 작업, 관리 우선 주기를 제공한다. 기존 모의 실행기는 그대로 보존한다.
시세 공급 실패 시 진입을 막되 관리 작업은 계속한다. 종료 시 보유가 남으면 정리 완료를
주장하지 않는다. 설정상 주기 제한은 시험·운영 종료 경계이며 실제 잔여 주문을 삭제하지 않는다.
실행 식별자의 소스 해시에 새 운용기 파일도 포함해 운용 제어 변경을 과거 코드의
전략 검증 결과로 실행할 수 없도록 한다. 중지 요청마다 별도 정리 식별자를 사용하므로
같은 날 중지·재개·재중지 시 이전 청산 주문과 충돌하지 않는다.
실행기는 기존 엔진의 관측·권한 계약을 완화하거나 임의 정상값으로 대체하지 않는다.
실주문 운용 모듈의 버전 복구는 새 대조로 STOPPED를 확인한 뒤 진행한다.
미정리 주문·보유가 남으면 이전 코드로 교체하지 않고 같은 코드·식별자로 관측과 권한을
복구하여 정리를 이어간다. 공용 장부·중지 기록·감사 이력은 삭제하지 않는다.

## 사용자 실행 시작 연결

FR-030/031, 등급4. intraday_launch.launch가 기존 설정/비밀값 로더, 이미 생성·변경 적용된
공용 DB, 원래 보유 기준표를 읽고 실제 OrderRouter와 고정 배포 쓰기 잠금을 구성한다.
build_kis_program의 내부 자격 검사가 성공한 뒤에만 운용기를 호출한다. 시세는 운용기
소유이며 봉 수집은 기존 ReadTransport/collect_kis를 사용한다. 시작 명령의 신호는
기존 지속 정리 요청으로 전달하고 종료 시 HTTP/DB를 닫는다. 인증 갱신 콜백을 체결
조회 앞에 추가해 초기 빈 토큰·만료 토큰을 체결 조회에 보내지 않는다.
승인에 운용 설정 지문을 추가하여 다른 DB/중지 파일/한도/기준표로 재사용하지 못하게 한다.
오류 결과의 주문0 단정을 제거하고 시작 명령의 종료 코드0은 검증된 STOPPED에만 준다.
실제 계좌 계산/체결 동등성이 미검증인 현 상태는 시작 거절이며 프로그램 전체 완료가 아니다.
모의 계좌/자격으로 실행 시작→중지→동일 장부 재개, 실패 전 네트워크0, 자료 보존과
핸들 닫기를 확인한다. 기존 run/status/stop과 기존 장부/안전장치를 제거하지 않는다.

## 전략 자격 재검사와 운영 승인 구분

FR-028/029, 등급4. prepare_qualification은 현재 코드 커밋을 읽고 select_research와
assess_registered_forward를 순서대로 호출한다. 원본 재계산·60세션·체결 동등성 검사를
통과해야 계좌/전략/자본에 묶인 동기 검사를 반환한다. KIS 구성은 이 내부 공급자를
사용하며 외부 qualify 콜백을 더는 받지 않는다. 일반 모의 조립용 build_program은 유지한다.
서버 root 소유의 고정 승인 파일은 별도 운영자 승인만 증명한다. 관련 검토 지문을 적었다고
실제 체결·캐너리 증거를 머신이 검증한 것으로 해석하지 않는다. 현재 전진 재계산기의
execution_parity_verified=false는 거절되며 생산 자격 전체가 완성됐다는 뜻이 아니다.
승인 파일은 매 호출 다시 읽어 삭제·변경·만료가 즉시 반영된다. 비밀 오류는 고정 코드만
반환한다. 새 검사를 사용 중지하면 기존 내부 모의 조립으로 되돌릴 수 있으며 어떤 장부도
변경하지 않는다. 시험은 합성 승인/의존성 결과로 검사 연결을 검증하고 실제 승인은 발급하지 않는다.

## 생산 계좌 인증 연결

FR-027: 운용기의 장부 잠금 안에서 비동기 자원 수명을 관리한다. KIS 구성 함수는
StrictQuoteFeed와 같은 계좌의 구독 인증을 고정 연결한다. 종료된 시세 작업이나
불완전 시세는 마지막 진입 검사에서 차단하고 원래 검사를 제거하지 않는다.
종료 순서는 봉 수집 취소→시세 연결 취소/닫기→진입 검사 복구→장부 잠금 해제다.
실행 중복·시세 작업 종료·실행 취소·정리 요청 재시작을 실제 엔진/임시 장부로 검사한다.
신규 구성 함수를 사용하지 않으면 기존 운용 수명은 유지되며 원본 장부는 보존한다.

FR-026, 등급4. `build_kis_program`은 같은 라우터의 주문 권한 객체에서 계좌와
인증 정보를 가져와 `KISExecutionObserver`로 연결한다. 기존 인증 캐시/갱신을
재사용하고 매 조회의 토큰을 같은 권한 객체에 갱신한다. 별도 계좌 입력이나
임의 계좌 정상값 주입 경로를 추가하지 않는다. 호스트·계좌·키·연결 변경은 조회
전후에 검사하고 인증 실패는 고정 오류로 숨긴다. 인증 외 계좌 요청은 GET이다.
전체 잔고 계약 검증과 전략 자격·CLI 시작 구현은 별도 잔여 작업이다.
되돌림은 신규 구성 함수 사용 중지이며 기존 수동 조립과 모의 경로는 보존한다.
모의 HTTP로 토큰 발급/캐시 재사용, 계좌 일치, 인증 중 계좌 변경, 오류 비노출,
조회 성공의 미검증 상태 유지와 기존 엔진 통합을 검증한다.

## 보관 자료의 연구 연결

등급2, 기존184 SDD의 FR017/018 확장. 날짜별 KIS 보관 자료가 기존177 검증기로
이어지지 않는 연결 공백을 없앤다. analytics/intraday_archive.py가 원본 지문과
고정 파일명을 확인한 뒤 비공개 임시 스냅샷에서 기존 자료 검증기를 사용한다.
모든 날짜가 완결되고 공급자·수정 정책·합성 여부가 같을 때만 새 연구 폴더에 결합한다.
기존 후보·756세션 기준·판정 로직은 그대로 호출한다. 명령은 자격값이나 네트워크가
필요 없으며 실행 장부를 열지 않는다. 오류는 고정 코드로 공개하고 원본을 보존한다.
동일·누락·변조·공급자 혼합·중복 날짜·기존 출력·CLI 무자격값 시험을 수행한다.
실패 시 신규 명령 사용을 중지하면 되며 기존 수집기·모의 운용·주문 경로는 유지된다.
FR056: 실제34686417861에서 OTCB 원본 가격/평가가 모두 양수였으나 현재 파서가
무조건None으로 버리고 있었다. 선택적 보고 필드는 제공되면 기존 엄격한 숫자 규칙으로
검사하고 누락과 구분한다. 원본 매도가능 수량은 보유를 넘으면 거절한다. 같은 USD
일반잔고의 보고 평가 합은 Decimal80으로 계산하며 공개 출력에는 완결 여부/건수만
남긴다. 미해석 자산의 실행 차단과 원본 발생시각 시세 조건은 유지한다. HTTP→파서→
합산의 누락/0/양수/잘못된 값·정밀도·비노출·시각 비대체를 시험한다. 새 조회·주문은
없다. 복구는 이 보존/합산 연결을 되돌리며 원본 장부와 감사 기록은 유지한다.
FR057: broker/intraday_cash_baseline.py에 독립 전후 조회와 금액 정규화를 구현한다.
새 입력기·계좌 파서·공유 숫자 파서를 실행 지문에 포함해 코드 변경 시 이전 자격을 재사용하지 않는다.
같은 KISExecutionObserver의 연결·계좌·갱신 토큰을 사용하고 전체 읽기30초 제한을
유지한다. 단일 USD 행을 기준으로 모든 margin USD 행을 대조하므로 country 중복
제거 가정이 필요 없다. 누락/음수/다른 현금/조정/연속 페이지/변화/노후 입력은 거절한다.
보고 현금은 result.reported_cash_baseline에만 연결하고 전체 계좌 검증 플래그를
바꾸지 않는다. 금액은 공개하지 않는다. 실제 HTTP 모의→전후 수집→실행 입력 연결과
오류·시각·비밀 비노출을 확인한다. 복구는 추가 읽기 연결을 되돌리는 것이며 기존 장부는 유지한다.

FR058 (위험3): 현재 엔진은 모든 보유를 실행 시세로만 표현해 OTC 보고 평가가 있어도
버리거나 시세 시각을 만들어야 하는 구조다. Observation에 보고 평가 전용 형식을
추가하고 보유 수량/조회 구간을 검증한다. 실제 단타 종목의 가격/원본 시각은 기존
marks/mark_times에만 남긴다. 새 평가 기초에서는 검증된 순현금+상장 실행 시세 평가+
비실행 보유 보고 평가로 NAV를 계산한다. 계좌/현금 검증이 없는 보고서는 계속 거절한다.
전체 노출 계산을 공통 함수로 모아 보고 자산을 정확히 한 번 포함한다. 모의 실제 엔진으로
보고 자산의 전역 한도 반영·소유 분리·주문 가격 비대체와 누락/중복/수량/구간 오류를
검증한다. 전체 계좌 생산 공급자 및 구독 연결은 별도 후속이며 단독 완료로 표시하지 않는다。
복구는 신규 평가 형식 연결을 되돌려 기존 모든 보유 시세 요구로 복귀한다. 한도 수치,
허용 종목, 감사 기록, 승인, 실거래 단계, 배포 설정은 변경하지 않는다.

FR059: KISExecutionObserver._read에서 실제 계좌의 미지원 OTC 보고를 조건부 변환한다.
전체 계좌 확인을 대신하거나 기존 미검증 목록을 비우지 않는다. _ValuationFeed는 같은
입력기를30초 제한으로 읽고 보고 형식이 있는 종목만 시세 발견/구독에서 제외한다.
검증되지 않은 OTC 평가를0으로 만들거나 임의 거래소를 고르지 않는다. 실제 모의 HTTP
읽기에서0/양수/누락 보고값을 검사하고 혼합 구독의 기존 종목/종료/시세 시각을 검증한다.
복구는 새 변환/구독 분리 연결을 되돌리는 것이며 원본·감사·한도는 유지한다.

FR060: 186의 국내 현재잔고 수집기를 통합하고 KISExecutionObserver._read에서 기존
토큰/계좌/제한 클라이언트로 호출한다. 전후 연결 동일성과 전체 입력30초 제한을 유지한다.
reported_domestic_account에 KRW 원본 정규화를 보존하며 기존 검증 false는 유지한다.
실제 모의 HTTP 계좌 읽기/프로그램 조립의 추가 요청·계좌 묶음·오류/검증 차단을 시험하고
새 소스를 실행 지문에 넣는다. 복구는 이 읽기 연결을 되돌리며 거래명세/비용 대조 및
기존 장부는 보존한다. 국내 현재 데이터와 전체 자산표를 함께 사용하는 범위 판정은 후속이다.
# FR064 구현과 검증

읽기 전용 계좌 수집기를 추가한다. CURRENT → 일반 미국 4개 → 국내 → 자산표 2개
→ CURRENT 순서와 요청별 실제 시작/수신 시각을 보존한다. 증거금 중복 호출을
제거한다. 전후 현재잔고/중간 증거금 원본만 기존 정규화기에 전달한다. 자산표는
공개 판정과 비공개 숫자 자료를 구분한다. 연결 변경·시간 역행·30초 초과·오류·
호출 취소·현재 수량 변화·중간 조회 구간 대조를 모의 HTTP로 검증한다.
등급3 입력 안전 변경이며 기존 검증 표시는 유지한다. 되돌림은 이 수집기 연결을
이전 순차 조회로 복귀하는 것으로 가능하다. 배포/실주문은 이 변경의 완료 기준이 아니다.
# FR065 보고 자산 분류의 현재 입력 대조

기존 자산표 숫자 파서와 고정20행 분류를 사용해 현재 현금/보유 결과와 대조한다.
두 원본은 현금 관측 구간 안에 있어야 하며 현재30초 이내다. 모든 분류의 합계를
소수80자리 문맥으로 계산하고 지원 외 분류/채무의 비영 값을 거절한다. 현재 현금은
KRW로 대조하고 외화 평가를 USD 현금으로 오인하거나 다른 보고 순자산에 더하지 않는다.
기존 정상 입력기에 추가 GET 없이 연결하고 실행 지문에 포함한다. 등급3 입력 검증
변경이며 원래 전체 계좌/현금 승인 조건을 보존한다. 되돌림은 새 대조 연결 제거다.
# FR066 거래내역 조회 범위 보정

현재 공식 property 계약의 OVRS_EXCG_CD=공백을 적용한다. 오래된 국가 코드와
다른 시세 API의 거래소 코드를 혼용하지 않는다. 같은 observe_transactions를
사용하는 정상 자격 검사와 선택적 CLI 조회에 공통 적용한다. 모든 페이지의 실제
요청 값, 잘못된 필터의 요청 전 거절, 복수 통화 보존을 시험한다. 위험 등급3,
기존 T028 거래내역 구현의 계약 보정이며 새 주문/승인/가동 경로는 없다.
복구 시 이번 조회 필터 보정을 되돌리되 이미 보관한 원본은 삭제하지 않는다.
