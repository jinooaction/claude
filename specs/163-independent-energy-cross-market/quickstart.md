# Quickstart: Independent Energy Cross-Market Factory

1. Run the focused unit tests for source parsing, lag chronology, 16-candidate identity, ridge determinism, objective lanes, post-hoc lock, and fail-closed behavior.
2. Run the probe against fixed fixtures and confirm the same JSON and fingerprints repeat.
3. After preregistration, download the four official EIA monthly XLS files, French 49-industry ZIP, FRED cash data, and existing incumbent sources.
4. Run the probe with the latest code commit and existing 720-trial factory evidence, gate calibration, and complete real-world controls.
5. Confirm `complete_trial_count=16`, `global_audit_trial_count=736`, and `unique_trial_fingerprint_count=736`.
6. Inspect the frozen development winner, untouched holdout, both objective lanes, controls, power, and prohibited post-hoc ranks.
7. Run `uv run pytest`, `uv run ruff check src tests`, YAML parsing, `git diff --check`, strict harness, handoff facts, and PR quality gate.
8. After merge, verify deploy and canonical strategy-factory production replays. Run KIS read-only smoke only to confirm plumbing; no order is authorized.
