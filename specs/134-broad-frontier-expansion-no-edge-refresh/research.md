# Research: Broad Frontier Expansion NO_EDGE Refresh

## Decision: add second-wave broad no-edge candidates

The first-wave broad no-edge map closed asset universe, signal/horizon, regime/cost, and data-gap causes. Current sidecars still show `PREVIEW_ONLY` and `NO_EDGE_YET`, so waiting only for fresh evidence would leave no immediate Codex work despite a live blocker still existing.

## Decision: do not lower live gates

The latest `money-path` still fails PSR and has `armed:false` plus `latest_intent_loss`. Lowering gates would be a grade 4 money-path change and is out of scope without explicit approval.

## Decision: parent candidate must not repeat after release

The parent candidate exists to open a broad map once. Once any broad no-edge parent is released, autonomous-work should choose the first unreleased map row instead of minting another parent fingerprint.
