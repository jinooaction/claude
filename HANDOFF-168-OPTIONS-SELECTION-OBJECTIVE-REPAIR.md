# HANDOFF-168 - 옵션 선택·목적 교정과 자동매매 시스템 전체 리뷰

## 상태

PR #684가 `main`에 병합됐고 배포 실행 `32910288188`과 생산 전략 공장
`32910288219`가 성공했다. 현재 돈 경로는 `PREVIEW_ONLY`, 단 0, 자본 0,
실주문 불가, 전진 0/20, 전략 체결 0건이다. 이 작업은 주문·자본·마진·한도·허용 종목을
바꾸지 않았다.

## 운영자 의심에 대한 결론

운영자의 의심은 일부 맞았다. 옵션 프리미엄이 존재하는가와 주식 포트폴리오에 넣을 가치가
있는가를 한 기준으로 묶었고, 한 번 고른 개발 승자를 긴 홀드아웃에서만 판단한 설계는 잘못됐다.
이번 작업은 그 오류를 다음처럼 고쳤다.

1. 프리미엄 존재, 포트폴리오 채택, 자동 타이밍 가치를 별도 질문과 기준으로 나눴다.
2. PUT 안에서만 중첩형 확장 검증으로 후보를 선택했다.
3. 선택에 쓰지 않은 WPUT에 같은 후보 식별자와 월별 가중치를 그대로 재생했다.
4. 기존 스펙 164 결과는 `legacy_selection` 아래 보존하고 과거 결과를 다시 쓰지 않았다.

고친 뒤 PUT 단독 프리미엄은 확인됐지만 독립 WPUT에서는 같은 경제적 프리미엄이 확인되지
않았다. 따라서 이번 탈락은 더 이상 “질문을 잘못 물어서만 생긴 탈락”이 아니다.

## 생산 결과

- 사용 기간: 2007-03~2026-06, 정렬된 232개월.
- 후보: 현재 16개, 과거 736개, 누적 고유 전략 지문 752개.
- 시간 구조: 12개 외부 12개월 평가 구간, 외부마다 내부 검증 최소 2개, 시간 위반 0건.
- 프리미엄 존재 PUT: 연 현금 초과 `+4.812579%`, PSR `0.954087`, 통과.
- 프리미엄 존재 WPUT: 연 현금 초과 `+0.536821%`, PSR `0.638860`, 실패.
- 포트폴리오 채택 PUT: 연 현금 초과 `+2.312035%`, 주식 대비 샤프 `+0.036908`, 실패.
- 포트폴리오 채택 WPUT: 연 현금 초과 `+0.476446%`, 주식 대비 샤프 `-0.322830`, 실패.
- 자동 타이밍 PUT: 같은 단순 노출 대비 연 `-1.199857%`, 샤프 `-0.173479`, 실패.
- 자동 타이밍 WPUT: 같은 단순 노출 대비 연 `-0.193512%`, 샤프 `+0.047806`, 실패.
- 최종 판정: `NO_CROSS_INDEX_PREMIUM`.
- 승격 상태: `research_canary_eligible=false`, `paper_forward_eligible=false`,
  `promotion_allowed=false`.

## 시스템 전체 리뷰

| 영역 | 점수 | 판단 |
|---|---:|---|
| 주문 자동화 | 8.0/10 | 서명된 예약 경로, 단일 계좌 권한, 사전 점검, 체결 동기화와 계좌 조정이 있다. |
| 자본·위험 안전 | 8.5/10 | 기본 거부형 한도·허용 종목·중단·자본 사다리·정확한 지문·즉시 강등이 강하다. |
| 자료 무결성 | 6.0/10 | 공식 자료와 형식·최신성·해시 검사는 있으나 연구 이력이 당시 시점별 보관본은 아니다. |
| 연구·통계 | 5.0/10 | 홀드아웃·비용·PSR/DSR/PBO와 독립 재생은 있으나 프로그램 전체 다중검정이 남았다. |
| 인공지능 활용 | 4.0/10 | 릿지와 기울기 상승 모델을 실제 전진 검증하지만 현재 두 인공지능 보고서 모두 `NO_EDGE`다. |
| 실행 동등성 | 4.0/10 | 주식 주문은 구현됐지만 옵션 기준지수와 소액 3종 포트폴리오는 그대로 주문할 수 없다. |
| 수익 엣지 준비 | 2.0/10 | 현재 후보 `NO_EDGE`, 전진 0/20, 전략 체결 0, 독립 옵션 확증 실패다. |

전체 실거래 수익 준비도는 가장 약한 필수 항목인 수익 엣지 때문에 2.0/10이다.

## 높은 우선순위 문제

1. **현재 투자 가능 엣지 없음**: 자동 주문을 내릴 증거가 없다. 자본 0 유지가 맞다.
2. **팩토리 자기신고 신뢰**: 소비자는 생산자가 선언한 `FACTORY_EDGE`와 차단 관문을 믿는다.
   당시 시점 자료, 프로그램 전체 다중검정, 주문 가능한 상품·비용 동등성을 독립 확인하지 않는다.
3. **752개 전체 다중검정 미해결**: 후보 지문은 고유하지만 거짓 양성 제어는 가족별 16/64개에
   주로 적용된다. 실패를 본 뒤 새 가족으로 이동한 전체 탐색 비용을 함께 벌점 처리해야 한다.
4. **옵션 실행 동등성 없음**: PUT/WPUT는 가상 기준지수다. 옵션 호가, 배정, 세금, 마진,
   담보와 실제 중개인 주문을 재현하지 못하므로 이 전략군은 자본에 연결하면 안 된다.
5. **소액 구현 가능성 부족**: 현재 NAV 약 1,454달러의 단1은 약 145달러다. 3개 동일 비중은
   종목당 약 48달러라 정수 주식과 최소 주문 50달러 조건 아래 검증 비중을 만들기 어렵다.

## 유지해야 할 강점

- 라이브 워크플로는 기본 미리보기이며, 실주문은 서명·예약 실행·기계 승인·정규장 관문을 요구한다.
- 주문 제출 전 의도를 기록하고 계좌당 단일 권한 잠금을 잡는다.
- 제출 결과가 불명확하면 재주문하지 않고 `SUBMISSION_UNKNOWN`으로 막는다.
- 미확정 제출이나 오래된 상태가 있으면 신규 매수를 막고 확인된 축소 주문만 허용한다.
- 체결 동기화와 다중 시장 계좌 조정이 필수이며, 불명확한 조정을 성공으로 간주하지 않는다.
- K1 한도, K2 허용 종목, 킬스위치, 정확한 전략 지문, 추가 전용 감사 기록은 유지한다.

## 검증과 생산 증거

- 기능 PR: #684, merge `534b9cd`.
- 배포: `Deploy on merge to main` 실행 `32910288188`, 성공. 서버는 `dry-run`이다.
- 생산 공장: 실행 `32910288219`, 성공. 16/16, 752/752, 중첩 구간 12개,
  `NO_CROSS_INDEX_PREMIUM`을 발행했다.
- 머지 직전 전체 테스트: 3087 passed, 5 skipped.
- 린트: `uv run ruff check src tests` 통과.
- 엄격 하네스: 14/14 통과. HANDOFF 사실 검사와 PR 품질 관문 통과.
- 결정론: 같은 입력 두 번의 전체 결과 SHA-256
  `5b95166184a6ca4f61a8d5fb8f830f3f2d5de38feda5381061d748f1a50d9c0d` 일치.
- KIS: 최신 읽기 전용 점검은 변경 전 main `2141a7c`에서 5/5 통과했다. 이번 변경은
  브로커·주문 경로를 건드리지 않아 공개 사이드카에 같은 계좌 상태를 다시 발행하지 않았다.

## 다음 세션 판단

새 전략군을 더 만드는 것보다 `family-complete-v3` 소비자 증거 계약과 자금 구현 가능성 관문이
먼저다. 생산자가 엣지를 선언해도 소비자가 당시 시점 자료, 누적 752개 전체 탐색 벌점, 실제
주문 상품·비용 동등성, 중첩 또는 완전 미사용 검증, 과거 자료 재사용 금지를 독립 확인해야 한다.
동시에 현재 NAV·정수 주식·최소 주문·시세·종목 치환으로 목표 비중 오차를 계산하고 한도를 넘으면
단1 진입을 막아야 한다. 그 다음에 프로그램 전체 연구 예산을 도입하고 새 전략 탐색을 재개한다.
