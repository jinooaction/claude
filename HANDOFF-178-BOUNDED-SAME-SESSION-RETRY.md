# HANDOFF-178 — 자동 실주문 무접수의 같은 장 1회 복구

## 상태

스펙 176의 같은 장 자동 복구 경로는 PR #749 merge `f5084abb`로 main에 출시됐고
production에도 정확히 배포됐다. 배포 감사 run `33817631524`는 상관관계
`bf0681bfcb1fd0c03196a79d7910ac74`의 `DEPLOY_STARTED` → `DEPLOY_KERNEL_TOUCHED` →
`DEPLOY_COMPLETED`와 대상 `f5084abb`를 확인했다.

이 경로는 일반적인 재주문 기능이 아니다. 첫 자동 실행의 모든 주문이 중개사에서 명시적으로
거부돼 접수·부분체결·체결·열린 주문이 하나도 없고, 사후 측정과 계좌 대사가 모두 정상인
고정 사고만 같은 XNYS 정규장 안에서 root 소유 서버 예약이 한 번 복구할 수 있다.

## 왜 했나

2026-09-03 첫 자동 실행은 전략·자금성·예약·위험 관문을 모두 통과했지만 IAUM 1주와 SCHX
2주가 KIS에서 모두 거부됐다. 공식 KIS 공통 REST 헤더를 보정해도 기존 거래일 선점이 이미
소비돼, 안전한 코드 수정 뒤 남은 장중 자동 후보가 스스로 회복할 수 없었다.

원래 선점을 삭제하거나 수동 주문·서비스 시작을 허용하면 중복 주문 위험이 커진다. 그래서
원래 선점은 불변으로 유지하고, 엄격한 무접수 증거가 있는 사고에만 별도 추가 전용 복구 슬롯을
한 번 허용했다.

## 무엇을 만들었나

- 헌법 15.5.0에 같은 세션 자동 복구의 단일 예외와 실패 폐쇄 조건을 기록했다.
- root 소유 추가 전용 복구 장부 `order-session-retries.tsv`를 원래 거래일 선점 장부와 분리했다.
- 복구 슬롯은 중개사 호출 전에 먼저 소비된다. 복구 시도가 실패해도 같은 장에서 다시 쓰지 않는다.
- `deploy/live-canary-retry-incident.json`의 정확한 사고·첫 실행·배포된 보정 계보가 모두 일치해야 한다.
- 첫 실행은 완료 상태, 주문 제출 0건, 사후 측정·체결 동기화·계좌 대사 성공, 정합 `OK`,
  증거 `VALID`, 중지 없음이어야 한다.
- 계획한 모든 주문이 정확히 한 번씩 `REJECTED_BY_BROKER`여야 한다. 접수 불명, 접수, 부분체결,
  체결, 열린 주문, 일부 결과 누락은 모두 복구를 막는다.
- 현재 배포 코드의 새 KIS 읽기 전용 점검이 성공하고 미체결 주문 0건이어야 한다.
- GitHub 예약과 서버 예약은 최초·복구 실행 식별자를 공유하므로 늦은 출처가 주문·체결 동기화·
  측정·계좌 대사를 반복하지 않는다.

## 확인한 증거

- 안전 경계 커밋: `95a9ae1`, `20ee1c9` — 둘 다 `this changes the safety perimeter` 기록.
- 기능 PR #749 merge: `f5084abb9ef2cfbcf047bbb9bf62b144ba4549de`.
- 관련 시험 120개와 전체 시험 `3370 passed, 7 skipped`, 실패 0.
- 린트, `bash -n`, workflow YAML 파싱, 엄격 하네스 14/14, HANDOFF 사실 검사와 PR 품질 관문 통과.
- exact-main 배포 run `33817467201` 성공.
- 배포 감사 run `33817631524`: 대상 `f5084abb`, terminal `DEPLOY_COMPLETED`.
- exact-main KIS 점검 run `33817633554`: 6/6, 현금 `$934.27`, NAV `$1436.87`, ORANY
  28주, 미체결 0건.
- 주문 없는 production 사전점검 run `33817635538`: `ENTRY_READY`, 자본 `$142`, 계획 IAUM
  1주·SCHX 2주, fundability schema 1.1, 사후 측정 `CLEAR`, 계좌 대사 `OK`, 증거 `VALID`,
  중지 없음, 주문 제출 0건.

## 안전 경계

- 현재 2026-09-03 과거 사고 manifest는 `enabled=false`라 닫힌 세션을 다시 실행할 수 없다.
- 수동 주문, 수동 live service 시작, `workflow_dispatch`·`repository_dispatch` 실주문은 계속 금지다.
- 원래 거래일 선점은 삭제·수정하지 않는다. 복구 권한은 root 소유 서버 예약에만 있다.
- 자본은 NAV 10%의 운용 1단이고, 허용 종목·지정가·정규장·위험 한도·킬 스위치·감사·대사를
  모두 다시 통과한다.
- ORANY 28주는 관리 대상이 아니므로 자동 매도하지 않는다.
- 전략 `globalfixed-ensemble-3-6-9-12`는 `alpha_confirmed=false`, `max_rung=1`,
  `promotion_above_rung1=false`를 유지한다.

## 다음 세션 판단

다음 XNYS 정규장의 첫 자동 GitHub 예약 후보는 2026-09-04 14:17 UTC, 독립 서버 예약은
14:35 UTC다. 최초 유효 자동 실행에서 IAUM·SCHX 등 당일 계획 주문의 신규 접수, 실제 체결,
전략 추가 전용 감사와 같은 실행 계좌 대사를 확인한다. 다른 예약 출처는 동일 최초 실행 식별자를
반환하고 중복 중개사 쓰기 0건이어야 한다.

정상 첫 실행이 접수·체결되면 같은 장 복구는 발동하지 않는 것이 정상이다. 다시 모든 주문이
명시적 무접수로 끝날 때만 다음 자동 서버 예약에서 복구 슬롯 한 번이 소비되는지 확인한다.
지정가가 즉시 체결되지 않아도 가격을 추격하지 않는다. 이 생산 증거 전에는 스펙 176의 T100,
T106과 사용자 목표를 완료로 표시하거나 heartbeat를 비활성화하지 않는다.
