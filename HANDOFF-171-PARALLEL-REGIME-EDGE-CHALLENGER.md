# HANDOFF-171 - 독립 저회전 레짐 후보와 생산 관찰 호환성

## 상태

PR #692~#695가 `main`에 병합됐다. 주식·채권 동반 약세에서 현금 또는 금으로 피하는 16개
후보를 사전등록하고 즉시 검증했으며, 선택 후보는 통계·수익·비용 관문 대부분을 통과했다.
그러나 개선이 234개월 중 3개월에만 나타나 최종 판정은 `NO_RESEARCH_EDGE`다.

최종 주문 없는 생산 자동 판정에서 세 실행 ETF의 동등성과 144달러 주문 구현 가능성을
확인했다. 실행 준비는 참이지만 전략 증거가 부족해 자본은 계속 0이고 실제 주문도 0건이다.

## 운영자 의심에 대한 결론

“검토한 전략이 모두 쓸모없다”는 결론은 틀리다. 역사적 성과가 강하거나 연구 관찰 기준을
통과한 전략은 여러 개 있다. 다만 “역사적으로 유망함”과 “지금 실제 자본을 맡길 만큼
독립적으로 재현됨”은 다른 상태다.

이번 후보는 가장 까다로운 통계 기준 때문에 탈락하지 않았다. 가족 PBO, 홀드아웃 PSR,
비용 후 연복리, 샤프, 최대낙폭, 회전율, 최신 60개월은 모두 통과했다. 탈락 이유는 최근
세 구간 중 한 구간만 이겼고, 전체 개선이 2022-09·2022-12·2023-03 세 달에만 존재했기
때문이다. 이 후보를 합격시키려면 사전등록 기준을 결과를 본 뒤 낮춰야 하며, 그렇게 하면
엣지를 찾는 것이 아니라 과거 우연을 승인하게 된다.

반대로 기준 전체가 지나치게 엄격하다는 의심도 수치로 다시 확인했다. 16개 후보 가족의
무효 합격률은 1.0%, 연 샤프 0.6 신호 검출률은 84.0%였다. 64개 후보에서도 각각 0.4%,
80.4%였다. 즉 강한 양성 전략은 약 10번 중 8번 잡고, 무효 전략은 약 100번 중 99번
막는다. 현재 통계 관문이 합격 불가능한 구조라는 증거는 없다.

## 전략 결과

- 선택 후보: `regime-corr24-thr0p2-weak6-cash`.
- 개발: 1971-02~2006-12, 431개월.
- 격리: 2007-01, 1개월.
- 홀드아웃: 2007-02~2026-07, 234개월.
- 가족 PBO: `0.171429`.
- 홀드아웃 PSR: `0.998215`.
- 후보 연복리/기존 연복리: `8.695121%` / `8.548482%`.
- 후보 샤프/기존 샤프: `1.841788` / `1.802237`.
- 후보·기존 최대낙폭: 모두 `5.659028%`.
- 최근 구간 우위: 1/3, 사전등록 기준 2/3 미달.
- 월간 차이: 234개월 중 3개월, 최대 한 달 집중도 `64.1856%`.
- 최종: `NO_RESEARCH_EDGE`, 승격·자본·주문 모두 금지.

## 생산 관찰 결함과 수정

PR #692 배포 실행 `33087984994`은 미국 정규장 중이라 서버 패키지 교체를 다음 허용
시각으로 미뤘다. 이 안전 동작은 정상이다. 하지만 배포 과정에서 SSH 관찰 스크립트는
즉시 최신화돼, 이전 서버 명령줄에 아직 없는 `--token-cache`를 호출했다.

자동 판정 `33088105605`은 계좌 NAV `$1447.51`과 주문 구현 가능성까지 확인했지만,
동등성 관찰이 `No such option: --token-cache`로 실패해 빈 JSON을 남겼다. 판단기는 이를
통과로 오인하지 않고 `WAIT_EDGE`, 단 0, 자본·주문 0건으로 안전 중단했다.

PR #693은 임시 가격 DB를 `${REPO}/data/` 아래 만들고 종료 시 DB·WAL·SHM 파일을
지운다. 이전 명령줄은 DB 옆의 기존 `kis_token.json`을 자동 사용하고, 최신 명령줄도 같은
동작을 하므로 관찰 스크립트가 서버 패키지보다 한 버전 앞서도 실패하지 않는다.

생산 자동 판정 `33090550150`에서 GLD→IAUM, IEF→SPTI, SPY→SCHX 세 쌍이 모두
`passed=true`였고 `execution_proxy_parity_passed=true`, `entry_execution_ready=true`를
확인했다. 계좌 NAV는 `$1447.51`, 연구 단 1 예상 자본은 144달러였고 IAUM 1주와 SCHX
2주가 드라이런에서 구현 가능했다. 실제 주문은 0건이다.

같은 실행에서 현재 자본 문자열 `0`이 비어 있지 않다는 이유로 `live-canary-profit 0`이
호출돼 `capital must be positive`를 남겼다. 이 증거는 단 0 판정에 필요하지 않아 결과는
정상적으로 `WAIT_EDGE`, 단 0→0이었다. 후속 보정은 숫자 자본이 0보다 클 때만 이 읽기 전용
조회를 실행하고, 0·음수·빈값·비숫자는 건너뛴다. PR #694에서 이 조건을 병합했다.

#694 확인 실행 `33092282493`은 `capital must be positive`를 제거했고 동등성 3/3과
144달러 주문 구현성을 다시 통과했다. 그러나 건너뛴 성과 조회의 stderr 파일이 없어 마지막
출력 명령이 실패했고, 동등성 임시 DB의 `RETURN` 트랩이 바깥 `main` 종료 때 사라진
`parity_db` 지역변수를 다시 참조했다. 자본 결정 전에 멈춰 자본·주문 변경은 없었다.

PR #695는 건너뛴 성과 JSON·stderr를 결정론적으로 만들고, 임시 DB/WAL/SHM을 명시적으로
지운 뒤 `RETURN` 트랩을 해제했다. 최종 실행 `33093842870`은 세 오류 문구 없이 동등성
3/3, 주문 구현성, 자본 결정, 사이드카 발행까지 모두 성공했다.

## 다음 우선순위

1. 기존 추세 전략에 드문 조건을 하나 더 붙이는 후보는 멈춘다. 이번 후보처럼 개선 월이
   3개뿐이면 장기 수치가 좋아도 독립 엣지가 아니다.
2. 다음 신규 가족은 개발 단계부터 서로 다른 최소 3개 시간 구간에서 실제 수익 차이가
   발생하도록 활동 빈도·효과 집중도를 사전등록한다.
3. 역사적으로 강한 `globalfixed-ensemble-3-6-9-12`는 현재 자본을 열지 않고 자동 전진
   관찰을 계속한다. 기다림을 합격 근거로 쓰지 않고 새 독립 가족은 즉시 백테스트한다.

## 검증

- #692 전체 테스트: 3131 passed, 6 skipped.
- #692 린트: `uv run ruff check src tests` 통과.
- #692 엄격 하네스 14/14, HANDOFF 사실 검사, JSON·셸 구문, PR 품질 관문 통과.
- #693 전체 테스트: 3131 passed, 6 skipped.
- #693 린트, 셸·JSON 구문, 엄격 하네스 14/14, HANDOFF 사실 검사 통과.
- #693 생산 자동 판정 `33090550150` 성공, 동등성 3/3·실행 준비·주문 구현성 통과.
- 단 0의 불필요한 라이브 성과 조회 보정은 전체 3132 passed, 6 skipped와 입력값 표를 통과했다.
- 확인 실행 `33092282493`은 자본 결정 전에 실패했고, 새 수명주기 보정은 전체 3132 passed, 6 skipped와 셸·린트를 통과했다.
- PR #695 merge `32807f4`, 배포 `33093767500`에서 helper 갱신과 장중 코드 배포 연기를 확인했다.
- 최종 자동 판정 `33093842870` 성공. 정확 오류 검색 0건, 동등성 3/3, 실행 준비 참,
  `WAIT_EDGE`, 단 0→0, 자본 이동·실주문 0건이다.

## 안전 경계

연구 자본 10%, 승격 20%, 손실 예산 20%, 정규장, 킬스위치, 주문·노출 한도,
`Backtest -> Canary -> Full` 순서는 바꾸지 않았다. 장중 배포 제한도 우회하지 않는다.
이번 변경은 실제 주문을 만들지 않으며, 빈 동등성 증거가 자본을 열지 못하는 안전 실패를
그대로 유지한다.
