# KPI threshold table — defaults per
# `specs/002-token-telemetry/contracts/kpi-thresholds.md`.
# Operators may edit values; KPI names and `direction` are fixed.

[cache_hit_rate]
direction = "higher_is_better"
tier_c = 0.40
tier_b = 0.70
tier_a = 0.90

[tokens_per_decision_p95]
direction = "lower_is_better"
tier_c = 8000
tier_b = 3000
tier_a = 1500

[usd_per_decision_mean]
direction = "lower_is_better"
tier_c = 0.05
tier_b = 0.01
tier_a = 0.003

[latency_p95_ms]
direction = "lower_is_better"
tier_c = 5000
tier_b = 2000
tier_a = 800
