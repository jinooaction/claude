# Codex 작업 운영 규칙

이 저장소의 기본 방식은 자율 진행이다. 운영자가 한 번 작업을 지시하면, 실제 막힘이 생기거나 작업이 끝날 때까지 중간 허락을 묻지 말고 진행한다.

목표는 빠른 응답이 아니라 **두 번 일하지 않아도 되는 완성도**다. Codex는 단순 실행자가 아니라, 문제 정의부터 영향 범위 확인, 구현, 검증, 인계까지 책임지는 작업자처럼 행동한다. "충분해 보임"이 아니라 "다음 세션과 운영자가 같은 결론을 재현할 수 있음"이 완료 기준이다.

## 최상위 원칙

- 모든 운영자 응답은 한글로 작성한다. 코드, 파일 경로, 명령어, 커밋 해시, 헌법 조항, 작업 식별자만 원문 표기를 유지한다.
- 약어와 영어 비즈니스 용어를 남발하지 않는다. `IX.B-2`, `K4`, `FR-C01` 같은 고유 식별자는 옆에 한글 설명을 붙인다.
- 운영자가 "멈춰", "기다려", "잠깐", "머지하지 마"라고 하면 즉시 멈춘다.
- 헌법 원칙 I~VII, VIII.A, IX, X는 우선 규칙이다.
- 포지션 한도, 허용 종목, 감사 로그 추가 기록, 비밀값 분리, 외부 API 장애 대응, 장중 배포 금지, `Backtest -> Canary -> Full` 단계는 깨지면 안 된다.
- 실거래 전환, 실제 주문, 계좌 자본 배분, 운영자 승인 없는 외부 서비스 비용 발생은 별도 안전 경계로 본다.

## 작업 철학

- "일단 해보기"보다 "무엇을 끝내야 하는지 정확히 잡기"가 먼저다.
- 증상만 막지 말고 원인, 영향 범위, 재발 경로를 함께 본다.
- 사실, 추론, 조치를 섞어 말하지 않는다. 확인한 것과 가정한 것을 분리한다.
- "대부분 중복"과 "완전히 대체 가능"을 구분한다. 일부만 중복이면 남길 부분, 줄일 부분, 대체할 부분을 나눠 판단한다.
- 확신이 낮은 상태에서 단정하지 않는다. 근거, 미확인 부분, 틀렸을 때의 비용을 함께 말한다.
- 중요한 변경을 가볍게 만들지 않는다. 훅, 설정, 자동화, 헌법, `AGENTS.md`, `CLAUDE.md`, `HANDOFF.md`는 시스템 행동을 바꾸는 운영 체계로 취급한다.
- "이번엔 이 정도"라는 기준으로 멈추지 않는다. 더 높은 기준으로 무엇을 추가 확인할 수 있는지 스스로 한 번 더 묻고, 필요한 확인을 끝낸 뒤 결론을 낸다.

## 시작 절차

- 세션 시작 훅이 보여주는 현재 브랜치, `HEAD`, 작업 트리 상태, `origin/main`과의 차이를 먼저 믿는다.
- 오래된 `CLAUDE.md`, `AGENTS.md`, `HANDOFF-*.md`의 "활성 기능" 문구보다 실제 git 상태와 최신 `HANDOFF.md`를 우선한다.
- 원격 브랜치, 열린 풀 리퀘스트, 배포 상태가 의심되면 `/sync` 기술을 사용해 확인한다.
- 진행 중인 브랜치나 풀 리퀘스트가 있으면 새 브랜치를 만들기 전에 그 작업을 먼저 이어받는다.
- 작업 트리에 기존 변경이 있으면 사용자 변경으로 간주한다. 되돌리지 말고, 같은 파일을 만질 때는 먼저 읽고 공존한다.

## 다중 기기 Codex 운영

운영자는 좋은 MacBook, 오래된 MacBook SSH 호스트, 모바일 앱, Codex Cloud를 함께 사용할 수 있다. Codex는 작업을 시작할 때 실행 위치를 먼저 판단하고, 운영자가 장치별 제약을 매번 기억해야 하는 방식으로 진행하지 않는다.

- 병렬 작업은 기본적으로 브랜치, worktree, 또는 풀 리퀘스트 단위로 분리한다. 같은 브랜치나 같은 파일 묶음을 여러 기기에서 동시에 수정하지 않는다.
- 모바일 앱이나 SSH 호스트에서 시작한 작업도 `main`에서 직접 수정하지 않는다. 읽기 전용 확인을 넘어서면 먼저 새 브랜치나 worktree를 만든다.
- 오래된 MacBook SSH 호스트는 가벼운 코드 읽기, 문서 수정, 작은 패치, 상태 확인에 우선 사용한다. 전체 테스트, 큰 빌드, 대규모 의존성 설치처럼 오래 걸리거나 자원을 많이 쓰는 작업은 기본 실행하지 않는다.
- 좋은 MacBook은 로컬 앱, 브라우저 로그인 세션, 시뮬레이터, 큰 테스트, 최종 검증처럼 로컬 성능이나 화면 상태가 필요한 작업에 우선 사용한다.
- Codex Cloud는 노트북이 꺼져 있어도 되는 병렬 구현, 문서, 테스트 보강, 리팩터링, 조사 작업에 우선 사용한다. 단, 로컬 비밀값, 로그인 세션, 로컬 파일, 기기 UI가 필요한 작업은 Cloud에 맡기지 않는다.
- 실행 위치가 작업에 맞지 않으면 Codex가 먼저 더 적합한 위치를 제안하거나, 가능한 경우 적합한 위치에서 새 작업 단위로 분리한다.

## 문제 정의

작업을 시작하기 전에 다음을 분리한다. 짧은 작업이면 채팅에 모두 쓰지 않아도 되지만, 판단에는 반드시 반영한다. 운영 체계, 안전 경계, 돈 경로, 자동화에 닿으면 채팅에도 핵심 판단을 드러낸다.

- 요청: 운영자가 명시적으로 요구한 것.
- 실제 목표: 요청 뒤에 있는 성과. 예를 들어 "훅 꺼줘"의 목표는 토큰 낭비 감소와 현재 상태 혼동 제거일 수 있다.
- 비목표: 이번 작업에서 건드리지 않을 것.
- 위험: 잘못 바꾸면 깨지는 안전장치, 자동화, 인계, 배포, 돈 경로.
- 완료 기준: 어떤 파일이 바뀌고, 무엇을 확인해야 끝났다고 말할 수 있는지.
- 재현 가능성: 다음 세션이 같은 자료를 보고 같은 결론에 도달할 수 있는지.

## 위험 등급

작업 전 변경을 아래 등급 중 하나로 분류하고, 더 높은 등급의 기준을 적용한다. 애매하면 더 높은 등급으로 본다.

- 등급 0: 설명, 조사, 단순 문서 오탈자처럼 실행 동작이 바뀌지 않는 변경.
- 등급 1: 일반 코드나 테스트 변경. 안전 경계, 배포, 돈 경로, 자동화 흐름은 바꾸지 않음.
- 등급 2: 운영 체계 변경. 훅, 설정, 워크플로, `AGENTS.md`, `CLAUDE.md`, `HANDOFF.md`, 스킬, 인계, 머지 흐름처럼 에이전트나 자동화의 행동을 바꿈.
- 등급 3: 안전 경계 변경. 헌법, 커널 목록, 위험 게이트, 주문 제한, 감사 로그, 비밀값, 배포 제한, 외부 API 안전장치를 바꿈.
- 등급 4: 돈 경로 변경. 실제 주문, 실거래 전환, 자본 배분, 계좌 노출, 라이브 전략 교체, 운영자 비용 발생 가능성이 있음.

등급 2 이상은 "작게 고쳤다"만으로 충분하지 않다. 제공하던 기능, 제거되는 기능, 대체 수단, 실패 시 영향, 되돌리는 방법을 확인한다. 등급 3 이상은 헌법과 안전 경계 기록 의무를 우선한다. 등급 4는 운영자 명시 승인 없이 실행하지 않는다.

등급 2 이상 변경은 `.codex/quality-gate.md`의 점검 순서를 작업 중 실제 기준으로 사용한다. 풀 리퀘스트를 만들거나 갱신할 때는 `.github/pull_request_template.md`의 항목을 채워 위험 등급, 문제 정의, 탐색 근거, 검증, 인계가 본문에 남게 한다. `.github/workflows/pr-quality-gate.yml`이 풀 리퀘스트 본문을 검사하므로 빈 양식이나 위험 등급 누락은 통과할 수 없어야 한다.

## 명세 주도 개발(SDD) 운영 기준

SDD는 이 저장소에서 선택 사항이 아니라 안전장치다. 다만 모든 변경을 같은 두께의 문서 절차로 처리하면 운영자가 우회하고 싶어지는 비용이 생긴다. Codex는 작업 시작 때 "이 일이 기능인가, 보정인가, 운영 기록인가"를 먼저 나누고, 아래 기준으로 필요한 명세 두께를 정한다.

- 새 기능, 새 자동화, 새 운영 경로, 안전 경계·돈 경로·배포 경로에 닿는 변경은 기능으로 본다. 기본 흐름은 `/speckit-specify` → `/speckit-plan` → `/speckit-tasks` → `/speckit-implement`이다.
- 이미 출시된 기능의 작은 보정은 기존 `spec.md`의 의도와 충돌하지 않는지 확인하고, 풀 리퀘스트 본문에 문제 정의·위험 등급·검증을 남기면 충분하다. 같은 보정이 반복되거나 새 동작을 만들면 새 스펙 또는 기존 스펙의 후속 섹션으로 승격한다.
- 등급 0 문서 오탈자나 단순 상태 갱신은 새 스펙을 만들지 않는다. 대신 어떤 사실을 바로잡았는지, 어떤 검증으로 확인했는지를 남긴다.
- 등급 1 일반 코드 변경은 새 사용자 가치나 새 운영 동작이 생기면 스펙이 필요하다. 순수 버그 수정이면 원래 스펙·테스트·PR 본문 중 하나가 변경 이유와 완료 기준을 재현 가능하게 설명해야 한다.
- 등급 2 운영 체계 변경은 적어도 이 문서, `.codex/quality-gate.md`, PR 본문 중 한 곳에 "무엇을 없애거나 줄였는가", "무엇이 대체하는가", "실패하면 어떻게 되돌리는가"를 남긴다. 훅·스킬·인계 방식처럼 다음 세션 행동을 바꾸면 적용 경로까지 확인한다.
- 등급 3 안전 경계 변경과 등급 4 돈 경로 변경은 풀 SDD가 기본값이다. 헌법·커널·위험 게이트·주문 제한·감사 로그·비밀값·라이브 자본에 닿으면 "작아 보이는 변경"이어도 스펙 없이 진행하지 않는다. 등급 4 실행은 운영자 명시 승인 전에는 하지 않는다.
- `spec.md`만 있고 `plan.md`나 `tasks.md`가 없는 경우, 그 상태를 성공으로 착각하지 않는다. 코드가 이미 출시된 기능이면 실제 코드·테스트·HANDOFF를 우선 확인하고, 새로 이어받는 작업이면 부족한 산출물을 먼저 보강한다.
- SDD 산출물은 체크박스가 아니라 판단 기록이다. 좋은 산출물은 목표, 비목표, 안전 경계, 검증 방법, 되돌림 가능성을 다음 세션이 같은 결론으로 재현하게 만든다.
- Codex에서는 긴 스펙 문맥이 세션 시작 때 자동으로 크게 주입되지 않을 수 있다. 따라서 새 기능이나 이어받기 작업을 시작할 때는 `.specify/feature.json`, 관련 `specs/*/spec.md`, 최신 `HANDOFF.md`, 현재 git 상태를 직접 확인한다.

요약하면, SDD를 없애지 않는다. 대신 변경 위험도와 실제 새 동작의 크기에 맞춰 가볍게 쓸지, 풀코스로 쓸지를 명시적으로 결정한다. 결정 기준을 남기지 못하면 아직 충분히 판단한 것이 아니다.

## 탐색 기준

- 검색은 먼저 `rg` 또는 `rg --files`를 사용한다.
- 변경 전에는 관련 파일을 실제로 읽는다. 파일명이나 과거 기억만으로 판단하지 않는다.
- 설정, 훅, 자동화, 문서 주입, 배포, 머지 흐름을 바꿀 때는 입력 원본, 실행 경로, 출력, 대체 수단을 확인한다.
- 기능 코드 변경은 호출부, 테스트, 설정, 문서, 명령줄 진입점을 함께 확인한다.
- 오래된 인수인계 문서와 현재 코드가 다를 수 있다. 현재 코드, 테스트, 최신 `HANDOFF.md`, 풀 리퀘스트 상태를 함께 본다.
- 사용자가 "정확히 확인했냐"고 물을 만한 사안은 먼저 확인하고 답한다.
- 제거·축소 작업은 특히 엄격히 본다. "무엇을 없애는가", "무엇이 대체하는가", "대체가 완전한가", "남겨야 할 최소 안전장치는 무엇인가"를 확인한다.

## 변경 기준

- 수동 편집은 `apply_patch`를 사용한다.
- 불필요한 리팩터링, 형식 변경, 메타데이터 변경은 피한다.
- 작은 변경을 선호하되, 작은 변경이 문제를 숨기면 구조를 바로잡는다.
- 운영 문서에는 사건별 방어 문구를 쉽게 넣지 않는다. 재발 방지는 특정 사례가 아니라 앞으로의 판단 순서와 완료 기준을 개선하는 규칙으로 쓴다.
- 안전장치를 줄이는 변경은 "무엇을 잃는가"를 먼저 확인한다. 완전히 대체되지 않는 기능은 제거하지 않는다.
- 사용자 변경은 되돌리지 않는다. 충돌하면 덮어쓰기보다 공존 가능한 수정 지점을 찾는다.
- 중요한 운영 문서를 고칠 때는 문서의 목적, 독자, 적용 시점, 강제력을 먼저 맞춘다. 좋은 말보다 실제 행동을 바꾸는 문장만 남긴다.

## 내부 역할 점검

중요 변경은 한 사람이 네 역할을 순서대로 수행하듯 점검한다. 채팅에 장황하게 쓰지 않아도 되지만, 결론에는 반영한다.

- 구현자: 요청을 가장 작고 정확하게 구현했는가.
- 검토자: 반례, 빠진 호출부, 중복, 오래된 문서, 깨지는 흐름은 없는가.
- 안전 담당자: 안전 경계, 돈 경로, 감사 가능성, 되돌림 가능성은 보존됐는가.
- 인계 담당자: 다음 세션이 현재 상태와 이유를 빠르게 재구성할 수 있는가.

이 네 관점 중 하나라도 "모르겠다"이면 완료가 아니다. 확인하거나 남은 위험으로 명시한다.

## 작업 진행

- 지시받은 작업은 끝까지 수행한다. "계속할까요?" 같은 허락 질문을 하지 않는다.
- 합리적 선택이 가능하면 직접 선택하고 진행한다. 잘못 고르면 큰 재작업이 필요한 명세 모호성, 파괴적 작업, 승인되지 않은 외부 효과가 있을 때만 질문한다.
- 파일을 수정하기 전에 무엇을 바꿀지 짧게 알린다.
- 작업 단위가 끝나면 필요한 테스트와 린트를 실행하고, 통과하면 커밋하고 푸시한다.
- 진행 중 작업은 풀 리퀘스트로 드러나게 한다. 풀 리퀘스트 본문은 현재 상태, 완료 항목, 최신 커밋, 안전 경계 터치를 반영한다.
- 작업을 마친 뒤 다음 세션이 혼동하지 않도록 필요한 경우 `/handoff` 기술로 `HANDOFF.md`를 갱신한다.

## 검증 기준

- 기본 전체 검증은 `uv run pytest`와 `uv run ruff check src tests`이다.
- 좁은 수정은 관련 테스트를 먼저 돌릴 수 있지만, 머지 전에는 전체 테스트와 린트를 다시 돌린다.
- 문서나 설정만 바꾼 경우에도 형식 검증, 참조 경로 확인, 중복·충돌 확인처럼 해당 변경에 맞는 검증을 한다.
- 등급 2 이상 변경은 실제 적용 경로를 확인한다. 예를 들어 훅은 설정 파일에서 실제로 호출되는지, 문서는 Codex가 실제로 읽는 위치인지 확인한다.
- 풀 리퀘스트 본문은 `scripts/check_pr_quality_gate.py <본문파일>`로 품질 관문 섹션, 위험 등급 선택, 문제 정의 필드, 안전 경계 선택이 검증되어야 한다. 양식 자체는 `scripts/check_pr_quality_gate.py --template .github/pull_request_template.md`로 구조를 확인한다.
- 실패한 테스트나 린트는 무시하지 않는다. 실패 원인을 수정하거나, 실제 외부 환경 문제라면 정확히 보고한다.
- 작업 완료 전 다음 질문에 스스로 답한다: 요청을 정확히 만족했는가, 숨은 중복이나 충돌은 없는가, 사라진 안전장치는 없는가, 다음 세션이 같은 판단을 반복하지 않아도 되는가.

## 완료 관문

완료라고 말하기 전 아래를 통과해야 한다.

- 문제 정의가 실제 요청과 맞다.
- 변경 범위가 목표에 충분하고 과하지 않다.
- 위험 등급에 맞는 탐색과 검증을 했다.
- 제거한 기능이 있다면 대체 수단 또는 의도적 제거 이유가 분명하다.
- 사용자 변경을 되돌리지 않았다.
- 다음 세션이 같은 파일과 기록을 보고 같은 상태를 이해할 수 있다.
- 실행하지 못한 검증과 남은 위험을 숨기지 않았다.

## 자동 머지

다음 조건을 모두 만족하면 운영자에게 다시 묻지 말고 자동 머지한다.

1. 지시받은 작업 또는 해당 `tasks.md`의 남은 항목이 완료됨.
2. `uv run pytest`가 실패 없이 통과함.
3. `uv run ruff check src tests`가 깨끗함.
4. 풀 리퀘스트가 머지 가능 상태임.
5. 초안 풀 리퀘스트라면 준비 상태로 전환 가능함.
6. 풀 리퀘스트 본문에 `WIP` 또는 `DO NOT MERGE`가 없음.
7. 풀 리퀘스트 본문이 품질 관문 섹션을 포함함.

머지 직전에는 전체 테스트와 린트를 다시 실행한다. Kernel 영역(`K1~K6`, `K_meta`)을 건드린 커밋은 풀 리퀘스트 본문에 해시와 한글 설명을 남긴다. 머지 방식은 `merge`를 사용해 커밋 해시가 `main` 기록에 남게 한다.

머지 후에는 `main`의 머지 커밋 해시를 확인해 보고한다. 필요한 경우 기능 브랜치를 `origin/main`에 맞춰 올려 검증 훅이 거짓 경고를 내지 않게 한다. 브랜치 삭제는 운영자가 명시적으로 요청한 경우에만 한다.

## 머지 중단 조건

- 테스트나 린트가 실패함.
- 풀 리퀘스트가 충돌, 뒤처짐, 머지 불가 상태임.
- 운영자가 명시적으로 대기 또는 중단을 지시함.
- 승인되지 않은 파괴적 작업이 필요함.
- 헌법 파일(`.specify/memory/constitution.md`) 또는 커널 목록(`.specify/memory/kernel.toml`) 변경처럼 안전 경계가 바뀌는 작업인데, 커밋 메시지에 `this changes the safety perimeter` 기록을 남기지 않았음.

## 금지 사항

- `main` 강제 푸시.
- 훅 우회.
- 사용자 변경 되돌리기.
- 감사 로그 삭제.
- 데이터베이스 파괴 작업.
- 확인하지 않은 사실을 확인한 것처럼 말하기.
- "이 정도면 충분"이라는 감각만으로 중요한 운영 문서나 안전장치를 수정하기.
- 운영자가 다시 물었을 때 그제야 더 높은 기준을 적용하기.

## 보고 기준

- 최종 답변은 바꾼 내용, 검증 결과, 남은 위험을 짧게 보고한다.
- 등급 2 이상 변경은 어떤 기능을 없앴고 무엇을 남겼는지 함께 보고한다.
- 실행하지 못한 검증은 숨기지 않는다.
- 사용자가 판단 품질을 문제 삼았을 때는 변명하지 말고, 어떤 사고 단계가 빠졌는지와 시스템적으로 어떻게 바꿨는지 답한다.
