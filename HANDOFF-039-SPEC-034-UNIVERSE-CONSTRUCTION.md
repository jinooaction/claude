# HANDOFF 039 — 스펙 034: 체계적 유니버스 구성 + 넓은 횡단면 정직한 검증 (2026-06-03)

main 머지 `7425151`(PR #159). Kernel 터치 0건. 운영자 지시 "세계 최고 수준이 되기 위한
작업 분석·우선순위 판단 뒤 자율 수행"에 대한 응답.

## 한 줄

유니버스를 손으로 고른 15개 메가캡에서 **유동성으로 체계적으로 구성**하는 역량을 추가하고,
~15개 마일스톤 동안 열려 있던 "넓은 유니버스가 다음 레버" 가설을 처음으로 당겨 정직하게
닫았다(폭 8배 → 그래도 옛 강세장에선 단순 보유를 못 이김).

## 분석 → 우선순위 (왜 유니버스였나)

시스템은 이미 기관급 알파·검증 도구를 다 갖췄다(33스펙, 테스트 1414→1427). 반복된 정직한
결론은 "디플레이티드 샤프 보정 후 단순 보유를 이기는 강건한 엣지 없음". 핵심 통찰: **알파
스택 전체가 넓은 횡단면을 전제로 설계됐는데 검증은 늘 상관 높은 15개 메가캡으로만 돌았다.**
15종목에서 "횡단면 모멘텀"은 사실상 기술주 베타 동전던지기 — 진짜 팩터가 표현될 폭 자체가
없다. 게다가 데이터셋(약 500종목 실데이터)은 손 닿는 곳에 있는데 스크립트가 15개로 잘라놨다.

우선순위 판단: #1 넓은 횡단면 유니버스(수익 레버 ★★★, 컨테이너 검증 가능, 비커널) > #2 섹터
중립화(섹터 데이터 부재, 15종목엔 중립화할 섹터도 없음 — #1 다음에야 의미) > #3 회전율
페널티 > #4 밸류·사이즈(펀더멘털 차단).

## 무엇을 했나

- **`src/auto_invest/strategy/universe.py`**(신규, 비커널, 선택 전용):
  - `median_dollar_volume(bars, lookback_bars)` — 중앙값 달러 거래대금(close×volume).
    중앙값(평균 아님)이라 한 번의 비정상 거래량이 thin 종목을 끌어올리지 못함.
  - `liquidity_rank(symbol_bars, lookback_bars)` — 유동성 내림차순, 동점 사전순, 데이터부족
    sentinel 맨끝.
  - `select_universe(symbol_bars, top_n, min_dollar_volume, min_history_bars, lookback_bars)`
    — 최소 이력·유동성 통과분을 유동성 상위 top_n 으로 구성, 정렬 심볼 리스트 반환.
  - 결정론 Decimal. 단위 테스트 13건(`tests/unit/test_spec_034_universe.py`).
- **CLI `auto-invest build-universe`**(`cli.py`, 읽기 전용) — 적재 데이터셋(`CSVDataSource`)을
  읽어 OHLCVBar→PriceBar 변환 후 유동성 상위 유니버스 구성. `--top-n/--min-history-bars/
  --min-dollar-volume/--lookback-bars`, 출력 text/`--json`/`--emit-toml`.
- **`scripts/fetch_sp500_subset.py --all`** — 데이터셋 전체 횡단면(약 505종목) 추출.
- **`specs/034-universe-construction/`** — spec/plan/tasks/FINDINGS + `sp500-broad-portfolio.toml`.

## 검증 결과 (표본 외 워크포워드 + DSR, `FINDINGS.md`)

데이터: 공개 GitHub raw `plotly/datasets/all_stocks_5yr.csv`(S&P500 5년 실 일봉,
2013-2018, 505종목). 유동성 상위 119종목(점 포함 `BRK.B`는 화이트리스트 규칙상 제외).
4개 365일 표본 외 구간.

| 설정 | 가중 | 보유 | 구간 승 | 평균 샤프(전략 vs 보유) | DSR | 판정 |
|---|---|---|---|---|---|---|
| A | equal | top-20 | 0/4 | 0.49 vs 1.41 | 0.81(1시도) | 엣지 없음 |
| B | min_variance | top-40 | 1/4 | 0.49 vs 1.41 | 0.057(3시도) | 엣지 없음 |

폭을 8배로 넓혀도 단순 보유(2013-2018 대형주 균등가중, 샤프 1.41·낮은 낙폭)를 표본 외로
못 이긴다. 디플레이티드 샤프가 우연 승리(B의 1/4)를 0.057로 정확히 깎아낸다.

## 왜 음수인데도 진전인가

1. "넓은 유니버스가 다음 레버"라는 ~15개 마일스톤의 변명을 닫았다. 폭은 충분했다.
2. 병목은 도구가 아니다 — 검증 장치가 스스로를 속이기를 거부한다(헌법 X = 돈 잃지 않기).
3. 진짜 레버가 분명해진다: **현재 데이터 forward 페이퍼 트랙**(스펙 032/033) + 컨테이너가
   못 닿는 데이터(펀더멘털·인트라데이·대체) + 다국면 regime(폭락 포함).

## 다음 세션이 이어받을 것

- **forward 페이퍼 트랙 누적 관찰**: `git show origin/automation/rebalance-paper-forward-last-run:LAST_RUN.md`.
  페이퍼 체결이 쌓이면 디플레이티드 샤프로 "지금 통하는가" 판정(스펙 027 재사용).
- **유니버스 구성 역량 활용**: `build-universe` 로 forward 페이퍼/캐너리 유니버스를 손이
  아닌 유동성으로 구성(현재 `canary-portfolio.toml` 은 손으로 고른 10종목).
- 새 데이터 출처(펀더멘털·인트라데이·다국면)는 라이브 인스턴스/외부 의존이라 운영자 결정.

## 안전 경계

Kernel 터치 0건(`risk/gates.py`·caps·whitelist·worker·audit 무변경). 돈 안 움직임
(백테스트·CLI·스크립트 전용, 라이브 워커 룰 루프 byte 무변경). 구성된 유니버스는 실주문 전
화이트리스트와 교집합(헌법 II)되어 라이브 거래 집합을 못 넓힘. 결정론·LLM 미사용. 데이터
아티팩트(`data/`)는 깃 무시 — 스크립트로 재현. 전체 1427 통과·4 스킵, 린트 깨끗.
