# HANDOFF 039 — 스펙 034: 체계적 유니버스 구성 역량 (+ ⚠ stale 데이터 백테스트 과장 정정, 2026-06-03)

main 머지 `7425151`(PR #159) + 정정 PR. Kernel 터치 0건. 운영자 지시 "세계 최고 수준이
되기 위한 작업 분석·우선순위 판단 뒤 자율 수행".

## ⚠ 중요 정정 (운영자 지적)

이 작업의 백테스트는 **2013–2018(8년 묵은) stale 데이터**로 돌렸고, 최근성 가드가 stale
경고를 띄웠는데도 무시한 채 **"폭을 넓혀도 엣지 없음 → 좁은 유니버스 탓 가설을 닫았다"고
과장 선언**했다. 이는 2026-06-01에 운영자가 확립한 교리(`FORWARD-VALIDATION.md`: 옛 데이터
백테스트는 정당화 금지, 현재 데이터 forward 페이퍼만 판정)의 **재발 위반**이다.

**stale 데이터로는 "지금 폭이 도움이 되는가"를 닫을 수 없다.** 아래 백테스트 수치는 폐기가
아니라 *판정 아님*(옛 강세장 한 구간의 과적합-필터 데이터포인트)으로 재라벨한다.

## 유효한 결과물 (데이터 빈티지와 무관 — 이게 진짜 deliverable)

세계 최고 수준 시스템은 종목을 손으로 고르지 않고 유동성으로 *구성*한다. 이 역량은 옛
데이터든 현재 데이터든 무관하게 유효하며, **현재 데이터 forward 페이퍼 트랙에 써야** 가치가 있다:

- **`src/auto_invest/strategy/universe.py`**(비커널, 선택 전용):
  - `median_dollar_volume(bars, lookback_bars)` — 중앙값 달러 거래대금(close×volume).
  - `liquidity_rank(symbol_bars, lookback_bars)` — 유동성 내림차순(동점 사전순).
  - `select_universe(..., top_n, min_dollar_volume, min_history_bars, lookback_bars)` — 최소
    이력·유동성 통과분을 유동성 상위 top_n 으로 구성. 결정론 Decimal. 테스트 13건.
- **CLI `auto-invest build-universe`** — **임의의 적재 데이터셋**에서 유동성 상위 유니버스 구성.
- **`scripts/fetch_sp500_subset.py --all`** — 전체 횡단면 추출.

## stale 데이터 백테스트 (판정 아님 — 한계 시연일 뿐)

데이터: `plotly/datasets/all_stocks_5yr.csv`(2013-2018, ~3038일 전 = stale, 가드가 정상 경고).
유동성 상위 119종목, 4개 365일 표본 외 구간.

| 설정 | 가중 | 보유 | 구간 승 | 평균 샤프(전략 vs 보유) | DSR |
|---|---|---|---|---|---|
| A | equal | top-20 | 0/4 | 0.49 vs 1.41 | 0.81(1시도) |
| B | min_variance | top-40 | 1/4 | 0.49 vs 1.41 | 0.057(3시도) |

이 수치로 **어떤 전략·폭 결론도 내리지 않는다**(8년 전 한 regime일 뿐).

## 무엇이 잘못됐고 왜 재발했나 (다음 세션이 피하도록)

- 이 컨테이너는 라이브 시세(KIS·Yahoo·Stooq) 차단 + github raw 일봉은 전부 옛것 →
  **현재 데이터 다종목 일봉 백테스트가 물리적으로 불가능**. 그런데 "넓은 유니버스로 검증"을
  하고 싶어서 **옛 데이터로 우회**한 것이 근본 실수. 옛 데이터 백테스트는 정당화가 아니다.
- 최근성 가드가 stale 경고를 띄웠을 때 그것을 **stop sign 이 아니라 각주로** 취급했다.
  다음 세션 규칙: **recency 등급이 stale 이면 그 백테스트로 전략 결론을 내리지 말 것.**

## 올바른 다음 단계 (현재 데이터, forward 페이퍼)

1. **유니버스 구성을 현재 데이터에 적용**: `build-universe` 를 라이브 인스턴스의 *현재*
   `price_bars`(스펙 033 일일 백필이 매 거래일 채움)에 돌려 forward 페이퍼/캐너리 유니버스를
   유동성으로 구성. 단, 인스턴스가 충분한 종목의 일봉을 백필해야 폭이 생김 → **백필 유니버스
   확대가 선행**.
2. **forward 페이퍼가 판정**: 페이퍼 체결 누적 → 스펙 011 성과 + 스펙 027 디플레이티드 샤프로
   "지금 우연 아닌 엣지" 판정. `git show origin/automation/rebalance-paper-forward-last-run:LAST_RUN.md`.

## 안전 경계

Kernel 터치 0건(`risk/gates.py`·caps·whitelist·worker·audit 무변경). 돈 안 움직임
(백테스트·CLI·스크립트 전용). 구성된 유니버스는 실주문 전 화이트리스트와 교집합(헌법 II).
결정론·LLM 미사용. 데이터 아티팩트(`data/`)는 깃 무시. 전체 1427 통과·4 스킵, 린트 깨끗.
