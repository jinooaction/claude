# HANDOFF-158 - 독립 국채 캐리 전략 공장과 576회 검정

## 상태

PR #659와 보고 정합 보정 #660이 `main`에 병합됐다. 배포, 공식 공개자료 수집,
KIS 실연결 점검, 생산 전략 공장이 모두 성공했다. 생산 판정은
`NO_FACTORY_EDGE`이므로 권위 상태는 `PREVIEW_ONLY`, `armed:false`, 단 0/5,
자본 0이며 실주문은 불가능하다.

## 무엇을 만들었나

- FRED DGS3MO·DGS2·DGS5·DGS10·DGS30을 시점 기준 월말 곡선으로 만들고,
  30년물 역사 공백을 미래 값으로 채우지 않는다.
- 3개월물 SGOV, 2년물 SHY, 5년물 IEI, 10년물 IEF, 30년물 TLT에 대응하는
  캐리·금리 추세·방어적 곡선·바벨 전략을 각각 16개씩 총 64개 생성한다.
- 보수적 롤링 액면가·듀레이션 수익 근사와 25bp·50bp 비용을 적용한다.
- 기존 가격 256회, 거시 사전 192회, 거시 공식 64회와 합쳐 DSR과 PBO의 총
  검정 횟수를 576회로 고정하며 지문도 576개가 모두 고유해야 한다.
- 동일 만기 국채 사다리와 기존 3자산 전략을 함께 비교하고, 신규 전략을 20%
  섞었을 때 샤프 개선·낙폭 비악화·낮은 상관이 모두 성립해야 한다.
- 연구와 주문 준비가 같은 `treasury_target_weights` 함수를 사용한다. 자료·코드·
  후보·전략 지문·목표 비중 다이제스트가 다르면 브로커 호출 전에 차단한다.

## 생산 결과

- main: `5872ff4`(#660), 기능 merge `4820cc1`(#659), 기능 커밋 `d0670e8`.
- 공개자료 run `32617443986`: 16/16 발행, `overall_ok=true`, 교차 검증 5/5.
- 전략 공장 run `32617443941`: 묶음 `treasury-carry-factory-5496f4ae998a`.
- 검정 수: 기존 512, 국채 공식 64, 합계 576, 고유 전략 지문 576.
- 판정: `NO_FACTORY_EDGE`, 선택 후보 없음, 배포 설정 없음, 연구 캐너리 자격 없음.
- 잠정 최고: `treasury-curve_barbell-dfacba3b4e91`.
- 통과: 자료 완전성·신선도, 연구/주문 일치, 표본 길이, 칼마 우위, 낙폭 방어,
  50bp 비용 후 양의 수익, 기존 전략 상관 `0.322005`, 혼합 낙폭 비악화.
- 실패: DSR `0.018830 < 0.95`, PBO `0.880952 > 0.10`, PSR
  `0.677424 < 0.95`, 구간 승률 `0.50 < 0.60`, 샤프 우위
  `0.099378 < 0.20`, 혼합 샤프 개선 `0.021310 < 0.05`.

## 계좌와 주문

- 최신 money-path run `32616997853`: `PREVIEW_ONLY`, 단 0/5, 배치 자본 0.
- capital-path run `32617019459`: `ACCUMULATING_EDGE`, 우선 승격 후보 없음.
- KIS smoke run `32616655186`: 5/5, 현금 934.27달러, NAV 1457.59달러,
  기존 외부 보유 ORANY 28주, 최근 주문 0건, 열린 미체결 0건.
- 전략 체결 0건, 전략 실현·미실현·총손익 0달러다. 외부 보유 ORANY는 전략
  측정 계약에서 제외한다.
- 합격 전략이 없으므로 강화된 주문 없는 캐너리와 자동 무장은 실행하지 않았다.

## 배포와 검증

- 구현 deploy run `32616655213`, 보정 deploy run `32617443947`: 모두 성공.
- `uv run pytest -q`: 2960 passed, 5 skipped.
- `uv run ruff check src tests`: 통과.
- 집중 61개, Ruby Psych YAML, `git diff --check`: 통과.
- `uv run python scripts/agent_harness_probe.py --strict`: OK 14/14.
- `uv run python scripts/check_handoff_facts.py`: OK.
- PR #659와 #660의 품질 관문과 merge 가능 상태 확인 뒤 merge 방식으로 병합했다.

## 안전 경계

공장 합격 기준을 낮추지 않았다. K1 포지션 한도, K2 허용 종목, 손실 예산 20%,
정규장, 현금 1% 여유, production 기계 승인, 서명·nonce, 추가 전용 감사 로그를
유지한다. 이 기능 출시와 생산 검증에서 실제 주문, 자본 배분, live 전략 교체는
발생하지 않았다.

## 다음 세션 판단

국채 만기 캐리는 기존 전략과 상관이 낮고 낙폭 혼합 효용은 있었지만, 독립 수익
우위가 우연과 구별될 만큼 강하지 않았다. 같은 64개를 다시 돌리거나 통계 기준을
낮추지 않는다. 다음 전략군은 `independent_credit_or_fx_carry`다. 회사채 스프레드
캐리 또는 외환 금리차처럼 다른 위험 프리미엄을 먼저 사전등록하고, 이번 576회까지
포함한 다중검정과 같은 주문 전 관문으로 검증해야 한다. 실제 전략 체결과 양의 전략
손익 전에는 돈을 벌었다고 보고하지 않는다.
