# Python
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
.pytest_cache/
.ruff_cache/

# Virtual env
.venv/
venv/

# uv
# (uv.lock is committed)

# Env / secrets
.env
.env.*
!.env.example
*.key
credentials.json

# IDE / OS
.vscode/
.idea/
.DS_Store

# Claude Code agent folder may contain auth tokens; per spec-kit guidance.
# Keep skills/commands committed but ignore anything sensitive.
.claude/settings.local.json
.claude/.credentials*

# Build / dist
build/
dist/

# auto-invest runtime artifacts
data/
# Curated synthetic-shock named datasets are spec-008 K7 Kernel files;
# they are versioned in git and protected by kernel.toml [K7_named_datasets].
!data/ohlcv/
!data/ohlcv/datasets/
!data/ohlcv/datasets/*.json
*.db
*.db-wal
*.db-shm
*.pid

# operator-specific TOML rules (the example fixture under tests/fixtures/rules/ is committed).
config/rules.toml
