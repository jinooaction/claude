# HANDOFF-156 - 자동 전략 공장과 10% 연구 캐너리

## 상태

PR #653이 `main`에 병합됐고 배포와 KIS smoke가 성공했다. 자동 전략 공장은
네 전략군 256개 고유 후보를 생산 자료로 모두 검증했다. 합격 후보는 없었으므로
권위 상태는 `armed:false`, 단 0, 자본 0이며 실주문은 불가능하다.

## 무엇을 만들었나

- 추세 균등가중, 추세 역변동성, 상대 모멘텀, 저회전 방어의 네 전략군을 각각
  64개 후보로 결정론적으로 생성한다.
- 후보마다 현재 live 엔진이 실행할 수 있는 TOML 설정과 정확한 전략 지문을 만든다.
- 월별 미래 누출 없는 재생, 10·25·50bp 비용, 10개 시간 구간, 벤치마크 비교를 수행한다.
- 모든 시도를 추가 전용 장부에 보존하고 누적 시도 수로 DSR과 PBO를 다시 계산한다.
- DSR, PBO, PSR, 구간 승률, 샤프·칼마 우위, 낙폭 방어, 50bp 비용 후 양의 수익을
  모두 통과한 후보만 승자로 발행한다.
- 합격 후보는 강화된 주문 없는 검사와 실제 설정 지문 일치 뒤에만 NAV 10% 연구
  캐너리로 자동 진입할 수 있다. 20% 이상은 기존 전진 관측과 성과 관문을 유지한다.
- 탈락하면 같은 묶음을 반복하지 않고 다음 독립 묶음으로 이동한다. 정해진 네 묶음을
  모두 소진하면 더 이상 숫자만 바꾸지 않고 탐색 공간 소진을 명시한다.

## 생산 결과

- main: `f91cc8c`(#653), 기능 커밋 `8698225`.
- 생산 실행: `32583681179`, `32583856609`, `32583876789`, `32583914149`, 모두 성공.
- 장부: 256줄, 고유 후보 ID 256개.
- 마지막 묶음: `strategy-factory-e676a1d58b7e`, 순번 3, 64/64 완료.
- 판정: `NO_FACTORY_EDGE`, `search_space_exhausted=true`, 연구 캐너리 자격 없음.
- 잠정 최고: `factory-trend_inverse_vol-001ad6d2e302`.
- 통과: DSR `0.999192`, 칼마 우위, 낙폭 방어, 50bp 비용 후 양의 수익.
- 실패: PBO `0.738095 > 0.10`, PSR `0.629816 < 0.95`, 구간 승률
  `0.50 < 0.60`, 샤프 우위 `0.087475 < 0.20`.

## 계좌와 주문

- `armed:false`, 단 0, 승인 자본 0, 계좌 NAV 1457.59달러.
- KIS smoke run `32583681175`: 5/5 통과, 현금 934.27달러, ORANY 28주.
- 최근 주문 0건, 열린 미체결 0건, 신규 체결 0건, 신규 전략 수익 0달러.
- 배포 run `32583681149`: 첫 시도 시간 초과 뒤 rollback, 재시도 2 성공.

## 검증

- `uv run pytest -q`: 2937 passed, 5 skipped.
- `uv run ruff check src tests`: 통과.
- `uv run python scripts/agent_harness_probe.py --strict`: OK 14/14.
- `uv run python scripts/check_handoff_facts.py`: OK.
- workflow YAML, `git diff --check`, PR 품질 관문과 GitHub 검사: 통과.

## 안전 경계

헌법 X.4 v8.0.0의 NAV 10% 단 1은 공장 모든 관문을 통과한 후보에만 열린다.
K1 포지션 한도, K2 허용 종목, 손실 예산 20%, 정규장, 현금 1% 여유,
production 기계 승인, 서명·nonce, 추가 전용 감사 로그는 유지된다. 기준을
낮추지 않았고 실제 주문·자본 배분·live 전략 교체는 발생하지 않았다.

## 다음 세션 판단

현재 매개변수 공간에서는 실거래할 만한 검증 엣지가 없다. 같은 SPY·IEF·GLD
조합을 반복하거나 합격 기준을 낮추지 않는다. 다음 작업은 독립적인 거시 국면
신호나 새 자료원처럼 경제적 이유가 다른 후보군을 SDD로 정의하고, 같은 누적
다중검정 장부와 실거래 연결 관문으로 검증하는 것이다. 체결과 양의 전략 손익
전에는 돈을 벌었다고 보고하지 않는다.
