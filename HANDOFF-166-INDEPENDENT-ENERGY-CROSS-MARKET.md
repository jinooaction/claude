# HANDOFF-166 - 독립 에너지 교차시장 전략·736회 판정

## 상태

PR #680이 merge 커밋 `08970e7`로 `main`에 들어갔다. 생산 전략 공장과 현재
커밋의 KIS 읽기 전용 점검은 성공했다. 에너지 전략은 `NO_FACTORY_EDGE`이며 현재
돈 경로는 `PREVIEW_ONLY`, `ACCUMULATING_EDGE`, 단 0, 자본 0, 실주문 불가다.
서버 코드 배포는 미국 장중 금지 규칙으로 연기돼 예약 타이머를 기다린다.

## 무엇이 잘못됐고 무엇을 고쳤는가

- 기존 관문은 포트폴리오에 20% 섞었을 때 개선되는지를 묻는 분산 보조 기준이었다.
  이것을 에너지 자체 수익 전략에도 공통 적용한 목적 분류는 불완전했다.
- 기존 분산 보조 기준은 낮추거나 바꾸지 않았다. 대신 에너지 전략이 현금과 에너지
  단순보유를 모두 이기는지 묻는 단독 수익 기준과 자본 없는 연구 관찰 기준을 추가했다.
- 개발 점수가 같은 후보를 문자열 역순으로 고르던 결함을 발견해, 낙폭이 작고 후보
  식별자가 안정적인 순서로 결정되게 고쳤다.
- 자료가 불완전하거나 오래됐을 때 결과를 출판하지 않게 했고, 실패한 실행이 마지막
  정상 증거를 덮어쓰지 못하게 했다.

## 기준 의심에 대한 결론

- 16후보 전체 선택을 포함한 500회 모의실험에서 무효 전략의 거짓 합격률은 `4.4%`다.
- 의도적으로 심은 강한 신호의 검출률은 `84.0%`, 올바른 후보 선택률은 `86.8%`다.
- 현실 AQR 양성·무효 대조군도 기존 분산 보조 기준에서 각각 통과·탈락한다.
- 따라서 기준은 통과 불가능하지 않다. 다만 목적별 질문을 구분하지 않았던 설계는
  실제 오류였고 이번에 분리했다.

## 에너지 전략 결과

- 공식 EIA WTI·휘발유·난방유·천연가스와 Kenneth French Oil 수익률을 사용했다.
- WTI 추세, 3:2:1 정제마진, 시장 폭, 확장형 릿지 예측의 네 문법에 6/12개월과
  최대 에너지 비중 50/100%를 결합한 16개 후보다.
- EIA 월 `t` 정보는 목표 월 `t+2`부터만 사용한다. 개발 120개월, 격리 1개월,
  홀드아웃 219개월, 거래비용 10/25/50bp를 결과 전에 고정했다.
- 사전 선택 후보 `energy-cross-ridge_forecast-d869beb7c67e`의 PSR은 `0.822115`,
  50bp 후 연 현금 초과수익은 `+1.912757%`, 에너지 단순보유 대비 샤프 변화는
  `-0.002315`, 최대낙폭은 `35.632935%` 대 `64.750875%`다.
- 활성 에너지 비율이 `95.8904%`라 매매 시점 전략보다 거의 상시 보유에 가깝다.
  단독 실거래 기준과 연구 관찰 기준 모두 통과하지 못했다.
- 사후 최상 시장 폭 후보는 PSR `0.861771`, 연 현금 초과 `+1.657890%`, 샤프 개선
  `+0.073612`, 활성 비율 `47.4886%`로 연구 관찰 기준만 통과했다. 홀드아웃을 본 뒤
  찾았으므로 `promotion_allowed=false`이며 승격할 수 없다.

## 생산 증거

- merge: `08970e7b2dbb065eda824a7d3de5485b876865d4`
- strategy factory: `32861194112` success, 16/16 current, 720 prior,
  736/736 unique, `OBJECTIVE_GATE_PASSABLE_CANDIDATE_UNCONFIRMED`
- KIS smoke: `32861858561` success, current merge commit, read-only 5/5
- KIS: cash 934.27달러, NAV 1454.23달러, ORANY 28주, 최근 주문 0건,
  열린 미체결 0건
- money path: `PREVIEW_ONLY`, `ACCUMULATING_EDGE`, rung 0, capital 0,
  forward 0/20, real-order submission false

## 배포 상태

deploy run `32861194460`의 GitHub 작업은 성공했지만 실제 서버 배포는 완료되지 않았다.
서버가 미국 정규장 중 코드 변경을 안전하게 거부했고 다음 허용 시각을
`2026-08-25T20:00:00Z`로 계산했다. `auto-invest-deploy.timer`의 다음 실행은
`2026-08-25T21:00:00Z`다. 다음 세션은 이 시각 이후 서버 감사 로그 또는 배포
사이드카로 `08970e7` 반영 여부를 확인해야 한다.

## 검증

- `uv run pytest -q`: 3072 passed, 5 skipped
- `uv run ruff check src tests`: 통과
- focused energy tests: 18 passed
- YAML, `git diff --check`, 엄격 하네스 14/14, HANDOFF 사실 검사,
  PR 품질 관문: 통과

## 안전 경계

실제 주문, 자본, 무장, 허용목록, 포지션 한도, 손실 예산, 헌법, 커널은 바꾸지 않았다.
`XLE`은 연구상 의도 상품일 뿐 라이브 구현·역사 정합·허용목록 승인·배포 설정·강화
캐너리·정확한 지문 정합이 없다. `Backtest -> Canary -> Full` 단계도 유지된다.

## 다음 우선순위

1. `2026-08-25T21:00:00Z` 이후 서버가 `08970e7`을 실제 반영했는지 확인한다.
2. 에너지 홀드아웃 결과를 이용해 기준이나 후보를 다시 조정하지 않는다.
3. 다음 독립 전략군은 옵션 변동성 위험 프리미엄이다. 옵션 지수·풋옵션 매도 수익,
   꼬리손실, 담보 현금, 실제 거래비용과 거래 가능 상품을 결과 전에 새 등급 4 SDD로
   사전등록한다.
4. 사후 시장 폭 후보를 추적하려면 새 식별자의 자본 없는 전진 관찰로 시작하고,
   이번 홀드아웃을 새 확증 자료로 재사용하지 않는다.
