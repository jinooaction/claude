# HANDOFF-164 - 판정·장부·연구 캐너리 증거 계약 정합화

## 상태

PR #673~#676이 `main`에 병합됐고 배포, 전략 공장, 자본 사다리, KIS 읽기 전용 점검,
돈·자본 경로, 계좌 정합성 재평가가 성공했다. 현재 권위 상태는 `PREVIEW_ONLY`,
`ACCUMULATING_EDGE`, 단 0/5, 자본 0, 체결 0건, 전략 손익 0달러다. 실주문은 불가능하다.

## 운영자 의심에 대한 결론

운영자의 의심은 상당 부분 맞았다. 후보가 탈락한 이유 전부가 기준 오류는 아니지만,
시스템에는 실제 구현 오류가 네 개 있었다.

1. 전략과 기준자산의 서로 다른 표본을 비교해 유의성을 왜곡했다.
2. 레버리지와 서로 다른 자본 베이시스가 전진 장부에 섞였다.
3. 생산자는 16/16 완결 전략군을 내는데 소비자는 exact 64/64만 받아 미래 합격 후보도 막았다.
4. 실제 첫 자본 계산은 10%인데 돈 경로와 재지정 안내 일부는 옛 25%를 표시했다.

네 오류는 각각 쌍체결 활성수익 PSR, 무레버리지 v2 장부, 버전형 공용 증거 계약,
공통 사다리 렌더링으로 교정됐다. 현재 후보는 교정 뒤에도 합격하지 못했으므로 남은 탈락은
구조적 거부가 아니라 관측된 증거 부족이다.

## 합격 기준 감사

- 완전 관문의 무효자료 거짓 합격률은 `4.82%`다.
- 48개 관측에서 연율 샤프 1.5 신호의 완전 관문 검출률은 `15.68%`다.
- 종이 PSR 0.80 단계의 무효자료 통과율은 `20.42%`라 자본 0으로만 운영한다.
- 즉 현재 기준은 불가능하지 않지만, 짧은 표본의 진짜 효과를 자주 놓치는 보수적 기준이다.
- 10% 연구 캐너리는 0.95와 모든 경제성 관문을 통과한 `FACTORY_EDGE`만 허용한다.
  0.80~0.95 `PAPER_CHALLENGER`는 새 증거를 모으되 브로커·자본 권한이 없다.
- 현재 공급·수요 후보 PSR `0.705637`은 0.95 실자본 기준뿐 아니라 0.80 종이 기준도 미달한다.

자동매매 프로그램이 많다는 사실은 그 프로그램들이 비용 후, 손대지 않은 자료에서도
검증됐다는 뜻이 아니다. 이 시스템의 합격 수가 적은 이유는 실제 오류와 더 엄격한 검증이
함께 있었기 때문이며, 이제 둘을 분리해서 볼 수 있다.

## 현재 전략 판단

- 정확 배포 후보 `globalfixed-ensemble-3-6-9-12`는 2007~2026년 235개월 홀드아웃에서
  연 50bp 비용 후 연복리 `8.682649%`, 샤프 `1.831434`, 최대낙폭 `5.572914%`다.
- 같은 기간 기준자산은 연복리 `8.291414%`, 샤프 `1.264685`, 최대낙폭 `17.268823%`다.
- 따라서 “마땅한 전략이 하나도 없다”는 말은 틀리다. 역사적으로 유망한 정확 후보는 있다.
- 다만 오염을 제거한 현재 `globalfixed` 전진 장부는 관측 `0개`라 지금 실거래 합격이라고
  부를 수 없다. 과거 장부를 새 장부에 복사해 기다림을 숨기는 것은 검증이 아니다.
- 최신 EIA 공급·수요 후보는 비용 후 연 초과수익 `+0.447504%`, 기존 전략 상관
  `-0.135530`, 혼합 샤프 개선 `+0.118590`, 낙폭 개선을 보였지만 통계적으로 미확정이다.

## 생산 결과

- #675 merge `8b05fb9`; deploy `32813701562`; KIS smoke `32813701577` 5/5.
- 전략 공장 `32813806129`: 16/16 완결, 704/704 고유 감사, `NO_FACTORY_EDGE`.
- 자본 사다리 `32814438836`: `WAIT_EDGE`, 단 0→0, 센티넬·PR·주문 변경 없음.
- 수익 증거 `32814663999`: 정확 배포 후보 역사 홀드아웃 통과, 전진 관측 0.
- 실계좌 손익 `32814666412`: 체결 0건, 실현·미실현·총손익 0달러.
- 정합성 `32814668670`: `CLEAR`, `OK`, halt 없음, 주문 0건.
- #676 merge `194b7ed`; deploy `32816471355`; 라이브 미리보기 `32816471308`.
- 라이브 미리보기는 `armed:false`, 자본 0이라 승인과 실주문 작업을 건너뛰었다.
- live-profit `32816515451`: `NO_FILLS_YET`, 체결 0건, 총손익 0달러.
- money-path `32816555495`: 단1 `10%`, 약 145달러로 정확히 표시.
- capital-path `32816589404`: `ACCUMULATING_EDGE`, `PREVIEW_ONLY`.

## 검증

- #675 전 `uv run pytest -q`: 3048 passed, 5 skipped.
- #676 전 `uv run pytest -q`: 3050 passed, 5 skipped.
- 건너뛴 KIS 실연결 5개는 생산 smoke에서 5/5 통과했다.
- `uv run ruff check src tests`, workflow YAML, `git diff --check`: 통과.
- `uv run python scripts/agent_harness_probe.py --strict`: OK 14/14.
- HANDOFF 사실 검사, PR #675·#676 품질 관문, 충돌 없음, merge 방식 병합을 확인했다.

## 안전 경계

헌법 X.4 v9.0.0, K1 한도, K2 허용목록, 손실 예산 20%, 정규장, 현금 1% 여유,
production 기계 승인, 서명·nonce, 추가 전용 감사 로그, `Backtest -> Canary -> Full`은
유지된다. ORANY 28주는 시작 전 외부 보유로 전략 손익과 자동 매도에서 제외한다.

## 다음 세션 판단

추가 배관 수정이나 결과를 본 뒤의 기준 완화가 우선순위가 아니다. 현재 해야 할 일은
고정된 `globalfixed` 무레버리지 전진 장부가 자동으로 쌓이는지 관찰하면서, 병렬로 완전히
독립적인 USDA 작물 수급 또는 에너지 교차시장 전략군을 결과 전에 사전등록하는 것이다.
현재 후보를 실거래 합격으로 꾸미거나 옛 49개 관측을 새 장부에 복사하지 않는다.
