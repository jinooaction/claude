# HANDOFF-162 - 현실 관문 감사와 독립 원자재 재고·포지셔닝 688회 검정

## 상태

PR #669가 `main`에 병합됐고 배포, 생산 전략 공장, KIS 실연결 읽기 전용 점검,
돈·자본 경로 재평가가 모두 성공했다. 현실 대조군은 통계선을 통과했지만 새 원자재
전략은 `NO_FACTORY_EDGE`다. 권위 상태는 `PREVIEW_ONLY`, `NO_EDGE_YET`, 단 0/5,
자본 0이며 실주문은 불가능하다.

## 무엇을 만들었나

- Fama-French 미국 시장 초과수익과 AQR 전체 자산 시계열 추세를 2007년 이후 실제
  양성 대조군으로 고정했다. 같은 수익률에서 평균을 제거한 자료는 널 대조군이다.
- 실제 대조군은 관문 진단 전용이다. 후보 선택, 다중검정 수, 승격 증거에 섞지 않으며
  자료가 오래되거나 코드·지문이 다르면 후보 성과와 무관하게 승격을 닫는다.
- CFTC 분리 선물 보고서의 12개 고정 계약에서 관리 자금과 생산자 순포지션을
  미결제약정으로 나누고 계약 안에서 표준화한 뒤 중앙값으로 합친다.
- EIA 상업 원유 재고 `WCESTUS1`을 역방향 표준점수로 사용한다. CFTC는 보고일+3일,
  EIA는 기간 종료+5일 뒤부터만 월초 결정에 사용할 수 있다.
- 관리 자금 추세, 생산자 희소성, 재고 긴축, 포지션·재고 확인의 네 문법에 26/52주와
  GSG 최대 50/100%를 결합한 정확히 16개 후보를 고정했다.
- 개발 96개월, 격리 한 달, 홀드아웃 최소 120개월, 10/25/50bp 비용, 라이브 PSR
  0.95와 종이 PSR 0.80을 결과 전에 고정했다. 과거 672개에 16개를 더해 688개
  고유 감사 장부를 생산한다.

## 배관 교정

첫 최신 자료 호출은 AQR 엑셀 파일 아래에 서식만 남은 완전 빈 행을 숫자로 바꾸려다
후보 계산 전에 중단됐다. 전략 JSON과 판정은 나오지 않아 감사 장부에 넣지 않았다.
여섯 기대 열이 전부 빈 행만 제외하고 일부 열이 빈 행은 계속 실패하도록 파서와 회귀
시험을 보강한 뒤 후보·기간·비용·기준을 바꾸지 않고 처음부터 재실행했다.

## 생산 결과

- main merge `ebbefee`, 기능 커밋 `75cdc51`, PR #669.
- 전략 공장 run `32789350616`, 생산 시각 `2026-08-24T23:34:11Z`.
- 현실 관문: `REAL_WORLD_CONTROLS_VALID`.
- Fama-French 234개월: 샤프 `0.648641`, PSR `0.996536`; 평균 제거 PSR `0.500000`.
- AQR 추세 233개월: 샤프 `0.440025`, PSR `0.974381`; 평균 제거 PSR `0.500000`.
- 감사 수: 과거 672, 현재 16, 합계 688, 고유 전략 지문 688.
- 개발 선택 후보: `commodity-positioning-managed_money_trend-9480c3c1956f`.
- 131개월 홀드아웃 PSR `0.525291`, 50bp 비용 후 연 초과수익 `-1.769314%`.
- 기존 전략과 상관 `0.088256`, 혼합 샤프 변화 `-0.194567`.
- 판정: `NO_FACTORY_EDGE`; 선택·종이 후보·배포 설정·연구 캐너리 자격 모두 없음.

## 합격 기준 판단

PSR 0.95 통계선은 실제 역사상 강한 시장 프리미엄과 분산 추세를 통과시키고, 같은
자료의 평균을 없애면 탈락시켰다. 따라서 통계선 자체가 모든 실제 전략을 영구 탈락시키는
구조는 아니다. 새 원자재 전략은 통계선 근처에서 아깝게 잘린 것이 아니라 비용 후
초과수익이 음수이고 기존 포트폴리오 혼합 샤프도 하락했다.

이 감사가 모든 경제성 조건이 영구 최적임을 증명하는 것은 아니다. 시장 프리미엄은
순수 알파가 아니고 과거 양성 대조군은 미래 수익을 보장하지 않는다. 다만 이번 가족의
불합격을 기준 하나의 오류로 돌릴 근거는 없으며, 기준을 낮춰도 음의 수익 문제는 남는다.

## 배포, 계좌와 돈 경로

- deploy run `32789350661`: main `ebbefee` 성공.
- KIS smoke run `32789992141`: 5/5, 키 유효, 현금 934.27달러, NAV 1457.59달러,
  기존 외부 보유 ORANY 28주, 최근 주문 0건, 열린 미체결 0건.
- money-path run `32790126398`: `PREVIEW_ONLY`, `NO_EDGE_YET`, 단 0/5,
  배치 자본 0, 전략 체결 0건, 전략 손익 0달러.
- capital-path run `32790129322`: `ACCUMULATING_EDGE`, 우선 승격 후보 없음.
- GSG는 라이브 허용목록 밖이며 자본 배분, 실제 주문, 라이브 전략 교체가 없었다.

## 검증

- 머지 직전 `uv run pytest -q`: 3013 passed, 5 skipped.
- 건너뛴 KIS 실연결 5개는 머지 뒤 생산 smoke에서 5/5 통과했다.
- `uv run ruff check src tests`: 통과.
- 집중 16개, workflow YAML, `git diff --check`: 통과.
- `uv run python scripts/agent_harness_probe.py --strict`: OK 14/14.
- PR #669 품질 관문 2회, 충돌 없음, merge 방식 병합을 확인했다.

## 자료 한계와 안전 경계

CFTC 과거 거래자 분류는 재분류 자료이고 EIA 현재 역사에는 개정값이 포함될 수 있다.
고정 12개 계약의 같은 비중 신호와 GSG 지수 구성도 정확히 같지 않다. 이 한계를 결과에
남겼다. 헌법·커널, K1 한도, K2 허용목록, 손실 예산, 정규장, 현금 여유, 기계 승인,
서명·nonce, 추가 전용 감사 로그, `Backtest -> Canary -> Full`은 변경하지 않았다.

## 다음 세션 판단

같은 포지셔닝 16개를 다시 돌리거나 홀드아웃에서 좋아 보이는 조합으로 바꾸지 않는다.
다음 신규 전략은 `commodity_fundamental_supply_demand`다. EIA 제품별 생산·재고와
USDA 작물 재고/사용처럼 가격·포지션과 다른 공급·수요 원천을 먼저 고정하고 게시 지연,
개정, 거래 상품 대응을 새 SDD에 사전등록한다. 실제 체결과 양의 전략 손익 전에는
돈을 벌었다고 보고하지 않는다.
