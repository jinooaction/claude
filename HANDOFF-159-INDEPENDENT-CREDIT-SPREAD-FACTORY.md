# HANDOFF-159 - 독립 회사채 스프레드 전략 공장과 640회 검정

## 상태

PR #663이 `main`에 병합됐고 배포, 생산 공개자료 검증, 생산 전략 공장, KIS 실연결
점검이 모두 성공했다. 생산 판정은 `NO_FACTORY_EDGE`이므로 권위 상태는
`PREVIEW_ONLY`, `armed:false`, 단 0/5, 자본 0이며 실주문은 불가능하다.

## 무엇을 만들었나

- 재무부 HQM 투자등급 회사채 10년·20년 금리와 DGS10·DGS30 국채 금리를
  시점 기준 월별 자료로 맞춘다. 회사채 자료는 최대 70일, 국채 자료는 최대 7일의
  최신성 한도를 둔다.
- 투자등급 회사채 수익을 LQD, 국채 수익을 IEF로 표현해 캐리·평균회귀·추세·완충
  전략을 각각 16개씩 총 64개 생성한다.
- 개발 구간 1990~2006년에서만 한 후보를 고정하고 한 달 격리 뒤 2007년 이후
  홀드아웃에서는 후보를 다시 고르지 않는다.
- 기존 가격 256회, 사전 탐색 192회, 거시 64회, 국채 64회를 원본 지문에서
  재구성해 과거 576개와 회사채 64개를 합친 640개 고유 감사 장부를 만든다.
- 현재 회사채 가족 64개만 DSR·PBO 진단에 사용하고 후보 수익 상관으로 독립
  시도 수 6.122831을 환산한다. 전체 640개는 연구 이력 감사에만 사용한다.
- 연구와 주문 준비가 같은 회사채 목표 비중 함수를 사용한다. 자료·코드·후보·
  전략 지문·목표 비중 다이제스트가 다르면 브로커 호출 전에 차단한다.

## 생산 결과

- main merge: `6c849a2`, 기능 커밋 `05a6dba`, PR #663.
- 전략 공장 run `32711600218`, 묶음 `credit-spread-factory-v2-0c525304d5c9`.
- 감사 수: 과거 576, 회사채 공식 64, 합계 640, 고유 전략 지문 640.
- 판정: `NO_FACTORY_EDGE`, 선택 후보 없음, 배포 설정 없음, 연구 캐너리 자격 없음.
- 잠정 최고: `credit-carry_buffer-d174c247f539`.
- 통과: 자료 완전성·최신성·게시 지연, 연구/주문 일치, 개발·홀드아웃 길이,
  개발 DSR `0.999900`, 개발 PBO `0.000000`, 50bp 비용 후 총수익
  `0.96879537`, 기존 전략 상관 `0.349515`, 혼합 낙폭 15.57% 대 17.27%.
- 실패: 최종 혼합 PSR `0.550035 < 0.95`, 혼합 샤프 개선
  `0.033178 < 0.05`, 단독 PSR 진단 `0.540020 < 0.95`.

## 공개자료

- HQMCB10YR·HQMCB20YR: 1984-01~2026-07, 각각 511개월.
- DGS10: 1962-01-02~2026-08-20, 관측값 16,144개.
- DGS30: 1977-02-15~2026-08-20, 관측값 12,374개.
- 회사채 자료 완전성·최신성·게시 지연 안전성과 최신 국채 자료가 모두 통과했다.
- HQM 합성 고품질 회사채 수익을 실제 거래 상품 LQD로 표현하는 기초 위험은
  결과에 명시했고, 이 차이를 숨긴 직접 수익률로 취급하지 않는다.

## 계좌와 주문

- 최신 money-path: `PREVIEW_ONLY`, `NO_EDGE_YET`, 단 0/5, 배치 자본 0,
  `can_submit_real_orders=false`, `armed:false`.
- capital-path: `ACCUMULATING_EDGE`, 우선 승격 후보 없음.
- KIS smoke run `32688063778`: 5/5, 현금 934.27달러, NAV 1457.59달러,
  기존 외부 보유 ORANY 28주, 최근 주문 0건, 열린 미체결 0건.
- 전략 체결 0건, 전략 실현·미실현·총손익 0달러다. 외부 보유 ORANY는 전략
  측정 계약에서 제외한다.
- `LQD`는 라이브 허용목록에 추가하지 않았다. 합격 전략이 없어 강화된 주문 없는
  캐너리, 자동 무장, 자본 배분은 실행하지 않았다.

## 배포와 검증

- deploy run `32647098681`: merge `6c849a2` 배포 성공.
- 기능 브랜치 `uv run pytest -q`: 2981 passed, 5 skipped.
- `uv run ruff check src tests`: 통과.
- 집중 117개, workflow YAML, `git diff --check`: 통과.
- `uv run python scripts/agent_harness_probe.py --strict`: OK 14/14.
- `uv run python scripts/check_handoff_facts.py`: OK.
- PR #663의 품질 관문과 merge 가능 상태 확인 뒤 merge 방식으로 병합했다.

## 안전 경계

공장 합격 기준을 낮추지 않았다. K1 포지션 한도, K2 허용 종목, 손실 예산 20%,
정규장, 현금 1% 여유, production 기계 승인, 서명·nonce, 추가 전용 감사 로그를
유지한다. 이 기능 출시와 생산 검증에서 실제 주문, 자본 배분, live 전략 교체,
헌법·커널 변경은 발생하지 않았다.

## 다음 세션 판단

회사채 후보는 비용 후 양의 수익과 낮은 기존 전략 상관, 낙폭 개선은 보였지만
독립 홀드아웃에서 우연과 구별될 신뢰도와 충분한 혼합 샤프 개선이 없었다. 같은
64개를 다시 돌리거나 통계 기준을 낮추지 않는다. 다음 전략군은 독립 외환 금리차다.
통화별 단기금리, 현물·선물 또는 거래 가능 ETF 대용물, 환헤지·거래비용·급격한
위험회피 국면을 먼저 사전등록하고 같은 개발/격리/홀드아웃 계약으로 검증한다.
실제 전략 체결과 양의 전략 손익 전에는 돈을 벌었다고 보고하지 않는다.
