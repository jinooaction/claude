# 세 방향 전략 로드맵 (2026-06-18) — 자율 성장 심화 · 실거래 적극화 · 새 알파

운영자가 2026-06-18 세션에서 세 방향을 모두 선택했다(1·3·4). 2번(폐회로 최종 검증)은
시스템 자율에 맡김. 이 문서는 그 세 방향을 **결정 준비 상태**로 정리한다 — 각 방향이
지금 정확히 어디에 막혀 있고, 안전하게(헌법 I~VII·VIII.A 불변) 갈 수 있는 다음 한 발이
무엇이며, 무엇이 운영자 전용 결정(돈 움직임·안전 경계 변경)인지.

근거는 모두 현재 코드다(추측 아님). 파일·줄 인용을 붙였다.

---

## 0. 한눈 요약 (세 줄)

- **1번 (자율 성장 심화)**: 캐너리 검증을 *통과한* L2 후보(예: `max_tokens`)가 지금
  **버려진다** — 감사 로그만 남기고 적용 안 함(`runner.py:200-213`, `promoted=False`).
  → 안전한 길 = **운영자 승격 큐**로 표면화(자동 승격 0, 헌법 IX.B-2 불변). 이 세션에서
  읽기 전용 모듈로 구현. 자동 적용(B안)은 안전 경계 변경이라 운영자 결정.
- **3번 (실거래 적극화)**: 첫 자본을 막는 건 **통계적 시간 게이트**(전진 관측 20개,
  거래일당 ~1개) + 엣지 확정이다. 셋 다 통과하면 자본 사다리가 단0→단1(NAV 25%)을
  **자율 무장**한다 — 운영자 게이트 없음(헌법 X.4). 안전하게 당기는 유일한 길 = 전진
  관측 리셋 방지(지문 안정). `min_obs` 단축은 거짓 엣지 위험 → **금지(기다리는 게 옳다)**.
- **4번 (새 알파)**: long-only 제약에서 추세추종은 이미 거의 최적임이 *입증됨*(스펙 054).
  현 제약 내 미시도 직교 후보 = **단기 평균회귀**(추세와 다른 시간프레임). 진짜 도약
  (롱숏·새 자산군)은 운영자 제약 확장 결정.

---

## 1. 방향 3 — 실거래 적극화: "왜 아직 돈이 0%인가"

운영자 비전 1순위("진짜 돈을 많이 벌자")에 가장 직접적인 질문이라 먼저 다룬다.

### 1.1 현재 단계 (money-path 종합)

`src/auto_invest/analytics/money_path.py` 가 흩어진 사이드카를 모아 첫-자본까지의 길을
단일 단계로 종합한다. 현재 단계는 **STAGE_ACCUMULATING**(단0, 자본 0%) — 전진 엣지를
매 거래일 쌓는 중. 막는 것: `전진 관측 부족: n_obs/min_obs`(`money_path.py:884`).

### 1.2 첫 자본을 여는 정확한 게이트 3개

자본 사다리(`src/auto_invest/portfolio/capital_ladder.py`)가 단0→단1을 무장하려면:

1. **전진 관측 ≥ 20** — `min_obs = forward_verdict.min_obs_required or PROMOTION_MIN_OBS`
   (`money_path.py:734`, `PROMOTION_MIN_OBS = 20`, `capital_ladder.py:66`). 전진 페이퍼가
   평일 22:30 UTC 1회 → **거래일당 ~1 관측**(`NOMINAL_OBS_PER_TRADING_DAY = 1.0`). 즉 깨끗이
   쌓이면 20관측 ≈ 20거래일 ≈ 4주.
2. **엣지 확정 (EDGE_CONFIRMED)** — 전진 성과가 벤치마크 칼마를 넘고 디플레이티드 샤프가
   유의(과적합 보정). 관측이 차도 엣지가 기준 미달이면 `NO_EDGE`(정상 — 과적합 방어).
3. **전략 지문 정합** — 라이브 배포 설정 == 전진 검증 설정. 불일치면 어떤 단에서도 자본
   배치 안 함(`STAGE_BLOCKED`, `money_path.py:815-827`).

### 1.3 핵심 사실 — 첫 자본은 운영자 게이트가 *아니다*

셋 다 통과하면 자본 사다리가 단0→단1(NAV의 25%)을 **자율 무장**한다(센티넬 PR 자동 머지,
헌법 X.4 상시 위임). 운영자가 "돈 넣어"라고 누르지 않는다(`money_path.py:949-953`).
**운영자 전용 레버는 셋뿐**: 입금(은행 이체 = NAV 상한), 킬스위치, 낙폭 예산(기본 20%,
강등 10%/정지 20%, `capital_ladder.py:63`).

### 1.4 분류 — 무엇이 "못 당김"이고 무엇이 "안전 조정 가능"인가

| 게이트 | 종류 | 당길 수 있나 |
|--------|------|---------------|
| 전진 관측 20개 | **순수 시간(통계)** | ❌ 당기면 거짓 엣지 위험. forward는 정의상 배포-후 실시간 관측이라 백필 불가 |
| 엣지 확정 | **측정** | ❌ 시장이 정하는 것. 당길 수 없음 |
| 지문 정합 | **설계** | ✅ 지문 안정으로 *보호* 가능(아래) |

### 1.5 안전하게 첫 자본을 앞당기는 정당한 길 (돈 무결성 유지)

- **(i) 전진 관측 리셋 방지가 최대 레버.** 전략 지문이 바뀌면 `consistent_basis_suffix`가
  과거 스냅샷을 떨궈 관측이 *줄고*(CONV_REGRESSED), 누적이 처음부터 다시 시작돼 첫-자본
  ETA가 영영 뒤로 밀린다(`money_path.py:504-514`). **전략 교체는 갈아엎지 말고 전진
  토너먼트에 *추가* 트랙으로** 넣어야 한다. 이미 감시 중(CONV_REGRESSED/SAMPLE_CHURNING).
- **(ii) 지문 정합 유지** — 라이브 배포 TOML과 검증 forward TOML을 일치시켜 BLOCKED 분기를
  차단.
- **(iii) 토너먼트 다트랙 중 선두 확인(검증 필요)** — forward 토너먼트가 6~7트랙이면 한
  트랙이 이미 관측+엣지를 채웠을 수 있다. 그러면 그 트랙으로 첫 자본이 더 빨리. 라이브
  money-path 사이드카(`automation/*` 사이드카 브랜치)에서 트랙별 n_obs를 읽어 확인할 것.

### 1.6 못 당기는 것 (해서는 안 되는 것)

- **`min_obs`(20) 단축 금지.** 통계 무결성(거짓 엣지 방어)을 깨는 것 — 헌법 X 측정 주도
  성장 위반. 적은 표본으로 자본을 넣는 건 "적극화"가 아니라 도박이다. **기다리는 게 옳다.**

### 1.7 운영자 결정 (돈 움직임)

- **입금으로 NAV 상한 상향** → 첫 자본의 *절대 달러 규모*가 커진다(비율은 단1=25% 고정).
  이건 운영자 전용 레버(은행 이체). "적극화 = 더 큰 첫 베팅"을 원하면 이게 정공법이다 —
  게이트를 약화하지 않고 같은 안전 비율에 더 큰 자본을 태운다.

---

## 2. 방향 4 — 새 알파: 비상관 수익원

### 2.1 입증된 부정 결과 (스펙 054)

`specs/054-uncorrelated-alpha/FINDINGS.md` 가 152년 데이터(Shiller 1871~)로 밸류·캐리를
추세추종에 결합해 측정했다. 결과: **추세↔후보 상관 0.46~0.63 — 비상관 아님.** 결합 샤프
이득은 노이즈 수준(+0.02). 조건부 결합(약세장에 밸류로)은 *더 나쁨* — 떨어지는 칼을 잡아
추세의 방어를 무력화.

**구조적 이유**: long-only는 셋 다 주식/채권 베타를 공유 → 양의 상관 불가피. AQR식 진짜
비상관 밸류/모멘텀은 *롱숏*(싼 것 매수 + 비싼 것 공매도)이고 그 시장 베타 중립이 직교를
만든다. 우리는 long-only ETF(KIS) 제약. → **현 제약에서 추세는 거의 최적의 long-only 전략.**

### 2.2 현 제약 내 미시도 직교 후보

스펙 054는 밸류·캐리만 반증했다. long-only 안에서 *시간프레임*이 다른 후보는 안 해봤다:

- **단기 평균회귀 (가장 유망)** — 추세=장기 모멘텀과 다른 시간프레임(며칠~2주). long-only로
  구현 가능(기존 데이터), 추세가 비어 있는 단기 구간을 메울 수 있어 직교 가능성. **스펙 054가
  안 한 probe.** `scripts/value_carry_probe.py` + `value_carry.py` 패턴 그대로 재사용해
  사과 대 사과로 직교성·결합 샤프를 *측정 후에만* 배선("측정한 것만 배선").
- 저변동성/품질 팩터 — 여전히 주식 베타라 밸류처럼 상관 높을 가능성. 우선순위 낮음.

### 2.3 진짜 도약 (운영자 제약 확장 결정)

- **롱숏(공매도)** — 마진 계좌·다른 위험 프로파일·다른 규제. 비상관 다변화의 정공법이지만
  현 시스템 제약 밖. **운영자 결정.**
- **새 자산군(통화·원자재 선물·변동성)** — KIS 화이트리스트 밖. **운영자 결정.**

### 2.4 권고

먼저 **단기 평균회귀 probe**(돈 0 이동, 측정만)로 현 제약 내 마지막 직교 후보를 정직하게
검증. 직교성이 확인되면 forward 토너먼트에 *추가* 트랙으로(라이브 무변경). 안 되면 스펙 054
권고대로 "공매도/새 자산군이 열릴 때까지 다변화 보류, 자율 전략 진화에 집중"이 옳다.

---

## 3. 방향 1 — 자율 성장 심화: 캐너리→적용 루프 닫기

### 3.1 현재 공백

자율 튜너(스펙 005)의 L1/L2/L3 흐름(`src/auto_invest/tuner/`):

- **L1** (`threshold_tighten`, KPI 임계값) → 즉시 자동 적용(`runner.py:216-264`).
- **L2** (`max_tokens` 등 판단 지점 LLM 설정) → 하드닝 캐너리로 검증(`canary_submit.py`).
- **L3/L4** → 캐너리 확장 윈도 / 커널은 운영자.

**문제**: L2 후보가 캐너리를 *통과해도*(`outcome="passed"`) 그 변경은 **적용되지 않는다**.
`AutoTunedCanaryValidatedPayload`로 감사 로그만 남고 `promoted=False`, 변경은 버려진다
(`runner.py:200-213`, `canary_submit.py:117` "불변: 자동 승격 없음"). 시스템이 스스로 검증한
개선이 *조용히 사라진다* — 자율 성장의 누수.

### 3.2 두 갈림길

- **(A) 운영자 승격 큐 — 안전, 자율 구현 가능 (이 세션)**
  캐너리 합격 후보를 세션 횡단으로 모아 운영자가 한눈에 보고 한 번 승인하는 표면으로
  올린다. **자동 승격 0건 유지(헌법 IX.B-2 불변)** — 운영자가 여전히 게이트. HANDOFF 후속
  후보의 "L2/L3 후보 → 캐너리 합격 시 운영자 승격 큐"에 정확히 해당. 안전 경계 무변경.
- **(B) 자동 적용 — 안전 경계 변경 (운영자 결정 필요)**
  합격하면 튜너가 곧장 config에 적용. `promoted=False` 불변(IX.B-2)을 완화한다.
  `max_tokens`는 판단 지점 LLM 설정이라 돈·포지션을 직접 안 움직이지만, **튜너의 자율 적용
  권한을 확대**하는 것이라 안전 경계 변경이다. 헌법상 "this changes the safety perimeter"
  표식이 필요한 종류 → **운영자 명시 승인 없이는 하지 않는다.**

### 3.3 이 세션 구현 (A)

`money_path.py`와 같은 **읽기 전용 소비자 계층** 패턴으로 `promotion_queue.py`를 만든다 —
감사 로그의 `AUTO_TUNED_CANARY_VALIDATED`(outcome=passed) 이벤트를 집계해 "승격 대기 큐"
리포트(사람용 텍스트 + 기계용 JSON)를 낸다. 순수·결정론·돈 0 이동. 운영자가 합격한 개선을
한눈에 보고 승격을 결정할 수 있게.

---

## 4. 이 세션 산출물 + 운영자 결정 대기 목록

**이 세션에서 자율로 수행(돈 0 이동·안전 경계 무변경):**
- [구현] 방향 1 (A): 캐너리 합격 후보 **운영자 승격 큐** 읽기 전용 분석 모듈 + 테스트.
- [설계] 방향 3·4 결정 준비 로드맵(이 문서).

**운영자 결정 대기(다음 상호작용에서):**
- **3번**: 입금으로 첫 자본 달러 규모를 키울까? (돈 움직임 — 운영자 전용. 게이트는 안 건드림)
- **4번**: 단기 평균회귀 probe 착수할까(돈 0)? / 롱숏·새 자산군으로 제약을 확장할까(큰 결정)?
- **1번 (B)**: 캐너리 합격 L2를 자동 적용까지 허용할까? (안전 경계 변경 — IX.B-2 완화)

진짜 현재 상태는 항상 `git_ground_truth` 시작 훅 + 최신 HANDOFF + `/sync`를 믿을 것.
