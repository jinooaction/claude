"""auto_invest.backtest — deterministic replay engine for spec 008.

Public surface (populated incrementally through the implementation):
- run_backtest(config: BacktestConfig) -> BacktestResult   (added in T041)
- BacktestConfig                                           (added in T012)
"""
