# HANDOFF-175 - PEAD 역사 엣지와 21가족 진단 교정

## 상태

스펙 175는 PR #706, merge commit `9d22a2b`로 `main`에 반영됐다. 기존 프로그램은
가족당 오합격 상한 0.010과 최대 20가족을 고정해 보수 합계 0.200을 모두 소진했으므로,
21번째 가족은 전략 성과와 무관하게 진입할 수 없었다. 이 용량 오류를 교정한 뒤 실적발표 후
주가 지연반응(PEAD) 16개 사전등록 후보를 공개 자료로 검증했고 역사 판정은
`PUBLISHED_EDGE`다.

이 결과는 실거래 승인이 아니다. 새 프로그램 계약 `3.2`는 진단 전용이고, 기존 돈 경로는
이를 명시적으로 거부한다. 연구 캐너리·승격은 거짓, 배포 설정은 `null`, 자본은 0%, 주문은
0건이다.

## 운영자의 의심에 대한 결론

의심은 중요한 부분에서 맞았다. “아무 전략도 합격하지 못했다”는 과거의 넓은 표현에는 서로
다른 세 가지가 섞여 있었다.

1. 합격 배관과 장부에는 실제 오류가 있었다. 앞선 작업에서 빈 앵커드 결합, 소비자가 교정을
   읽지 않던 문제, JSONL 숫자 오염, 공개 후보 ID 가림을 고쳤다.
2. 이번에는 20가족 고정 한도가 21번째 가족을 결과와 무관하게 막는 용량 오류를 확인했다.
3. 그렇다고 개별 전략의 모든 탈락이 오류였던 것은 아니다. 바로 전 회계 전략은 격리 구간의
   비용 후 성과가 실제로 음수여서 교정 뒤에도 탈락한다.

따라서 현재의 정확한 결론은 “역사적으로 합격한 전략이 없다”가 아니라 “PEAD 역사 엣지는
발견했지만, 현재 계좌에서 실자본을 맡길 만큼 독립적인 전진·체결 증거는 아직 없다”다.

## 결과 전에 고정한 프로그램 계약

- 기존 `3.1` 실자본 계약은 변경하지 않았다.
- 진단 `3.2`의 가족 구성은 16후보 가족 11개, 64후보 가족 10개다.
- 16후보 가족 상한은 0.010, 64후보 가족 상한은 0.009다.
- 독립성 가정 없이 합산한 프로그램 오합격 상한은 `0.200`이다.
- 고정 모의실험의 샤프 0.6 신호 검출률은 16후보 `0.840`, 64후보 `0.804`다.
- 모의 프로그램 무효 진입률 `0.138`은 진단값이며 실자본 근거가 아니다.
- `capital_entry_eligible=false`를 계약 자체에 고정했다.

## 공개 PEAD 역사 판정

- 자료: Open Source Asset Pricing 2025-10 공개 월별 PEAD 수익률.
- 자료 SHA-256:
  `sha256:6cdaf0ff051222b813e6f8404d7a30b94e146b421625f202bd04473fe97a1817`.
- 전체 감사: 후보 ID 816개·지문 816개 고유, 21가족, 가족 크기 16×11·64×10.
- 선택 후보: `pead-ann06of07-scale100-fd242e072b8b`.
- 판정: `PUBLISHED_EDGE`.
- PBO: `0.000000`.
- 홀드아웃 PSR: `0.999985`.
- 연 1.5% 비용 후 복리 초과수익: `6.892267%`.
- 연 3% 비용 스트레스: `5.3077%`.
- 연 5% 비용 스트레스: `3.2300%`.
- 최대낙폭: `13.8916%`.
- 단일 연도 집중도: `11.874%`, 상위 5개월 집중도: `7.289%`.
- 부호 반전 위약시험의 비용 후 연수익: `-9.650%`.
- 실패 관문: 없음.

시대별 연수익은 1998-2004 `13.156%`, 2005-2010 `5.897%`, 2011-2015 `8.721%`,
2016-2024 `1.936%`다. 최근 비중을 겹치지 않게 나눈 세 36개월 구간은 `3.273%`,
`2.130%`, `0.424%`로 모두 양수지만 약해지는 방향이다.

## 왜 아직 실거래가 아닌가

공개 자료는 전략 존재 가능성을 확인하는 자료이지 현재 계좌 주문 계약이 아니다.

- 이번 미리보기는 결과를 전혀 보지 않은 최종 홀드아웃이 아니다.
- 당시 구성 종목과 공시 도착시각을 계좌 주문 단위로 재구성하지 않았다.
- 상장폐지 수익률, 공매도 가능 여부와 대차비용이 현재 계좌 기준으로 확정되지 않았다.
- 정수 주, 허용 종목, 주문 한도, KIS 체결 가격과의 동등성이 없다.
- 최근 시대와 최근 세 구간에서 엣지가 줄어드는 징후가 있다.

그래서 `historical_published_edge=true`와 `capital_eligible=false`를 동시에 기록한다.
2026-09-01 이후 신규 실적발표 200건과 최소 12개월을 시점보존 방식으로 모으고, 위 실행
계약을 모두 채우기 전에는 연구 자본도 열지 않는다.

## 코드와 재현 경로

- 명세: `specs/175-pead-program-gate/`.
- 프로그램 교정: `src/auto_invest/analytics/edge_gate_calibration.py`.
- PEAD 생산자: `src/auto_invest/analytics/pead_factory.py`.
- 독립 소비자: `src/auto_invest/analytics/pead_factory_evidence.py`.
- 실행기: `scripts/pead_factory_probe.py`, `scripts/pead_evidence_gate.py`.
- 자동 실행: `.github/workflows/autonomous-strategy-factory.yml`.

로컬 공개 실행 결과는 `PUBLISHED_EDGE`, 독립 증거 `valid=true`를 재현했다. 기존
`factory_evidence_gate.py`는 `calibrated_family_entry_v31_required`로 종료코드 3을 내며
진단 `3.2`가 실자본 경로로 새지 않음을 확인했다.

## 검증과 배포

- 집중 검증: 54 passed.
- 전체 검증: 3192 passed, 6 skipped, 실패 0.
- 린트: `uv run ruff check src tests` 통과.
- 엄격 하네스: 14/14 통과.
- HANDOFF 사실 검사와 PR 품질 관문 통과.
- 기능 PR: #706.
- 기능 merge: `9d22a2b9e4cb34779f58253a87dc3f38255ea8b4`.
- main 배포 run: `33337092305`, 성공.
- 첫 전략 공장 run `33337092281`은 시도 1·2 모두 PEAD 계산에서
  `PUBLISHED_EDGE`를 재현한 뒤 게시 전 셸 검증에서 종료코드 1로 실패했다. 무출력
  문자열 `test` 묶음이라 어떤 필드가 달랐는지 남기지 못한 운영 결함이었다.
- 후속 보정은 같은 816개·21가족·상한 0.200·자본 0 계약을 숫자·불리언·`null`의
  원래 JSON 자료형으로 검사한다. 불일치 시 가족 수·타당성·안전 필드를 로그에 출력하고
  게시를 계속 실패 폐쇄한다. 기준 완화나 돈 경로 변경은 없다.
- 보정 PR #707은 merge `77d0cf896818f8c525a0485882c3d5d1dbffec7f`로 반영됐다.
  배포 run `33338912939`와 전략 공장 run `33338912951`이 성공했다.
- 공개 sidecar commit `fa0a253a5bca6028de221ed9d5362710a7ee271a`은 main commit
  `77d0cf8`을 기록하고, `PUBLISHED_EDGE`, 후보 816개·가족 21개·보수 상한 0.200,
  독립 증거 `valid=true`, 역사 엣지 참, 자본 적격 거짓을 재현한다.

배포 성공은 드라이런 작업자의 코드 반영을 뜻한다. 실거래 전환, 자본 배분, 주문 승인을
뜻하지 않는다. 컨테이너에서 서버 감사 로그와 GitHub Actions 요약의 작업자 배포 커밋을 직접
대조할 수 없으므로 그 부분은 운영자 전용 관찰면으로 남긴다.

## 다음 우선순위

1. 2026-09-01부터 SEC 공시 수신시각을 보존한 신규 이벤트 장부를 누적한다.
2. 최소 200개 실적발표·12개월 전에는 통계 결론이나 자본 승격을 내리지 않는다.
3. 상장폐지 포함 수익률, 공매도 대차, 정수 주문, 현재 KIS 체결 동등성을 별도 명세로
   구현하고 독립 소비자가 다시 계산하게 한다.
