# HANDOFF-161 - 독립 원자재 기간구조 전략 공장과 672회 검정

## 상태

PR #667이 `main`에 병합됐고 배포, 생산 전략 공장, 후속 자동 작업, KIS 실연결
읽기 전용 점검이 모두 성공했다. 생산 판정은 `NO_FACTORY_EDGE`이므로 권위 상태는
`PREVIEW_ONLY`, `armed:false`, 단 0/5, 자본 0이며 실주문은 불가능하다.

## 무엇을 만들었나

- BlackRock iShares API의 GSG 펀드와 S&P GSCI 총수익 기준지수, 세계은행 Pink Sheet의
  원자재 현물 총지수, FRED DGS3MO를 월 단위 시점 기준으로 결합한다.
- 총수익에서 현물수익과 이전 달까지 알려진 3개월 국채 현금수익을 뺀 값을 선물 곡선의
  캐리·롤수익 대용 신호로 정의했다. 현물과 총수익이 같은 원자재 묶음을 완전히 복제하지
  않는 기초 위험은 명시적으로 남겼다.
- 캐리 양수, 캐리+모멘텀, 캐리 순위, 방어 캐리의 3/12개월과 GSG 최대 50/100% 조합
  총 16개를 만들었다. 거래비용은 10/25/50bp로 고정했다.
- 공통 자료 240개월 중 개발 96개월에서 후보 하나를 고정하고 한 달 격리 뒤 2014-10부터
  2026-07까지 142개월 홀드아웃에서는 후보를 다시 고르지 않는다.
- 과거 656개와 현재 원자재 16개를 합친 672개 고유 감사 장부를 보존한다. 현재 가족의
  유효 독립 시도 수는 생산에서 8.620968이다.

## 계산 결함 교정

첫 로컬 배관 재생은 담보 현금수익을 차감하지 않아 곡선 수익과 담보 이자를 섞었고
PSR 0.757994를 냈다. 이 값은 유효한 전략 결과가 아니므로 672개 감사 장부, 종이 후보,
라이브 승격에 넣지 않았다. 후보 문법, 비용, 개발·격리·홀드아웃 구간, 합격 기준을
그대로 둔 채 현금수익을 차감하는 v2 정의를 결과 전에 다시 사전등록하고 재실행했다.

## 생산 결과

- main merge: `7fe7334`, 기능 커밋 `cbbadd1`, PR #667.
- 전략 공장 run `32759149869`, 생산 시각 `2026-08-24T17:58:30Z`.
- 감사 수: 과거 656, 원자재 공식 16, 합계 672, 고유 전략 지문 672.
- 판정: `NO_FACTORY_EDGE`, 선택 후보 없음, 배포 설정 없음, 종이 후보 없음.
- 개발 선택 후보: `commodity-carry_rank-9d2f0baf1d27`.
- 홀드아웃 PSR `0.611866 < 0.95`, 종이 기준 `0.80`에도 미달했다.
- 50bp 비용 후 연 초과수익 `-0.641336%`, 혼합 샤프 변화
  `-0.183303 < +0.05`다.
- 기존 전략 상관은 `0.134074`로 낮고 혼합 낙폭은 14.842601%에서 11.707921%로
  좋아졌지만, 음의 초과수익과 샤프 하락을 상쇄하지 못했다.

## 관문 진단

- 후보 16개 가족의 고정 시드 500회 무신호 거짓 합격률은 5.0%다.
- 참 연율 샤프 0.60 신호의 라이브 검출률은 84.8%다.
- 따라서 실용적으로 의미 있는 신호를 거의 영구 탈락시키는 관문은 아니다.
- 이번 후보는 개발과 홀드아웃의 신호 방향이 뒤집혔고 비용 후 초과수익도 음수다.
  기준을 낮추는 대신 독립된 설명 자료를 쓰는 다음 전략군으로 이동한다.

## 배포, 계좌와 주문

- deploy run `32759149802`: merge `7fe7334` 배포 성공.
- released-work run `32759149819`, autonomous-work run `32759149845`: 성공.
- KIS smoke run `32759829708`: main `7fe7334`, 5/5, 키 유효, 현금 934.27달러,
  NAV 1457.59달러, 기존 외부 보유 ORANY 28주, 최근 주문 0건, 열린 미체결 0건.
- GSG를 라이브 허용목록에 넣지 않았고 자본 배분, 실제 주문, 전략 교체가 없었다.

## 검증

- 머지 직전 `uv run pytest -q`: 3002 passed, 5 skipped.
- 건너뛴 5개 KIS 실연결 시험은 머지 뒤 생산 smoke에서 5/5 통과했다.
- `uv run ruff check src tests`: 통과.
- 집중 13개, workflow YAML, `git diff --check`: 통과.
- `uv run python scripts/agent_harness_probe.py --strict`: OK 14/14.
- `uv run python scripts/check_handoff_facts.py`: 기능 머지 전 기준 OK. 이 인계에서
  새 main 행을 반영한 뒤 다시 확인한다.
- PR #667 품질 관문과 merge 가능 상태 확인 뒤 merge 방식으로 병합했다.

## 안전 경계

라이브 PSR 0.95, 종이 PSR 0.80, K1 포지션 한도, K2 허용 종목, 손실 예산 20%,
정규장, 현금 1% 여유, production 기계 승인, 서명·nonce, 추가 전용 감사 로그를
유지한다. 실제 주문, 자본 배분, 라이브 전략 교체, 헌법·커널 변경은 발생하지 않았다.

## 다음 세션 판단

같은 기간구조 16개를 다시 돌리거나 홀드아웃에서 좋아 보인 후보로 교체하면 최종 시험을
학습 자료로 오염시키므로 금지한다. 다음 신규 전략군은 독립 원자재 재고·포지셔닝이다.
CFTC 거래자 포지션, EIA 에너지 재고, USDA 농산물 재고처럼 가격 총수익과 다른 원천을
먼저 고정하고, 게시 지연과 개정 이력을 반영한 시점 기준 자료 계약을 새 SDD로 만든다.
실제 전략 체결과 양의 전략 손익 전에는 돈을 벌었다고 보고하지 않는다.
