# HANDOFF-172 - 전략 합격 경로 감사와 연구 교착 해소

## 상태

PR #697이 `main`의 `cb14c9e`로 병합됐고 배포 실행 `33284833563`이 성공했다.
새 레짐 후보 전진 관찰 실행 `33284833549`도 성공했다. 이 변경은 연구 판정과 읽기 전용
관찰만 다루며, 자본 배분·실주문·라이브 전략은 바꾸지 않았다.

## 운영자 질문에 대한 결론

“수많은 전략 중 마땅한 전략이 하나도 없다”는 표현은 정확하지 않다. 역사적으로 유망한
전략은 있고, 이번 레짐 후보도 8개 관문 중 7개를 통과했다. 정확한 현재 결론은
“실자본을 맡길 만큼 전진 검증까지 끝난 전략은 아직 없다”이다.

동시에 합격 판정 경로에도 실제 오류가 있었다.

1. 기존 84% 검출력 수치를 실제 8관문 전체의 검출력처럼 읽을 여지가 있었지만, 직접
   교정된 것은 가족 PBO와 홀드아웃 PSR 두 관문뿐이었다.
2. 48관측 전진 교정은 상대 비교만으로 `CALIBRATED`가 될 수 있어, 절대 검출력이 낮아도
   합격 기준이 충분한 것처럼 보일 수 있었다.
3. 신규 연구 후보 ID에 관측 수 버킷이 들어가 같은 의미의 연구가 관측 증가 때마다 다른
   후보가 되고, 출시 억제가 풀리는 교착 경로가 있었다.
4. 역사 관문 7/8인 고정 레짐 후보를 미래 자료로 계속 관찰하는 전용 계약과 자동화가 없었다.

따라서 운영자의 “오류가 있거나 지나치게 둔한 것 아니냐”는 문제 제기는 일부 맞았다.
오류는 기준을 낮추는 방식이 아니라, 검정력 범위와 관찰 상태를 더 정직하게 만드는 방식으로
고쳤다.

## 구현한 변경

- `forward_gate_calibration.py`는 paper와 live의 심은 신호 검출률이 각각 절대 80% 이상일
  때만 `CALIBRATED`를 낸다. 유효한 계산이지만 부족하면 `UNDERPOWERED`로 정상 종료해
  관찰은 계속하되 자본 근거로 쓰지 못하게 한다.
- `strategy_acceptance_path_audit.py`와 명령줄 진입점은 실제 레짐 8관문을 다시 검사하고,
  기존 교정의 직접 범위를 `family_pbo`, `holdout_active_psr`로 한정해
  `PARTIAL_COVERAGE`를 발행한다.
- `regime-corr24-thr0p2-weak6-cash`를 후보 지문
  `sha256:f006b817472a487f5d115fc9b833cf3d850e7e092bf576826db954c913adf512`,
  동결일 2026-07, 최소 20관측으로 고정했다.
- 월별·push 자동화 `regime-challenger-forward-observation.yml`은 공개 자료만 읽고 별도
  sidecar를 발행한다. 브로커·SSH·주문·자본·라이브 설정에 접근하지 않는다.
- 자율 작업 후보 ID에서 `n_obs // 5`를 제거하고 의미 기반 버전
  `independent-edge-family-v2`를 사용한다. 관측 4→5 변화로 같은 작업을 새 후보처럼
  만들지 않는다.

## 현재 전략 증거

### `regime-corr24-thr0p2-weak6-cash`

- 역사 관문: 7/8.
- 실패 관문: `recent_segment_wins`.
- 개선 발생: 홀드아웃 234개월 중 3개월.
- 전체 합격 경로 교정: `PARTIAL_COVERAGE`.
- 48관측 검출력: paper 42.40%, live 15.45%, 요구 80%.
- 전진 관찰: 동결 뒤 0/20, `OBSERVATION_WAIT`.
- 결론: 역사적으로 유망하지만 연구 합격·실자본 적격은 아니다.

### `globalfixed-ensemble-3-6-9-12`

- 역사 검증은 통과했다.
- 최신 자본 경로 전진 관측: 4/20.
- 상태: `ACCUMULATING_EDGE`, live `PREVIEW_ONLY`.
- 결론: 기다림은 자동으로 계속하지만 아직 자본을 열 근거는 아니다.

## 생산 상태와 다음 우선순위

- 기능 merge: `cb14c9e`.
- deploy-on-merge: 실행 `33284833563`, 성공.
- 레짐 관찰: 실행 `33284833549`, 성공.
- 자동 작업 후보: `candidate-parallel-edge-challenger-d59c4161bcba`,
  `EXECUTION_READY`.
- 다음 작업: 기존 후보의 관찰 시계를 건드리지 않고, 공개 데이터와 시간 분리
  walk-forward를 사용하는 독립 신호군을 별도 연구한다. 통과하지 못하면 라이브 승격 없이
  기각한다.

KIS smoke 최신 sidecar는 2026-08-29 예약 실행에서 6/6 성공했지만 대상 커밋이
`320cf14`라 #697 이후 배포 확인으로 과장하지 않는다. #697의 배포 성공은 GitHub 배포
실행으로 확인했고, 다음 예약 KIS smoke가 최신 코드에 대한 별도 브로커 연결 증거가 된다.

## 검증

- #697 기능 브랜치: 3138 passed, 6 skipped.
- 최신 main 인계 브랜치 재검증: 3139 passed, 6 skipped.
- `uv run ruff check src tests`: 통과.
- 관련 최종 테스트: 5 passed.
- 엄격 하네스: 14/14.
- HANDOFF 사실 검사: 통과.
- PR 품질 관문 실행 `33284811206`: 통과.
- 배포 실행 `33284833563`: 통과.
- 레짐 관찰 실행 `33284833549`: 통과.

## 안전 경계

연구 자본 10%, 20% 이상 승격, 손실 예산 20%, 정규장, 킬스위치, 주문·노출 한도,
`Backtest -> Canary -> Full` 순서는 바꾸지 않았다. 새 자동화는 주문 0건, 자본 변경 없음,
라이브 전략 변경 없음을 결과에 명시한다. 현재 자본은 0이고 실제 주문도 0건이다.
