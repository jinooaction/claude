# HANDOFF 049 — 스펙 046: 일일 전략 모니터 (지속 감시 대시보드, 2026-06-06)

main 머지 `a36ea23`(PR #211). 운영자 지시: "응 이어서 자율 수행해. 세계 최고 수준으로 돈 벌자."

## 한 줄 요약

검증된 스펙(042 추세신호 · 043 분산 · 044 레버리지 복리 · 045 regime)을 합쳐, forward 페이퍼
트랙이 돌 때마다 운영자가 한눈에 확인하는 *일일 전략 대시보드*를 사이드카에 찍는다. 읽기 전용·
돈 0 이동. 라이브 레버리지/무장은 여전히 운영자 게이트(감시 보고이지 거래 변경이 아님).

## 매 실행마다 답하는 네 가지 (현재 스냅샷, 2026-05)

```
① 엣지(분산 추세)가 최근에도 유효한가:
   최근 5년 샤프 +1.70 | 최근 10년 샤프 +1.76 | 최근 5년 낙폭 +3.63%
② 분산 가정이 지금 신뢰 가능한가 (주식·채권 상관):
   현재 -0.03 | 최근 5년 평균 +0.10 → 판정: DIVERSIFICATION_WEAKENED
③ 낙폭 예산 15%에서 레버리지 복리 권고 (최근 25년 기준):
   무레버 복리 +7.89% → 권고 L=2.0 → 복리 11.5%/년 (낙폭 13%)
④ 오늘 추세 신호 (S&P vs 10개월 SMA): 투자(추세 위) (갭 +8.74%)
```

낙폭 예산별 레버리지 복리: 15% L=2.0 → 11.5%/년, 20% L=3.0 → 14.9%, 25% L=3.5 → 16.6%.

## 핵심 — 레버리지 권고를 *최근 25년* 기준으로 (운영자 지적 일관 적용)

스펙 044 레버리지 권고를 처음 전체 1871~ 로 계산했더니 낙폭 예산 15%서 *레버리지를 줄이라*
(L=0.5)고 나왔다 — 대공황 낙폭(18%)에 묶인 것 = 운영자가 지적한 "먼 과거 기준" 실수의 재발.
**최근 25년(2000~, 닷컴·GFC·코로나·2022 포함)** 으로 고쳐 일관성 확보(최근이라 regime 관련,
~25년이라 꼬리위험 과소평가 안 함). `leverage_window_years` 파라미터로 노출.

## 왜 지속 감시가 세계 최고 수준의 방어선인가

스펙 045 가 밝혔듯 정적 분산 가정은 regime 따라 깨진다(2022 상관 양수). 일회성 분석으론 그
변화를 놓친다. 이 대시보드는 forward 페이퍼가 도는 매 거래일 갱신되어 엣지 쇠퇴(샤프 하락)·
상관 양수 전환(WEAKENED)·오늘 신호를 조기 경보 → regime 비정상성 *상시* 방어선. 사이드카
`automation/rebalance-paper-forward-last-run:LAST_RUN.md` 의 "🔭 일일 전략 모니터" 섹션.

## 배선 (안전)

- 러너 로컬 스텝: `astral-sh/setup-uv` + `scripts/strategy_monitor_probe.py`(Shiller 데이터만
  받아 계산, 워커 SSH 불필요). 완전 격리(`continue-on-error: true` + `|| true`)라 setup 이나
  프로브가 실패해도 ARM A/B/C/D forward 트랙을 절대 못 깨뜨린다.
- 사이드카 publish 스텝에 모니터 섹션 추가(`cat /tmp/monitor.txt`, 같은 job 이라 파일 공유).

## 안전 경계 (지킨 것)

읽기 전용·측정 전용 — 주문 0건, 돈 0 이동, **Kernel 터치 0건**. **라이브 레버리지/무장 변경
없음**(권고/감시이지 거래 변경 아님 — 라이브는 운영자 게이트 헌법 X.4, K1 캡 노출≤100% 불변).
전체 1590 통과·4 스킵, 린트 깨끗, YAML 유효. 신규 테스트 5건.

## 다음 (운영자 결정 / 자율 후보)

1. **운영자 결정 — 라이브 적용**: 대시보드 권고 레버리지(낙폭 예산 15% → L=2.0 → 복리 ~11.5%)를
   실제로 적용할지. K1 캡(노출≤100%)을 바꾸므로 운영자 게이트. 가드형 채널로 배선 가능(예산·
   상한 지정 시).
2. **자율 후보**: 대시보드를 우리 forward NAV 트랙(KIS 실측)에도 적용 — Shiller 베타가 아니라
   우리 실제 체결 기준 레버리지·복리 권고(NAV 데이터 누적 후). forward-verdict 섹션과 상호 보완.
3. **사이드카 확인**: 다음 forward 실행(평일 22:30 UTC) 후 LAST_RUN.md 의 "🔭 일일 전략 모니터"
   섹션이 찍히는지 확인.
